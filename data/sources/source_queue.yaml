# CONTRACT 5 (v3) — Path C PHASE A source queue
# ===========================================================================
# This REPLACES v2's source_metadata.yaml with the concrete PHASE A ingestion
# queue from Proposal v3 §5 / Alpha v3 §3. Each entry is one Path C Phase A
# source: what evidence classes it carries, its license + is_open status, the
# sampled area (for derivable kg/m²), and how to normalize it.
#
# Track E writes a SourceAdapter per entry (against SYNTHETIC data first);
# Track G runs the downloads and fills the null fields. Only is_open=true
# sources may enter a PUBLISHED run.
#
# Phase B ([25]-[46]) and Phase C (targeted requests) are OUT of the alpha and
# NOT listed here.
#
# ***DATA REALITY (recap):*** DeepData's abundance/grade layer is CONFIDENTIAL
# and has NO public API. Nodule abundance comes from these OPEN sources. Public
# DeepData layers (APEI/contract polygons) + GEBCO bathymetry are context only.
#
# metadata_version 2 -> 3.
#
# metadata_version 3 -> 4 (P2.0c, 2026-08-08): METADATA-ONLY. Added origin
# markers (engine/prospectivity/provenance/origin.py): file-level
# `data_origin`/`author` below, and `data_origin:` per source entry declaring
# the origin class of the DATA the source contributes (the .tab files
# themselves are hash-pinned and never edited — audit §6). No parameter value
# and no semantics changed; re-sync is a read, not a review.

metadata_version: 4

# P2.0c origin declaration for the FILE (this catalog document itself). The
# file's effective origin is COMPUTED by combine_origins over the entry
# declarations below plus this remainder — never hand-written.
data_origin: AUTHORED
author: unrecorded

# ---- normalization defaults referenced by Contract 7 (normalization.yaml) ----
# Box cores in this queue are 50 x 50 cm = 0.25 m^2 unless a row says otherwise.
box_core_area_m2_default: 0.25

sources:

  # ===================== PHASE A — [MASS] training sources =====================

  - source_id: src_so268_boxcore            # [01]
    data_origin: MEASURED                     # real downloaded PANGAEA dataset; hash in manifest.json
    title: "SO268 box-core nodule summary (BGR & GSR areas)"
    doi: "10.1594/PANGAEA.904967"
    url: "https://doi.org/10.1594/PANGAEA.904967"
    source_type: dataset
    evidence_classes: [MASS, COUNT, COVER]
    primary_use: TRAIN                        # MASS rows train the model
    is_open: true
    license: "CC-BY-NC-4.0"
    sampled_area_m2: 0.25                     # → kg/m2 = total nodule mass / 0.25 (×4)
    derivation: "abundance_kg_m2 = nodule_mass_kg / sampled_area_m2"
    accessed_date: null                       # [GEOLOGY — ISAAC] fill on download
    # Filled P2.0d-2 (2026-08-09): recomputed from the downloaded file's bytes
    # and verified against manifest.json's input_content_hash. This is the
    # RECORDED evidence the production guard re-hashes against — MEASURED's
    # proof, per the contract's own fill-on-download lifecycle.
    content_hash: "sha256:36078e69a22f0413ace0bc7704a076fc4e179bf5416f9ff55a08806a7a1138f0"
    notes: "First modern contractor-area table. Keep event_id; COVER column stays COVER (not converted)."

  - source_id: src_domes_fewkes1980          # [02]
    data_origin: MEASURED                     # published measurement dataset (DOI); hash on download
    title: "DOMES floor-density measurements, Fewkes et al. (1980)"
    doi: "10.1594/PANGAEA.878220"
    url: "https://doi.org/10.1594/PANGAEA.878220"
    source_type: dataset
    evidence_classes: [MASS]
    primary_use: TRAIN
    is_open: true
    license: "CC-BY-3.0"
    sampled_area_m2: null                     # already kg/m2 — no area needed
    derivation: "reported directly (kg/m2); confirm wet/dry basis"
    accessed_date: null
    content_hash: null
    notes: "Historic, already-normalized kg/m2. High-value training data."

  - source_id: src_domes_piper1979           # [03]
    data_origin: MEASURED                     # published measurement dataset (DOI); hash on download
    title: "DOMES abundance & seafloor observations, Piper et al. (1979)"
    doi: "10.1594/PANGAEA.880886"
    url: "https://doi.org/10.1594/PANGAEA.880886"
    source_type: dataset
    evidence_classes: [MASS, COVER]
    primary_use: TRAIN
    is_open: true
    license: "CC-BY-3.0"
    sampled_area_m2: null
    derivation: "MASS reported as kg/m2; COVER stays COVER"
    accessed_date: null
    content_hash: null
    # WET.1 (2026-08-28): NOT INDEPENDENT OF [04]. Table 8's Site C rows for
    # cruise RP-8-OC-76 Leg 9 and [04]'s Table 1 describe THE SAME 19 PHYSICAL
    # BOX CORES -- proven by set identity (the two box-core sets are equal once
    # [04]'s two "recovery probably incomplete" cores are removed), by the
    # station-to-box-core mapping, and by the shipboard label in [04]'s Fig. 6
    # ("RP-8-OC-76 LEG 9 / STATION 12 BC 18"). This chapter is pp. 437-474 of
    # Bischoff & Piper (eds.), Plenum 1979; [04] Table 1 is footnoted "From
    # Sorem et al. 1979a", pp. 475-527 of THE SAME VOLUME. This chapter cites it
    # as "(Sorem et al., this volume)" (p. 456) and credits Sorem for the box-core
    # photographs its coverage column rests on (Fig. 4 caption, p. 453).
    # WET/DRY BASIS: UNRESOLVED (verdict AMBIGUOUS). Table 9's caption (p. 458)
    # states its column-9 average "was lowered by 20% to give a nodule
    # concentration on a dry-weight and salt-free basis" -- so Table 9 col 9 is
    # dry+salt-free and its inputs are not. Table 8's own Concentration column
    # has NO stated basis, and is NOT uniformly a weighed box-core mass: Table 9
    # prints "-" in the box-core Weight column for box cores 11 and 23 while
    # Table 8 prints 9.6 and 6.5 for them. Detail: docs/walkthroughs/WET.1.md.
    notes: "Compare events against [02] before dedup (DOMES family, rule 1). NOT INDEPENDENT of [04] -- same 19 RP-8-OC-76 Leg 9 box cores; never count both as separate MASS stations. abundance_basis UNKNOWN, not guessed."

  - source_id: src_domes_sitec_sorem1989     # [04]
    data_origin: MEASURED                     # published measurement dataset (DOI); hash on download
    title: "DOMES Site C nodule size & weight, Sorem (1989)"
    doi: "10.1594/PANGAEA.879534"
    url: "https://doi.org/10.1594/PANGAEA.879534"
    source_type: dataset
    evidence_classes: [MASS, COUNT]
    primary_use: TRAIN
    is_open: true
    license: "CC-BY-3.0"
    # FILLED WET.1 (2026-08-28) from the primary document, not assumed: Sorem
    # 1989, Marine Mining 8(2), printed p. 193 -- "it is realized that the area
    # of seabed represented by each box core is only 0.25 m2." Corroborated on
    # printed p. 191: the sampler is "a stainless steel sample box 50 cm square"
    # (0.5 m x 0.5 m = 0.25 m2). Two statements, one number, no ambiguity.
    sampled_area_m2: 0.25
    derivation: "group by event, sum size-class weights, divide by 0.25 m2 (DERIVE)"
    accessed_date: null
    content_hash: null
    # WET.1 (2026-08-28): NOT INDEPENDENT OF [03]. Table 1 is footnoted "From
    # Sorem et al. 1979a" = Sorem, Fewkes & McFarland 1979a, pp. 475-527 of
    # Bischoff & Piper (eds.), "Marine Geology and Oceanography of the Pacific
    # Manganese Nodule Province", Plenum -- the COMPANION CHAPTER to [03]
    # (pp. 437-474) in the same edited volume, one of whose editors is Piper.
    # The two tables describe THE SAME 19 PHYSICAL BOX CORES (see [03]'s row).
    # TABLE 1 SHAPE, transcribed and verified at WET.1: 22 printed rows, box
    # cores 6-28 with NO row for 26; 21 carry data; core 17 carries none
    # ("Core reserved by NOAA-DOMES for biological research"); cores 14 and 25
    # are footnoted "Recovery probably incomplete" -- both absent from [03].
    # The Total-number column sums to 4113 over the 21 data rows, matching
    # Sorem's own prose on p. 193.
    # WET/DRY BASIS: UNRESOLVED (verdict AMBIGUOUS). Table 1 states grams, with
    # no basis given anywhere in the paper. Detail: docs/walkthroughs/WET.1.md.
    notes: "DERIVE. Area VERIFIED 0.25 m2 (p.193). NOT INDEPENDENT of [03] -- same 19 RP-8-OC-76 Leg 9 box cores; never count both as separate MASS stations. abundance_basis UNKNOWN, not guessed."

  - source_id: src_so268_nodules             # [05]
    # Row count corrected 2026-07-29 (P3) from "~9,000 nodules": the actual
    # downloaded file (data/sources/SO268-bc-nodules-PANGAEA-904962.tab) has
    # 1,658 data rows across 36 box-core events. The ~9,000 figure was an
    # estimate made before the download and was never true of this dataset.
    data_origin: MEASURED                     # real downloaded PANGAEA dataset; hash in manifest.json
    title: "SO268 individual nodule measurements (1,658 rows / 36 events)"
    doi: "10.1594/PANGAEA.904962"
    url: "https://doi.org/10.1594/PANGAEA.904962"
    source_type: dataset
    evidence_classes: [MASS, COUNT]
    primary_use: TRAIN_AGGREGATE              # aggregate to EVENT, then train
    is_open: true
    license: "CC-BY-NC-4.0"
    sampled_area_m2: 0.25
    derivation: "aggregate nodules to event → event mass; kg/m2 = event_mass / 0.25; also yields mean_nodule_mass_g"
    accessed_date: null
    # Filled P2.0d-2 (2026-08-09): recomputed from bytes, verified against the
    # manifest — the guard's recorded evidence (see [01]'s note).
    content_hash: "sha256:af06fbda795f08c9d1f3f069c6874d2c6408750a3c88a9412fdfb1226588d12f"
    notes: "Nodules are NESTED in [01] events (dedup rule 4). The EVENT is the spatial sample, not each nodule. Also the source of mean_nodule_mass_g for COUNT→MASS."

  - source_id: src_dryad_chamber             # [06]
    data_origin: MEASURED                     # published measurement dataset (DOI); hash on download
    title: "CCZ benthic-chamber experiments & nodule abundance (Dryad)"
    doi: "10.5061/dryad.tdz08kq6w"
    url: "https://doi.org/10.5061/dryad.tdz08kq6w"
    source_type: dataset
    evidence_classes: [MASS]
    primary_use: TRAIN                         # if station + chamber footprint recoverable
    is_open: true
    license: null                              # [GEOLOGY — ISAAC] confirm Dryad license
    sampled_area_m2: null                      # chamber footprint — record per experiment
    derivation: "kg/m2 = nodule mass / chamber footprint (DERIVE); do not merge replicates blindly"
    accessed_date: null
    content_hash: null
    notes: "Experimental. Retain chamber footprint; treat replicates carefully."

  # ================= PHASE A — [COVER]/[COUNT] covariate sources ================
  # Ingested and TAGGED; used as covariates/context. NEVER converted to kg/m2.

  - source_id: src_amon2016_frames           # [11]
    data_origin: MEASURED                     # published image-derived measurement dataset; hash on download
    title: "Amon et al. 2016 UK-1/EPIRB image-frame nodule data"
    doi: null
    url: "https://www.nature.com/articles/srep30492"
    source_type: paper_supplement
    evidence_classes: [COUNT, COVER]
    primary_use: COVARIATE
    is_open: true
    license: "CC-BY-4.0"
    sampled_area_m2: null                      # frame area — keep per frame
    derivation: "NONE for kg/m2 (image metrics only)"
    accessed_date: null
    content_hash: null
    notes: "Supplementary Table S3. Keep image-derived metrics separate from recovered mass."

  - source_id: src_apei6_cover               # [12]
    data_origin: MEASURED                     # published image-derived measurement dataset (DOI); hash on download
    title: "APEI-6 nodule-cover gradient, Simon-Lledó et al. (PANGAEA)"
    doi: "10.1594/PANGAEA.893220"
    url: "https://doi.org/10.1594/PANGAEA.893220"
    source_type: dataset
    evidence_classes: [COVER]
    primary_use: COVARIATE
    is_open: true
    license: null                              # [GEOLOGY — ISAAC] confirm
    sampled_area_m2: null
    derivation: "NONE for kg/m2 (cover is a covariate)"
    accessed_date: null
    content_hash: null
    notes: "Image-derived cover. Preserve transect/image IDs."

  - source_id: src_gsr_mendeley_cover        # [14]
    data_origin: MEASURED                     # published measurement dataset; hash on download
    title: "GSR collector-test site nodule coverage (Mendeley Data)"
    doi: null
    url: "https://data.mendeley.com/datasets/7jst5wyc6j/1"
    source_type: dataset
    evidence_classes: [COVER]
    primary_use: COVARIATE
    is_open: true
    license: "CC-BY-4.0"
    sampled_area_m2: null
    derivation: "NONE for kg/m2"
    accessed_date: null
    content_hash: null
    notes: "Separate pre-impact / impact / post-impact records."

  # ===================== PHASE A — [GRID] priors / benchmark ===================
  # Compiled/interpolated. Priors + benchmark ONLY. Never independent stations.

  - source_id: src_ts6_grid                  # [18]
    data_origin: LITERATURE                   # compiled model product from a publication, not a measurement here
    citation: "ISA Technical Study No. 6 (2010); specific figure/table pends the Contract 6 digitization decision (source_figure) — an open evidence gap, not guessed"
    title: "ISA Technical Study No. 6 (2010) — abundance grid"
    doi: null
    url: "https://isa.org.jm/wp-content/uploads/2022/06/tstudy6.pdf"
    source_type: benchmark
    evidence_classes: [GRID]
    primary_use: BENCHMARK                     # also see Contract 6 (ts6_reference)
    is_open: true
    license: "ISA publication — benchmark use, not redistributed as data"
    sampled_area_m2: null
    derivation: "compiled 0.1deg grid; observation_or_prediction = compiled"
    accessed_date: null
    content_hash: null
    notes: "Benchmark surface for compare_to_ts6 (Contract 6). Never a training station."

  - source_id: src_washburn2021_grid         # [19]
    data_origin: LITERATURE                   # derived/compiled published points ("largely derived from TS-6 + Morgan")
    citation: "Washburn et al. 2021, Front. Mar. Sci. 8:661685, supplementary 0.5-degree resource points"
    title: "Washburn et al. 2021 — regional 0.5° resource points"
    doi: "10.3389/fmars.2021.661685"
    url: "https://www.frontiersin.org/articles/10.3389/fmars.2021.661685/full"
    source_type: dataset
    evidence_classes: [GRID, GRADE]
    primary_use: PRIOR
    is_open: true
    license: null                              # [GEOLOGY — ISAAC] confirm (supplement)
    sampled_area_m2: null
    derivation: "largely derived from TS-6 + Morgan; observation_or_prediction = interpolated"
    accessed_date: null
    content_hash: null
    notes: "Regional prior. Record inherited source per point; NOT independent stations."

  # ===================== PHASE A — public CONTEXT (not samples) =================

  - source_id: src_bathymetry_primary
    # CLASSIFIED AT DOWNLOAD (Karl, G.3, 2026-08-24): DERIVED — decided by
    # the source's own words, not our reasoning about it. GEBCO's terms of
    # use (data/bathymetry/GEBCO_Grid_terms_of_use.pdf, tracked): the grid is
    # "created by interpolating, applying algorithms and mathematical
    # techniques to bathymetric data"; "GEBCO considers the GEBCO Grid to be
    # an information product"; GEBCO "does not provide the underlying source
    # bathymetric data"; and the grid's resolution "may be significantly
    # different to that of the resolution of the underlying measured data".
    # Under TAX.1's rule (what the artifact IS decides its class), a global
    # interpolated product is DERIVED; the TID composition corroborates
    # independently (73.146% of the subset box is predicted, not measured).
    # The publisher's derivation is the recorded evidence — documented in
    # GEBCO_Grid_documentation.pdf (tracked), summarized in `derivation`.
    data_origin: DERIVED
    title: "GEBCO_2026 Grid (bathymetry + TID companion), CCZ subset N25/S0/W-160/E-110"
    doi: "10.5285/4f68d5c7-45eb-f999-e063-7086abc036fa"
    url: "https://download.gebco.net/"          # Grid Subsetting App
    # THE SUBSET BBOX IS WHAT MAKES THE FILES REPRODUCIBLE FROM THE SOURCE:
    # the release is fixed at the DOI, so release + bbox -> the same subset.
    subset_bbox: {north: 25.0, south: 0.0, west: -160.0, east: -110.0}
    source_type: dem
    evidence_classes: []                        # covariate raster, not an observation
    primary_use: COVARIATE
    is_open: true                               # PUBLIC GEBCO-class, NOT DeepData confidential bathy
    # Verified against the shipped terms PDF (G.3; CORRECTED at the G.3
    # approval, 2026-08-24 — the record below had TWO obligations where the
    # terms state THREE; the missing one is the clause about not
    # misrepresenting the source, and a licence obligation recorded nowhere
    # cannot be honored by anyone reading the record). Users are free to
    # copy, publish, distribute, transmit and commercially exploit. Users
    # MUST, all three verbatim in the terms PDF and §7.2 of the
    # documentation: (1) acknowledge the source in the stated attribution
    # form; (2) not use The GEBCO Grid in a way that suggests any official
    # status or that GEBCO, the IHO or IOC endorses any particular
    # application; (3) NOT MISLEAD OTHERS OR MISREPRESENT THE GEBCO GRID OR
    # ITS SOURCE. Also: NOT for navigation or any purpose involving safety
    # at sea.
    license: "GEBCO terms of use (public; redistribution and commercial use permitted; three obligations — attribution, no implied endorsement, no misrepresentation of the Grid or its source — see comment)"
    sampled_area_m2: null                       # not a sampler
    derivation: >-
      Publisher's (GEBCO Compilation Group): global bathymetric grid
      interpolated from heterogeneous soundings — satellite-gravity-guided
      prediction over sparse areas, remove-restore blending of Seabed 2030
      regional grids over surveyed ones — per GEBCO_Grid_documentation.pdf.
      Per-cell source class in the TID companion grid (code table: the
      documentation's section 3.0). Not a derivation we performed; the
      publisher's, cited with its recording artifact shipped.
    accessed_date: "2026-08-24"
    # TWO FILES UNDER ONE ROW, hashed separately. THE LEDGER IS THE HASH
    # OBSERVER, NOT THE AUDIT (TRACK-G.md §0.5, probed): the origin audit has
    # no MEASURED/DERIVED hash branch, so a clean audit is NOT
    # hash-verification — these recorded values, asserted by
    # tests/test_bathymetry_ledger.py whenever the files are present, are
    # what make the declaration mean anything. The rasters are deliberately
    # untracked (.gitignore `data/bathymetry/*.tif`, Phase 0); re-obtain via
    # doi + subset_bbox and verify against these hashes.
    content_hash:
      gebco_2026_n25.0_s0.0_w-160.0_e-110.0_geotiff.tif: "sha256:beba30531d066b0b370a49a1a8fbb22af7beaa0daab014563f0a20287df686d9"
      gebco_2026_tid_n25.0_s0.0_w-160.0_e-110.0_geotiff.tif: "sha256:674fa459dcab2b03129a5b7e160e10e16274b8d38223e6ee928b764e6195a77f"
    citation: "GEBCO Compilation Group (2026) GEBCO 2026 Grid (doi:10.5285/4f68d5c7-45eb-f999-e063-7086abc036fa)"
    notes: "Public global bathymetry (GEBCO). NOT DeepData confidential processed bathymetry. The TID grid is ITS OWN ARTIFACT (provenance metadata about the bathymetry, classified in its own right — both DERIVED, each with its own derivation; see data/bathymetry/data_origin.yaml comments)."

  # -------------------------------------------------------------------------
  # THE AOI ITSELF (Contract 2). Filled at download, G.2, 2026-08-25.
  # NOT the same source as src_deepdata_public_context below: this is Marine
  # Regions' redistribution of the ISBA-document limits (the MANAGEMENT AREA);
  # that row is ISA DeepData (the APEI / contract polygons, the exclusions
  # half), still open. Two publishers, two rows — merging them would have left
  # the APEI source with no row at all.
  - source_id: src_ccz_boundary_marineregions
    data_origin: LITERATURE                     # G.2: the artifact is a boundary DECREED in ISBA
                                                # instruments and digitised from them. NOT MEASURED —
                                                # nothing in its chain was measured, and the sha256
                                                # proves only WHICH COPY we hold. NOT DERIVED — that
                                                # requires MEASURED inputs, and there are none (GEBCO's
                                                # DERIVED rests on measured soundings; this has no
                                                # analogue). LITERATURE's evidence rule — a citation
                                                # that LOCATES the value — is the one that is satisfiable.
                                                # Karl to confirm; BACKLOG §2's classify-context-sources.
    title: "Clarion-Clipperton Zone management area (MRGID 64222)"
    doi: null                                   # the gazetteer entry has no DOI
    url: "https://www.marineregions.org/gazetteer.php?p=details&id=64222"
    wfs_request: "https://geo.vliz.be/geoserver/MarineRegions/wfs?service=WFS&version=1.0.0&request=GetFeature&typeName=MarineRegions:isa_ccz_managementarea&outputFormat=application/json"
    source_type: polygon
    evidence_classes: []                        # geometry, not observations
    primary_use: CONTEXT                        # the AOI denominator; nothing filters on it (G.2-PRE)
    is_open: true
    license: "CC-BY-4.0 (Marine Regions); underlying limits are ISA's"
    sampled_area_m2: null
    derivation: >-
      Downloaded verbatim by the WFS request above; preserved unedited at
      data/aoi/sources/ccz_management_area_mrgid64222.geojson. The Contract 2
      file data/aoi/study_area.geojson carries THE SAME GEOMETRY with ONLY the
      properties rewritten (Contract 2 needs area_id/name; the download carries
      {"dummy": 0, "mrgid": 64222}) — asserted equal by
      tests/test_study_area_aoi.py, so the edit cannot drift silently.
    accessed_date: "2026-08-25"
    content_hash: "sha256:98aa2a83ad5fe0718996756426218265dfd7016b5c53920a6f704c00fb947200"
    gazetteer_status: "Proposed standard"       # the gazetteer's own word, not ours
    gazetteer_last_edited: "2023-03-13"         # STALENESS RISK, recorded so it is checkable: the
                                                # entry cites documents up to ISBA/26/C/58; a later ISA
                                                # revision would NOT be reflected here and nothing in
                                                # this repo would notice.
    authority: >-
      A REDISTRIBUTION. ISA's own Esri shapefiles at
      isa.org.jm/exploration-contracts/maps/ are the primary record.
    citation: >-
      International Seabed Authority (2023). The geographical limits of the
      Clarion-Clipperton Zone management area, exploration areas, reserved areas
      and areas of particular environmental interest (APEIs). ISBA/17/LTC/7,
      ISBA/18/C/22, ISBA/26/C/58, ISBA/26/C/43.
    notes: >-
      MultiPolygon, 2 parts, kept as published: part 0 is a 1.41 km2 triangle
      touching the main polygon at exactly one point (its own ring-closure
      vertex) — a digitisation spur at the seam, not an island. Recorded rather
      than dropped so the geometry still verifies against the source. The file
      vouches for nothing about itself (properties are {"dummy": 0,
      "mrgid": 64222}), so this row and the in-file declaration ARE the
      provenance; there is nothing embedded to cross-check them against.

  - source_id: src_deepdata_public_context
    data_origin: null                          # [KARL] classify at download: published regulatory geometry —
                                               # MEASURED portal dataset vs LITERATURE reference product. BACKLOG §2.
    title: null                                # e.g. "ISA DeepData — APEI & contract polygons (public)"
    doi: null
    url: "https://data.isa.org.jm/isa/map"
    source_type: polygon
    evidence_classes: []
    primary_use: CONTEXT
    is_open: true                               # PUBLIC layers only
    license: null                               # [GEOLOGY — ISAAC] confirm ISA public-data terms
    sampled_area_m2: null
    derivation: null
    accessed_date: null
    content_hash: null
    notes: "PUBLIC DeepData layers only (APEI/contract polygons). Confidential geological layer EXCLUDED. No public API — browse/export."

# ---------------------------------------------------------------------------
# ENGINE OUTPUT LICENSE (makes the corpus + maps citable; underpins the funding thesis)
output_license:
  data_products: "CC-BY-4.0"                    # the corpus + prediction maps
  source_code: "Apache-2.0"
  notes: "Phase-A corpus is publishable/citable. Derived products honor upstream terms (note CC-BY-NC on [01]/[05] restricts commercial reuse of those rows)."
