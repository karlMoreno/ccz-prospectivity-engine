# E2.4 implementation audit — 2026-08-19

## VERDICT — APPROVED

- **Approved by:** Karl, 2026-08-19, via the "E2.4 AUDIT — APPROVED; VERDICT,
  DOC FIXES, AND TWO ESCALATIONS" prompt (recorded in the commit that fills
  this block).
- **Disposition:** **E2.4's implementation is APPROVED.** 16 findings, **all
  documentation-level; ZERO severity-1 defects in code.** The engine is sound
  and the numbers reproduce off-machine.
- **Fix task scope:** the doc fixes ride the commit that fills this block
  (F-1, F-2, F-3, F-4, F-5, F-11, F-13, F-15, F-16, obligation 7's column, and
  the seed-robustness record); the two escalations (rows M and N) are rewritten
  in the commit after it. Nothing in this audit required an `engine/` behaviour
  change — the only `engine/` edits are docstring text, proven documentation-only
  by an AST comparison.

### The three process facts, recorded because they are what this ledger is for

- **REPRODUCTION.** 15/15 substance fields byte-identical when regenerated
  **from a different directory**; 74/74 §3 table numbers re-derived against a
  **freshly generated** manifest; the theorem to **1e-15**; §1's per-fold
  prediction correct **5/5**.
- **K3-A — SELF-CAUGHT.** The audit's own strongest probe was **structurally
  blind**: LOCO's refusal is fold 0 on a never-fitted instance, so the probe
  could not distinguish "no stale prediction" from "no stale state existed."
  This is the **fifth instance of the fixture-degeneracy class** in this
  project and the **FIRST found in the checking apparatus rather than in the
  work**. Re-run on `leave_one_site_out` — where fold 3 refuses while the
  instance still holds fold 2's 28-station fit, the order that *can* falsify
  the claim — the property **held**.
- **F-12 REFUTED BY PROBE, not propagated.** A conformance lens claimed a
  first-fold-only assertion would survive the suite; **three tests fail**. The
  claim was plausible from reading alone and wrong; it is recorded as refuted
  rather than carried into the findings.

### The engine/-docstring judgment call, RATIFIED (Karl, 2026-08-19)

The audit's doc fixes touched three files under `engine/` — the runner's module
docstring and two stale Phase-0 docstrings — while the fix prompt's closing said
the diff should show nothing under `engine/`. **The constraint meant NO
BEHAVIOUR CHANGE, and the rule is now stated so no session re-litigates it:**

> **Docstring and comment corrections inside `engine/` are DOCUMENTATION and
> ride documentation commits — provided the proof accompanies them.** The proof
> is a comparison of the module's AST with docstrings stripped, before and
> after: identical trees mean the change cannot alter behaviour. This is the
> same class of proof as P2.0c's metadata-only contract bumps, which showed the
> corpus artifacts byte-identical (`git diff` empty) rather than asserting the
> bump changed nothing — a claim of "documentation only" is verified, not
> declared.

Applied here: `runner.py`, `domain/results.py` and `provenance/__init__.py` all
compare identical after stripping docstrings, and the suite is unchanged at
446 passed / 2 skipped. A behaviour-affecting edit could not pass that check,
which is what makes the rule safe to grant.

### Karl's note on F-1

**F-1's "3/5" travelled INTO the audit prompt from §1's summary cell — the
reviewer copied it as a verification target.** A wrong summary in a walkthrough
became a wrong expectation in the audit built to catch wrong summaries. Its
per-fold measurement was correct 5/5; only the arithmetic was wrong.

## Findings at a glance

| Sev | # | Finding | Touches |
|---|---|---|---|
| **1** | F-1 | §1B says kriging fits "3/5"; the code fits **4 of 5** (the same cell's own enumeration implies 4). Per-fold predictions were 5/5 correct | docs |
| **2** | F-2 | The runner docstring and §2 table describe a `try/except/else` that **does not exist** (guarantee holds; mechanism does not) | code comment + docs |
| **2** | F-3 | The 2B stale-reference sweep is **incomplete and undisclosed** — two Phase-0 docstrings survive in live code | code comments |
| **2** | F-4 | The §2 inventory names a test that **does not exist** (renamed in §3) | docs |
| **2** | F-5 | A biconditional the fixture only half-exercises (the floor flag is never True) | docs |
| **3** | F-6 | Obligation 7's semantics **column** is a column in one §3 table and adjacent prose in the other; no test observes the tables | docs |
| **3** | F-7 | The known-answer leakage **margin is not seed-robust** — the pinned ≥ 0.15 gap fails at 2 of 8 seeds | tests |
| **4** | F-8 | The path defect: scoped here, and the BACKLOG entry's "absolute path" **understates** it (any path *string* difference) | docs |
| **4** | F-9 | The mutation-harness rule binds **reviewers only** and omits verify-the-backup-before-writing | docs |
| **4** | F-10 | Three mutation tables, not one (IDs clean: M1–M53 + M6b, no duplicates) | docs |
| **4** | F-11 | Two inventory rows understate their tests | docs |
| — | F-12 | **REFUTED by probe** — a lens's claim that per-fold coverage is unobserved | — |
| **2** | F-16 | §1C's decision table describes the live stale-refit case as "LOCO fold B refuses after fold A fitted" — in LOCO the refusal comes **first**, on a never-fitted instance; the fit-then-refuse order lives in the SITE design | docs |
| — | **K3-A** | **A CORRECTION TO THIS AUDIT** — K3's first run proved the property on a fold order where no stale state could exist; re-run below with the dangerous order actually exercised | — |
| **4** | F-13 | The §2 mutation heading contradicts its own sub-table on the restore mechanism | docs |
| **4** | F-14 | `matrix_sha256` claims "deterministic across machines"; `tobytes()` is native byte order (latent) | code comment |
| **4** | F-15 | §3 inventory says "+8", lists seven | docs |

**No severity-1 finding touches the §3 report numbers** — those reproduce
exactly (row H). F-1 is a count in a §1 proposal table.

---

## What this document is

The **fourth** entry in the verdicts channel, and the first that audits an
IMPLEMENTATION rather than a review's disposition. E2.4 was built across
rate-limit interruptions and Karl has not independently verified Sections 2–3,
so this audit re-derives conformance from the code and the repo-resident
specification directly.

**The rule that governed every row: no claim was verified by reading the
document that makes it.** `docs/walkthroughs/E2.4.md` is the thing under
audit. For each row the implementing code and its test were read (and run),
and only then compared against what the walkthrough says. Where they disagree,
the code is the fact and the disagreement is a finding.

**Authorities used:** `docs/prompts/phase2_prompts.md` §E2.4 (revised
2026-08-14); `docs/prompts/E2.4-sequencing-prompt.md`; `docs/BACKLOG.md` §3
"E2.4 runner obligations" (verbatim); Karl's two §1 decisions.

**Performed against:** commit `ff5f2a7`, working tree clean apart from the
untracked `demo.py`. All mutation work ran against a **clone of HEAD** in a
scratch directory — the review-workflow rule's "committed state or a copy",
taken at its strongest, so that no audit write could touch the tree the
read-only lenses were reading. Suite at audit time: **446 passed, 2 skipped**
(unchanged by this audit; this document adds no code and no test).

---

## PHASE 0 — the commit map

| Commit | Claims to be | Adversarial review before it? | Rate-limit interruption recorded? |
|---|---|---|---|
| `b7b5d36` | E2.4 §1 (folds, metric policy, doc fixes, two proposals, STOP) | a lighter pre-commit pass, not the scheduled one | **Yes, two** — 3 of 4 review lenses died (HTTP 429) and were re-run; a second limit killed 14 of 42 skeptics, so 6 walkthrough-claim findings carry no verdict and were author-re-derived |
| `3ef0cc6` | E2.4 §2 (the runner) | **Yes** — snapshotted as WIP `ff2d0c6` BEFORE the review launched (the E2.3 procedure; layer (a) of the BACKLOG review-workflow entry), 5 lenses, 26 findings | **Yes** — 68 of 79 agents died; only 2 findings received refutation verdicts. Also a **mutation-harness incident** (see F-9) |
| `ff5f2a7` | E2.4 §3 (the comparison report) | not required by the prompt (§2 was the review commit) | none recorded |

Both sections completed. Suite trajectory **402 → 438 → 446**; the walkthrough
reports 402 → 438 (§2) and 438 → 446 (§3), and `446 passed, 2 skipped`
reproduces today. `git diff --stat` for this audit: `docs/audits/` only.

---

## PHASE 1 — conformance matrix

Disposition per row. Eight read-only lenses produced **53 rows — 43
IMPLEMENTED, 7 PARTIAL, 3 DIVERGED, 0 MISSING** — and every citation was
re-opened by the auditor. Where a lens and an author-run probe disagreed, the
probe wins and the disagreement is recorded (one lens finding was **refuted**
this way; see F-12).

### A. The eight obligations

| # | Obligation (BACKLOG §3) | Disposition | Code | Observing test |
|---|---|---|---|---|
| 1 | iterate `names()`, never cherry-pick via `get()` | **IMPLEMENTED** | `runner.py:290` (`assert_complete` first), `:349` (`for name, estimator in self._registry.items()`); zero `registry.get` in `engine/` | `test_cv_runner.py:234` (a registry whose `get()` raises — the run completes); behavioural probe K2 below |
| 2 | refit per fold; no stale state | **IMPLEMENTED** (mechanism mis-described — F-2) | `runner.py:404–435`; fresh registry per run at `run_cv.py:95` | `test_cv_runner.py:260` (spy: 5 fits / 4 predicts); K3 below instruments the shared instance |
| 3 | sd=0 policy decided at design time | **IMPLEMENTED** | `metrics.py:231–273` (the only sd division, guarded at the site), `ZERO_TOL_REL` `:121`, policy string `:110`; runner passes sd through unaltered | `test_cv_metrics.py:78` (the named observer), `:92` (both live ~1e-15 sources); `test_cv_runner.py:573` (counts on the flat table) |
| 4 | kriging's reportable state reaches run provenance | **IMPLEMENTED** | `kriging.py:310–341` (`provenance()` = `asdict(report())` + semantics + verdict) | `test_cv_runner.py:353` (21 named fields, excluded bins with reasons, the alternative with its bins) |
| 5 | `range_at_candidate_ceiling` NEXT TO `range_km`, + floor twin + `residual_dof` | **DIVERGED, disclosed** | adjacency holds in the in-memory record (`kriging.py:102–111` field order); the emitted file is `sort_keys=True` (which orders them ceiling, floor-twin, `range_km`), so the file-reader mechanism is `range_km_reported` (`kriging.py:326–335`) | `test_cv_runner.py:390` (in-memory adjacency), `:761` (the one-sentence form); the divergence is disclosed at E2.4.md §2/§3 |
| 6 | RF's allow-list only; never reach around to `_forest` | **IMPLEMENTED** | `random_forest.py:316–323` (`provenance()` returns exactly `validation_facing_fields()`); `runner.py:433` is the sole estimator-state access in the module | `test_cv_runner.py:371` (exact key set; no "oob" in the whole manifest with the diagnostic computed) |
| 7 | the uncertainty-semantics **column** in every table printing an sd-shaped number | **PARTIAL — F-6** | manifest side complete (`runner.py:399, 161, 468, 514, 301, 572`); no report renderer exists in `engine/`, so the tables are hand-written markdown | `test_cv_runner.py:418`, `test_cv_run_artifact.py:118` (manifest side). **No test observes the walkthrough tables** |
| 8 | the two-fold geometry theorem frames the report | **IMPLEMENTED** | `splitter.py` design `purpose` strings carried into the manifest; §3's framing | `test_cv_run_artifact.py:88` (the theorem visible in the committed file); rows I/J below |

### B. The original four

| Requirement | Disposition | Evidence |
|---|---|---|
| baseline in every fold, uplift **per fold** | **IMPLEMENTED** | probe over the committed artifact: 47 folds across 4 designs, `folds missing baseline: []`, every baseline row scored; 92 per-fold `rmse_uplift` values present. `test_cv_runner.py:194`, `:301` |
| per-fold results, not just means | **IMPLEMENTED** | `cross_validation.designs[].results[]` carries one record per (design, fold, estimator) with metrics, predictions and provenance |
| fold assignment deterministic **and recorded** | **IMPLEMENTED** | full frozen-dataclass equality across repeat splits and across the stability plateau (`test_cv_splitter.py:243`); the record carries labels, rule, parameters, stability interval, per-fold indices and the coords fingerprint |
| spatial-leakage assertion with a **stated** minimum per scheme | **IMPLEMENTED** | `splitter.py:580–622` re-measures per fold and refuses by fold name; buffers declared per design (100 / 2 / 0 / 0 km) and recorded with the measured minima (986.036 / 4.614 / 0.054 / 0.054 km). Name-vs-body caveat: F-12 |

### C. Karl's decision 1

All eight clauses **IMPLEMENTED**: A + B both in the committed run; A labelled
sub-kilometre within-site and cited nowhere as a generalization result (§3
places it under "GATE 3 — the two designs that may never back a claim"); B is
the headline gate; the A−B RF gap sits immediately beside the known-answer
ratios in §3; per-scheme separations recorded (F-6's column caveat is about
tables, not the manifest); pure-nugget folds carry the fitter's verdict text,
not a tie; the refused fold is recorded per (estimator, fold) with baseline and
RF still scored on it; C and D are rejected on the record with reasons in §1B.

### D. Karl's decision 2

All six clauses **IMPLEMENTED** except the mechanism description (F-2).
`CrossValidator` is retired — the remaining repo references are the historical
Phase-0 sketch (dated note added) and `engine.py`'s own before/after docstring.
`Estimator.input_kind` is enforced at class-definition time with an instance
re-check in `assert_complete`; K4 confirms it fails **by name**, not as a
downstream `KeyError`. Predict-after-refusal is structurally unreachable (early
return from the handler), AST-verified and behaviourally probed (K3).

### E. §2D specifics — verified BY CONSTRUCTION

| Clause | Result |
|---|---|
| chain to the **actual** matrix artifact | `upstream_hashes["training_matrix"]` equals the matrix manifest's own `content_hash`; a manifest whose `matrix_sha256` does not describe the arrays is **refused by name**; a manifest mutated after `finalize()` is **refused by name** |
| `scores_first_visible` outside the substance hash | two emissions five years apart → **identical `content_hash`**; field absent from `substance()`; `run_id` likewise excluded (an emission's identity, not its substance) |
| the description names the commit | description contains "COMMIT", "MUTABLE METADATA", "git history is the witness"; the same sentence is emitted as a **value** (`scores_first_visible_note`), so a file reader sees it |
| the kriging post-hoc nuance recorded | present in `BACKLOG.md`, `E2.4.md` and `domain/results.py` |

### F. The consolidated test list

All eight named tests exist and assert what their names claim, with two
exceptions recorded as F-5 and F-12. The leakage-number test exists and is
pinned — its **margin** is seed-fragile (F-7).

### G. Closeout hygiene

| Item | Result |
|---|---|
| PATTERNS §4.2 points at obligation 8 | **yes** — the theorem replaces the old "That's BACKLOG §4" pointer |
| obligations entry closed without a duplicate checkbox | **yes** — one `[x]` entry, closure text on the original, no twin (the §4 defect §1 fixed was not repeated) |
| one consolidated mutation table | **DIVERGED, minor (F-10)** — three tables (§1, §2 base, §2 review), IDs continuous M1–M53, **no duplicate IDs**, no contradictions |
| suite trajectory per commit | reported and reproduces (446 passed, 2 skipped) |
| 1E(a–c), 1F not regressed | **confirmed** — CLAUDE.md's countermand gone (only the note recording its removal survives), BACKLOG §4 item closed on its original box, both instruction records still tracked, the P2.C entry present |

---

## PHASE 2 — independent re-execution

### H. Reproduction — substance and identity reported separately

Re-ran `run_cv` from the committed code **in the clone** (a different
directory — the stronger test).

| | Result |
|---|---|
| **SUBSTANCE** — `cross_validation`, `cv_scores`, `estimator_declarations`, `inputs`, `claim_eligible_designs`, `contract_versions`, `data_origin`, `derivation`, `generator`, `run_id`, `seed`, `scores_first_visible_note`, `output_hashes`, `ts6_agreement`, `economic_results` | **ALL 15 IDENTICAL** |
| **IDENTITY** — `content_hash`, `upstream_hashes` | **DIFFER** (`feature_stack` and `training_matrix`; `corpus` is stable and `matrix_sha256` over the arrays is stable) — the absolute-path defect, row M |
| `generated_at`, `scores_first_visible` | differ by design (outside the hash) |

**Every number in the §3 tables was then re-checked against the FRESHLY
REGENERATED manifest** by parsing the markdown independently: **74 numbers
matched, 0 mismatched** (per-fold Gate 1 and Gate 2 rows, all pooled rows,
Gate 3, the A−B gap, separations, and the 1.886 kg/m² cluster difference).

**No number in the §3 report fails to reproduce.**

### I. The theorem against the actual numbers

| Fold | Result |
|---|---|
| train E (21) → predict W (14) | kriging − baseline deltas: RMSE **−1.78e-15**, MAE −8.88e-16, bias −3.77e-15; `no_structure=True`, `partial_sill=0.0`, `range_km=0.0`. **kriging ≈ baseline within numerical noise** |
| train W (14) → predict E (21) | kriging **REFUSED at fit** ("only 2 bin(s) survive"); the baseline still scored the fold (RMSE 3.8594), so the fold is measured |
| the measurement the design DOES make | bias ∓1.8857 kg/m² — the same magnitude, opposite signs |

No deviation from the theorem, so the prompt's STOP-and-investigate condition
never triggered. **RF's saturation, confirmed independently:** on each LOCO
fold RF emits exactly **2 distinct values** over 14–21 predictions, and the
training cluster has exactly 2 distinct covariate rows — RF is serving the
training cluster's cell means, as the theorem's parenthesis says.

### J. §1's falsifiable prediction vs what ran — **one mismatch (F-1)**

| Held-out site | §1 predicted | Observed | |
|---|---|---|---|
| E-7 (north) | structured | structured | match |
| W-b | pure nugget | pure nugget | match |
| W-a | pure nugget | pure nugget | match |
| E-8 (middle) | refused | refused | match |
| E-6 (SW) | structured | structured | match |

**Per-fold: 5 of 5 correct** — a genuine falsifiable prediction, made before
the runner existed, and borne out fold by fold. **But the summary count is
wrong:** §1B's design-B row says "**3/5 fit**" while the code refits **4 of 5**
— and the same table cell's own enumeration (2 structured + 2 pure-nugget + 1
refused) implies 4. See F-1.

### K. The six sampled mutation re-runs

Against the clone; every restore `cmp`-verified; tree clean afterwards.
Verbatim failure messages:

**K1 — the leakage assertion weakened** (`if measured < min_separation_km:` →
`< -1.0`). Two tests fail, in two different files:

```
FAILED tests/test_cv_splitter.py::test_assert_spatially_separated_remeasures_from_coordinates_and_fails_by_fold_name
E       AssertionError: Regex pattern did not match.
E        Regex: 'near_pair.*1\\.001 km'
E        Input: "fold 'near_pair' records min_train_test_km=999.000000 but the coordinates measure 1.000756 km — the recorded separation is not a measurement"
FAILED tests/test_cv_runner.py::test_required_separation_is_recorded_per_design_and_asserted_on_every_fold
>       with pytest.raises(ValueError, match="below the stated minimum separation of 2000"):
E       Failed: DID NOT RAISE <class 'ValueError'>
```

The buffer check therefore has an observer **independent** of the
recorded-vs-measured check (the runner test), which the first failure alone
would not have established.

**K2 — cherry-picking.** A dummy fourth estimator, registered with **zero
runner change**:

```
designs ['leave_one_cluster_out', 'leave_one_site_out'] total folds 7
dummy rows in report : 7 (expected 7 )
dummy in declarations: True
dummy flat-score rows: 49 across 2 designs
dummy in every pooled: True
provenance carried   : {'planted_by': 'E2.4 audit row K2'}
VERDICT: PICKED UP with zero runner change
```

**K3 — THE LIVE STALE-REFIT CASE.** The shared kriging instance was
instrumented to log every `_predict` call together with the training state it
would use:

```
fold n_train=14 n_test=21 status=refused phase=fit refusal=only 2 bin(s) survive the exclusions
fold n_train=21 n_test=14 status=scored
predict calls on the SHARED kriging instance: [{'n_train_state': 21, 'n_targets': 14}]
VERDICT: NO prediction served from a stale system matrix
```

Exactly one predict call, on the fold whose training set it actually holds;
the refused fold carries no prediction.

**K3-A — A CORRECTION TO THIS AUDIT, made before Karl read it.** The run above
used `leave_one_cluster_out` alone, and in LOCO the **refusal comes first**
(fold 0, n_train 14) on a **never-fitted** instance. So there was no stale
state to serve from, and the probe could not distinguish "no stale prediction"
from "no stale state existed" — the same fixture-degeneracy shape this project
has found four times, here in the audit's own evidence. Re-run over all four
designs, instrumenting `fit` as well as `_predict`:

```
leave_one_cluster_out    refused(14) -> scored(21)
                         refusal preceded by a successful fit IN THIS DESIGN: False
leave_one_site_out       scored(28) -> scored(28) -> scored(28) -> refused(27) -> scored(29)
                         refusal preceded by a successful fit IN THIS DESIGN: True

DANGEROUS-ORDER CHECK — at each REFUSAL, did the instance already hold state?
  fit REFUSED  n_train=14  state before=None after=None   -> stale state present: False
  fit REFUSED  n_train=27  state before=28   after=28     -> stale state present: True

scored folds: 45   refused folds: 2   predict calls: 45
every predict used ITS OWN fold's training size: True
no prediction on any refused fold: True
```

**The property holds, and now on the order that can falsify it.** The site
design's fold 3 refuses while the instance still holds fold 2's 28-station fit
(`state before=28 after=28` — the atomic fit leaving prior state intact is
exactly the E2.2 hazard), and no predict is issued for that fold; the next
predict uses its own fold's 29. Across all 47 folds: 45 scored, 45 predict
calls, every one matching its own fold's training size, zero predictions on a
refused fold.

**F-16 (severity 2, undisclosed).** E2.4.md:271 (the §1C decision table)
describes the live case as "**LOCO** fold B kriging refuses after fold A
fitted". In the committed run LOCO refuses on fold 0 with a never-fitted
instance; the fit-then-refuse order occurs in `leave_one_site_out`. The claim
names the wrong design, and — because the LOCO order is the harmless one — it
also overstates what the LOCO case demonstrates. The runner's guarantee is
unaffected.

**K4 — routing declaration removed** (kriging's `input_kind`). Fails at class
definition, both on plain import and through the production registry:

```
TypeError: OrdinaryKrigingEstimator does not declare `input_kind` as one of
('covariates', 'coordinates') (got None) — which design matrix an estimator
consumes is a DECLARATION on the class (E2.4 §2C); the CV runner routes by it
and never matches on a name
```

Not a downstream `KeyError`.

**K5 — random k-fold for a published claim:**

```
leave_one_cluster_out    spatially_blocked=True  ACCEPTED (may back a claim)
leave_one_site_out       spatially_blocked=True  ACCEPTED (may back a claim)
leave_one_station_out    spatially_blocked=False REFUSED: design 'leave_one_station_out' is not spatially blocked …
random_k_fold            spatially_blocked=False REFUSED: design 'random_k_fold' is not spatially blocked …
```

**K6 — `scores_first_visible` hand-edited in a copy** (this SHOULD pass
silently; the audit verifies the documented limitation is real):

```
committed scores_first_visible: 2026-08-19T22:07:36.681679Z
self-hash verifies             : True
edited   scores_first_visible : 2020-01-01T00:00:00Z
self-hash STILL verifies       : True
=> nothing detects the edit    : True
the field description names the witness: True
and the artifact carries it as a VALUE too: True
```

The limitation is real, it is documented in the field's own description, and
the description travels in the emitted file as a value.

### L. Fixture-degeneracy spot-check — **F-7**

`rmse(random 8-fold) / rmse(leave-one-cluster-out)` on the planted field,
across eight seeds:

| seed | baseline | kriging | RF | gap (base−krig) | gap (base−RF) |
|---|---|---|---|---|---|
| 11 | 0.868 | 0.759 | 0.658 | **0.109** | 0.209 |
| 12 | 0.884 | 0.615 | 0.644 | 0.269 | 0.240 |
| **13** (pinned) | 0.814 | 0.568 | 0.468 | 0.247 | 0.346 |
| 14 | 0.978 | 0.731 | 0.545 | 0.247 | 0.432 |
| 15 | 0.910 | 0.666 | 0.497 | 0.244 | 0.414 |
| 16 | 0.944 | 0.770 | 0.570 | 0.174 | 0.374 |
| 17 | 0.787 | 0.642 | 0.427 | 0.144 | 0.360 |
| 18 | 0.825 | 0.586 | 0.650 | 0.239 | 0.175 |

- The **direction** survives: both models sit below the baseline floor at 8 of
  8 seeds. That is the claim the report actually rests on.
- The **magnitude pins do not**. *(Corrected 2026-08-19, after an adversarial
  pass over this audit's own doc fixes: this row first said "the test would be
  red on two of eight seeds", counting only the `>= 0.15` gap pins. Evaluating
  EVERY assertion per seed gives a worse answer.)* `ratio_baseline −
  ratio_kriging >= 0.15` fails at seeds 11 (0.109) and 17 (0.144);
  `ratio_kriging <= 0.65` fails at 11, 14, 15, 16; `ratio_RF <= 0.60` fails at
  11, 12, 18; the three ± 0.02 point pins hold only at seed 13. **The whole
  test would be RED at 7 of the 8 seeds — green only at 13** — and the ratio
  ceilings are more fragile than the gap. `ratio_baseline − ratio_RF >= 0.15`
  and the baseline floor hold 8 of 8.
- The point pins (0.814 / 0.568 / 0.468 ± 0.02) hold only at seed 13, which is
  disclosed ("Measured at this seed").
- The fixture's docstring explains the design and the baseline-floor logic but
  does **not** state which neighbouring claims it separates (CLAUDE.md rule 4,
  partial compliance).

### M. The absolute-path defect — scoped

**(a) What is machine-dependent.** In `FeatureStackManifest`'s substance the
DEM's absolute path appears **nine times** — once at `dem.path` and once inside
each of the eight `layers[i].dem.path`. Demonstrated with identical DEM
**bytes** written to two directories:

```
dem.content_hash equal : True
rasters byte-identical : True
manifest content_hash  : DIFFERENT   <-- the defect
substance fields that differ: ['layers', 'dem']
```

No hostname, username, locale or timestamp enters any substance (the only
wall-clock fields are already excluded). **Blast radius:** `FeatureStackManifest`
→ `TrainingMatrixManifest.upstream_hashes` → `RunManifest.upstream_hashes` and
`content_hash`. `CorpusManifest` is unaffected (its inputs are repo-relative);
`matrix_sha256` (over the arrays) is unaffected.

**(b) The test that claims determinism it cannot observe.**
`tests/test_covariate_stack.py::test_two_independent_builds_produce_identical_rasters_and_substance`
builds both stacks from **one** `dem_path`, varying only the OUTPUT directory:

```
same dem path, different OUTPUT dir -> content_hash equal: True
different dem path                  -> content_hash equal: False
```

The test varies the axis the manifest does **not** record and holds fixed the
axis it **does**. Its assertion is true; its docstring's claim — "two
independent builds must produce the SAME hash", the property PROVENANCE.md
states as "same hash on any machine, at any time" — is broader than the fixture
can observe. This is the coverage-that-isn't shape sitting under the
provenance layer's most-cited invariant, and it has passed since E1.4.

**(c) Is the chaining rule checkable elsewhere?** Only partly:

| upstream hash | verifiable on another machine? |
|---|---|
| `corpus` | **yes** — `data/corpus/manifest.json` is committed and its hash matches |
| `feature_stack` | **no** — recomputing gives a different hash, and no stack artifact is committed to compare against |
| `training_matrix` | **no** — its substance embeds the stack hash (though `matrix_sha256` over the arrays does reproduce) |

So the chain is currently **locally checkable, not portably checkable**.

### N. The mutation-harness rule — scope gap

BACKLOG §3's entry is titled "**Review workflows** must run against a COMMITTED
state or a copy", and its layer (b) reads "a **reviewer** that probes by
MUTATION takes a `cp` copy before its first write and restores by `cp`,
verifying with `cmp`". Two gaps:

1. **Scope.** It binds *reviewers*. The E2.4 §2 incident was the session's own
   harness — a `for f in $FILES` loop that (zsh not word-splitting an unquoted
   scalar) made **no backups**, so the restore was a silent no-op and 16
   mutations accumulated. Nothing in the entry addresses a mutating process
   that is not a reviewer.
2. **Content.** It prescribes restore-by-`cp` with `cmp` verification, but not
   the step whose absence caused the incident: **verify the copy exists and
   matches BEFORE the first mutation.** (A second missing step, learned here:
   verify the mutation actually changed the file — a no-op edit makes the probe
   pass vacuously.)

Both prior instances (E2.2, E2.4 §2) were recovered by **reconstruction**, not
from a verified backup. The entry is still open with trigger "before the next
adversarial review runs"; two have run since, both of which satisfied layer (a)
by committing first.

### O. M42's inertness — independently checked, **confirmed inert**

M42 makes `DesignResult.to_record()` record the splitter's claim instead of the
assertion's measurement. Decisive test — emit the manifest with and without the
mutation:

```
content_hash unmutated : sha256:906579734383adaa2cc90d4
content_hash mutated   : sha256:906579734383adaa2cc90d4
IDENTICAL EMITTED BYTES: True
separation values that differ: 0 of 47
```

Case **(i)**: the mutation genuinely cannot change behaviour. Both values come
from the same pure function on the same coordinates, so they are bit-identical
for any assignment a shipped splitter produces. **Not** a missing test.

One refinement the walkthrough's wording overstates: it says the two are
"provably EQUAL", but the guard admits `np.isclose(rtol=1e-9, atol=1e-9)`. A
**hand-built** `FoldAssignment` recording a value drifted inside that tolerance
is accepted (demonstrated: a 5.0e-10 km drift passes) and would change the
emitted bytes with no observer. No shipped splitter can produce one, so this is
a wording refinement, not a defect.

---

## FINDINGS, severity-ranked

Nothing below was fixed. Anything touching `engine/` or `tests/` is listed for
a separate fix task.

### Severity 1 — a number the code does not reproduce

**F-1. §1B's design-B row says kriging fits "3/5"; the code fits 4 of 5.**
`docs/walkthroughs/E2.4.md` §1B, option-B row. The same cell's own enumeration
(2 structured + 2 pure-nugget + 1 refused) implies 4, and the run confirms 4.
Per-fold predictions were 5/5 correct, so the underlying measurement is sound
and only the summary count is wrong. Blast radius: this one cell, plus anything
that quoted it (the audit prompt itself inherited "3/5"). Docs-only fix.

### Severity 2 — undisclosed divergences

**F-2. The runner's own docstring and the §2 obligations table describe a
`try/except/else` that does not exist.** `runner.py:26–28` says predict is
"structurally unreachable after it (the `else:` of the try)"; E2.4.md:568 says
"`predict` sits in the `else:`". `_fit_score` has **two separate `try` blocks
with early returns from their handlers** and no `else:` clause (AST-verified:
`orelse=0` on both). The **guarantee holds** (K3 confirms behaviourally); the
described mechanism does not exist. Introduced when the §2 review's
predict-time-refusal fix restructured the code and the prose was not updated.
Touches a live code comment → fix commit, not docs-only.

**F-3. The 2B stale-reference sweep is incomplete, and the incompleteness is
undisclosed.** Two Phase-0 staleness references survive in live code and appear
in no sweep list: `engine/prospectivity/domain/results.py:1–2` ("Phase 2-4 fill
these in; Phase 0 only needs the shapes to exist") — contradicted by the same
file now carrying the emitted, tested `CVScore`/`RunManifest`; and
`engine/prospectivity/provenance/__init__.py:10`, which still lists
`RunManifest` as "(domain/results.py; Phase 2-4)" although the emitter exists
and its artifact is committed.

**F-4. The §2 test inventory names a test that does not exist.** E2.4.md:683
lists `test_two_runs_with_the_same_inputs_and_seed_have_the_same_content_hash`;
the test was renamed in §3 (to `…_two_runs_in_one_process_on_one_matrix_…`,
precisely to narrow its claim) and the §2 inventory was not updated. A reader
looking for that pin finds nothing.

**F-5. A biconditional the fixture only half-exercises.** E2.4.md:702 says the
range-caveat test asserts the "ceiling / floor / dof caveats **iff** the flags
are set". `range_below_first_supported_lag` is `False` on **every** scored
kriging fold in the committed run, so the floor half of the biconditional is
never exercised in the True direction. The ceiling half is exercised both ways.

### Severity 3 — obligations partial

**F-6. Obligation 7's "column" is met in one table and by adjacent prose in the
other.** There is **no report renderer in `engine/`** — the tables are
hand-written markdown. The manifest side is complete and tested. In §3, the
GATE 1 per-fold table carries an explicit "uncertainty semantics" column; the
**GATE 2 pooled table** — the one place three different KINDS of sd-derived
number appear side by side (`cov ±1σ`, `z-RMS`) — has no column, relying on a
prose paragraph immediately above it. Every other §3 table prints only
error-derived numbers. **No test observes the walkthrough tables at all**, so
deleting the column from either fails nothing.

**F-7. The known-answer leakage MAGNITUDES are not seed-robust — the test is
green at one of eight sampled seeds.** *(Sharpened 2026-08-19; first stated as
"red on 2 of 8", which counted only the gap pins.)* Sweeping the field seed
over 11–18: the gap pin fails at 11 and 17, `ratio_kriging ≤ 0.65` at 11/14/15/16,
`ratio_RF ≤ 0.60` at 11/12/18, and the ± 0.02 point pins everywhere but 13 — so
the test would be RED at 7 of 8. The direction (both models below the baseline
floor) and RF's gap hold 8/8, and the direction is what the report's conclusion
rests on. The fixture docstring does not state which neighbouring claims it separates
(CLAUDE.md rule 4, partial).

### Severity 4 — disclosed divergences and hygiene

**F-8. The absolute-path defect (row M) — and the BACKLOG entry describes it
too narrowly.** Already disclosed by the session and already carrying a
BACKLOG §3 entry; the audit adds the scope above (nine occurrences in one
manifest, a determinism test that cannot observe the property it names, a
chain that is locally but not portably checkable) **and one correction**: the
manifest hashes the *caller-supplied path string*, not "the absolute path", so
identity breaks even for two invocations **in the same directory on the same
machine** when the same file is passed relatively vs absolutely —
demonstrated: `dem.path` `d.tif` vs `/private/var/.../d.tif`, identical
`dem.content_hash`, different manifest `content_hash`. The BACKLOG entry
(written by the session) says "hashes the DEM's ABSOLUTE PATH", which
understates the trigger.

**F-13. The §2 mutation-table heading contradicts its own sub-table about the
restore mechanism — and the subject is the incident itself.** E2.4.md's §2
heading reads "Every mutation, one table (§2; **all restored by `cp`**,
byte-identity confirmed, suite green after)", while the second table beneath
that same heading introduces M38–M53 as "restored by **`git checkout`** against
the committed WIP". Both statements are individually true of their own
mutation set (M24–M37 by `cp`; M38–M53 by `git checkout` after the harness
incident), but the heading generalizes over both. A reader auditing the
restore discipline — exactly what row N is about — gets two different answers
from one section.

**F-14. `matrix_sha256` and `coords_fingerprint` claim more determinism than
`tobytes()` provides (latent).** `training_matrix.py:200` states "Deterministic
across machines" without qualification; `ndarray.tobytes()` serializes in the
array's **native byte order**, so the property holds only across
same-endianness hosts (verified: the same values as big-endian hash
differently). Every mainstream target is little-endian, so this is latent, not
active — but it is an unqualified claim in a docstring about a hash the
provenance chain depends on. The same applies to
`splitter.py: coords_fingerprint`.

**F-15. The §3 inventory says "+8" and lists seven.** `tests/test_cv_run_artifact.py`
contains eight tests; the §3 table enumerates seven — `test_the_run_entry_point_composes_the_four_designs_the_report_reads`
is missing from it. The count is right; the enumeration is short by one.

**F-9. The mutation-harness rule binds reviewers only (row N).** The rule that
would have prevented the §2 incident exists but is scoped to reviewers and
omits the verify-the-backup-before-writing step. Entry text is a fix, not an
audit action.

**F-10. Three mutation tables, not one.** §1's, §2's base, and §2's
review-driven table. IDs are continuous M1–M53 with **no duplicates and no
contradictions**; the prompt asked for "one table in the walkthrough".

**F-11. Two inventory rows understate their tests.** E2.4.md:669 says the
fourth-dummy test covers "both LOCO folds"; it now runs three designs and 12
folds. (An understatement, the harmless direction — but the same drift class as
F-4.)

**F-12 — RAISED BY A LENS, REFUTED BY PROBE, recorded so it is not
re-raised.** A conformance lens reported that
`test_required_separation_is_recorded_per_design_and_asserted_on_every_fold`
overclaims per-fold coverage, and that a first-fold-only assertion would
survive the suite. **It does not.** Mutating the loop to
`for fold in assignment.folds[:1]` fails **three** tests, including the accused
one:

```
FAILED tests/test_cv_runner.py::test_required_separation_is_recorded_per_design_and_asserted_on_every_fold
FAILED tests/test_cv_runner.py::test_the_manifest_records_measured_separations_not_the_splitters_claims
FAILED tests/test_cv_splitter.py::test_assert_spatially_separated_remeasures_from_coordinates_and_fails_by_fold_name
3 failed, 443 passed, 2 skipped
```

The per-fold coverage is observed — the assertion returns its measurements and
the manifest test compares them fold by fold, so a truncated loop cannot
produce a complete record. The lens's claim was plausible from reading alone;
the probe settles it.

---

## What the audit CONFIRMED

Stated because a findings list read alone misrepresents the work:

- **Every number in §3 reproduces** — 15/15 substance fields byte-identical
  from a different directory, and 74/74 table numbers re-derived against the
  freshly generated manifest.
- **The theorem holds to 1e-15**, the refusal is recorded, and RF's saturation
  is visible as exactly 2 distinct predictions per fold over 2 training cells.
- **§1's prediction was falsifiable and correct fold-by-fold** (5/5).
- **All six sampled mutations behaved as documented**, including the one that
  should pass silently (K6). The LIVE stale-refit case is the strongest single
  result here — but only after K3-A re-ran it on the fold order that can
  falsify it; as first published its evidence was weaker than its claim, and
  that correction is recorded above rather than quietly amended.
- **Obligations 1–6 and 8, both of Karl's decisions, the original four, and
  every §2D specific are implemented**, with the divergences listed above.
- **M42 is genuinely inert** — byte-identical emitted manifest.
- All eight conformance lenses returned (53 rows: 43 IMPLEMENTED, 7 PARTIAL,
  3 DIVERGED, **0 MISSING**). Rows E, F, G, M and N were ALSO derived by the
  auditor independently before the last lenses landed; where the two agreed the
  citation is the auditor's, and where they disagreed (F-12) the probe won.

---

## LEDGER — appended at review time

*Started at the E2.4 audit; it now carries Phase-2 Track E's verdicts through
E2.5, because a second file would split one approvals chain in two.*

| Date | Event | Disposition |
|---|---|---|
| 2026-08-19 | **E2.4 implementation audit** performed against `ff5f2a7`; 16 findings, severity-ranked; K3-A self-correction appended before Karl read it | reported |
| 2026-08-19 | **Audit APPROVED by Karl.** E2.4's implementation approved: all findings documentation-level, zero severity-1 defects in code | approved |
| 2026-08-19 | **Doc fixes APPROVED** — and the fixes themselves were verified before commit. A verification pass over the CORRECTIONS (not over the findings) caught **four self-introduced false claims**: the "red on two of eight seeds" inference (the test is red at 7 of 8; the ratio ceilings are the fragile pins, and a remedy naming only the gap would have left it red at 6 of 8); a wrong mutation-restore baseline (`ff2d0c6`, which predates the guards those mutations target — the correct baseline is `64679a9`); a wrong `try`-scope in the corrected runner diagram; and a wrong "0 of 35" denominator. All four corrected pre-commit | approved, corrected |
| 2026-08-19 | **Two sibling occurrences found that this audit's own findings had missed** — §1's surviving wrong-ORDER claim that LOCO holds stale state (F-16 named only §1C), and a third live `try/else` reference in `docs/PATTERNS.md:150` (F-2 named only `runner.py` and E2.4.md) | fixed in the same commit |
| 2026-08-19 | **CORRECTION DRIFT added to CLAUDE.md** as testing convention 5, with the defect-class inventory (coverage-that-isn't ×4, fixture degeneracy ×5, deferral-without-a-landing-spot ×4, correction drift ×1). The inventory did not previously exist and was created here; the deferral count is ×4, not the ×3 the approving prompt proposed — the fourth is E2.4 §0 finding C, the rule's first post-adoption instance | recorded |
| 2026-08-19 | **The `engine/`-docstring judgment call RATIFIED** — documentation-only edits inside `engine/` ride doc commits when accompanied by an AST-with-docstrings-stripped proof | rule stated above |
| 2026-08-19 | **Escalations**: row M rewritten to what was measured (nine path occurrences; the caller-supplied STRING, so relative-vs-absolute breaks it in one directory; only `corpus` verifiable off-machine; PROVENANCE.md claims a property the implementation does not deliver); row N widened from reviewers to ANY mutating process, with verify-the-copy-before-writing named as the missing step | recorded |

| 2026-08-19 | **E2.5 APPROVED by Karl. Phase 2 Track E COMPLETE.** Suite 200 passed / 2 skipped at the end of Phase 1 → **460 passed / 2 skipped** now (202 → 462 collected; the approving prompt's "202 → 460" compared a collected figure to a passed one). Three facts make the guard credible: it **DISCRIMINATES** — on both claim-eligible designs preconditions 1–5 pass on real recorded evidence (4.614 km measured separation over 5 folds, the baseline scored in all 5, 97 paired predictions, 8 layers on one DEM, 3 sha256 upstream hashes) while 6 fails, and schemes A and random k-fold fail 1 by record; it **REFUSES** on today's data for three independent correct reasons with nothing softened and no override; and **two defects in E2.5's OWN tests were caught by mutation before commit** — G5 masked by a robustness fix (the two branches are now separately observed) and four mutations failing with a downstream `IndexError` because the tests indexed `failures[0]` before asserting a failure, i.e. this project's fails-BY-NAME standard violated inside the guard that enforces it | approved |
| 2026-08-19 | **The E2.5 prompt's inventory error, recorded as a CONVENTION-5 instance at a NEW SITE** (a task prompt's premises, not a correction's text). No sixth defect class was created — the existing rule covers it, and the count moves to ×2. BACKLOG §2's parallel conflation ("refuses an AUTHORED value outside its admissible set") corrected one level down | recorded |
| 2026-08-19 | **Contract decision, Karl: ADD the `acceptance_thresholds` slot** (null, no origin because no value, `[GEOLOGY — ISAAC]`, `model_config_version` 1 → 2) — the next task. The refusal is identical whether the field is absent or null, but the contract is Track G's INTERFACE, and a slot that does not exist cannot be filled by the person whose job it is to fill it | decided, scheduled |
| 2026-08-19 | **The four due triggers consolidated** into one "Phase-2 closeout batch" (seed-robustness pins, P2.C, the F-6 table-observer residue, the sole-observer hygiene pass) so they stop expiring individually | decided |
| 2026-08-19 | **Correction drift ×3, the third inside this commit.** The sentence recording instance (b) claimed P2.C carries "two `[KARL — DECIDE]` points"; the block carries ONE. Caught by grepping the block rather than re-reading the sentence — the same instrument that caught (b), turned on the session's own edit. Karl's approval specified ×2; the third is counted because CLAUDE.md's tally is derived from citable instances, and omitting the drift produced while writing the tally would be the defect the row names. | `CLAUDE.md` inventory row; `docs/BACKLOG.md` §3 batch header |
| 2026-08-20 | **C8.1 APPROVED by Karl. Contract 8 is at v2 and the slot exists.** `acceptance_thresholds` ships `value: null` with no `data_origin`, `[GEOLOGY — ISAAC]`; the loader refuses an AUTHORED gate **by DERIVED REALNESS** — a slice of `ORIGIN_ORDER_MOST_REAL_FIRST` up to LITERATURE — rather than a hand-maintained deny-list, so SYNTHETIC is refused by the same rule and mutation M2 (slice off by one) separates the two implementations. **The guard's verdict on E2.4's committed run is byte-identical before and after**: the same five preconditions passing, the same `{PRE_REGISTERED_THRESHOLD}` failure, `eligible False`, `is_scientific False`, on both claim-eligible designs — only the ROUTE moved, from ABSENT to NULL. Suite 460 → 470 passed / 2 skipped; six mutations, six caught by name. | approved; the Phase-2 closeout batch is next |
| 2026-08-20 | **THE LAUNDERING FINDING — the sharper form of the inventory error already on file.** The false premise ("Contract 8's loader refuses AUTHORED thresholds") was not misremembered when the E2.5 prompt was written: it was WRITTEN INTO THE REPO by the E2.X approval (`009835e`, committed 2026-08-18) and then read back as corroboration. **All three provenance channels this repo built are repo-resident — VALUES, INSTRUCTIONS, VERDICTS (see "What this document is", above) — and NONE of them distinguishes a claim that was VERIFIED from one that was merely RECORDED.** Being in the repo is not evidence; it is only persistence. Two refinements the primary sources add: (i) the carrier here was `BACKLOG.md`, a register the three-channel set does not name, which the claim entered by an APPROVAL — a verdict act writing into the open-items register; and (ii) the TRACKED spec (`docs/prompts/phase2_prompts.md` §E2.5) never carried the claim — its AUTHORED mentions are all `target_definition` — so the INSTRUCTIONS channel was clean and the laundering ran approval → BACKLOG → pasted prompt. | recorded; convention 5 gains the corollary |
| 2026-08-20 | **CREDIT WHERE IT IS DUE: the ATTRIBUTION convention did the work nobody assigned it.** The bullet self-attributed "(Karl, E2.X approval, 2026-08-14)", so the claim could be traced to a PERSON and a DATE instead of read as ambient repo fact — which is what made it refutable as a claim rather than merely wrong as a line. Stated precisely, because the two halves have different causes: what exposed the FALSITY was reading the loader at C8.1 (no such refusal existed); what exposed the PROVENANCE — and made "laundering" the right word — was the attribution plus `git log -S`. **And its known failure mode, measured here:** the attributed date is the day the claim was STATED (2026-08-14), not the day it entered the repo (2026-08-18) — a four-day gap that produced this commit's own drift instance. Attribution buys traceability, not chronology. | credited; the gap recorded, not fixed |
| 2026-08-20 | **Correction drift ×4** — the fourth is C8.1's walkthrough claiming the false premise "was written into the BACKLOG five days earlier", conflating the day Karl STATED the tripwire with the day it was COMMITTED. Caught pre-commit by `git log -S`, corrected in place inside the table that is itself about mis-dated premises. **The pre-commit ratio, corrected: 3 of 4, not 4 of 4.** Karl's approval said 4 of 4, counting the two exemplars INSIDE founding instance (a) as separate instances and omitting (b); under the tally's own membership — (a) E2.4 doc fixes, (b) the E2.5 prompt's premises, (c) the `[KARL — DECIDE]` miscount, (d) this dating — instance **(b) is precisely the one that was NOT caught pre-commit**, which is why there is a laundering finding at all. **The implication survives and sharpens:** an escapes-only tally would read ONE, not zero — and that one escape is the instance that reached a task prompt and cost two tasks to unwind. The check is what keeps the other three off the record. | ×4 recorded with the corrected ratio |
| 2026-08-20 | **P2.CLOSE APPROVED by Karl. PHASE 2, TRACK E IS COMPLETE.** The closeout batch and all four sub-items closed in five commits (`ef0db97`, `c1a7e1a`, `2b477de`, `93855c0`, `4516304`); suite **470 → 471 passed / 2 skipped**. Karl's approval dated this 2026-08-19; the commits are **2026-08-20** — corrected here, and the same one-day stated-vs-committed gap the C8.1 rows above carried. | approved; Phase 3 planning is next, opening with the AOI decision |
| 2026-08-20 | **THREE OF THE TASK PROMPT'S PREMISES FAILED VERIFICATION, and one would have SHIPPED RED.** (i) The remedy the E2.4 audit proposed — assert `base − RF >= 0.15`, "8 of 8" on seeds 11–18 — fails at **seed 4** on a 40-seed sweep, and seed 4 is in the test's own seed list, so adopting it as written would have left the suite red; the baseline floor, which the finding never flagged as fragile, holds at only **29/40**. (ii) The root README read "Phase 1, Track E complete through E1.4", not "Phase 0 (scaffold)" — the BACKLOG's own sub-item was right and the prompt's paraphrase had drifted. (iii) `SESSION_STATE.md` is at `docs/`, and two of its inbound references are markdown LINKS in the origin audit, not mentions — deleting without repairing them would have traded a stale file for two broken links. | all three corrected before acting; (i) became a new convention |
| 2026-08-20 | **Two new conventions, from two different failure modes in one batch.** MEASUREMENT RESOLUTION VS CLAIM LOAD (adjacent to convention 5, not a special case: 5 asks whether a claim is TRUE against its source, this asks whether the evidence is SUFFICIENT for the weight about to be placed on it). And THE VACUOUS-COLLECTION IDIOM, its own defect class at **×3** — two empty-collection instances (commits 1 and 2, both mutation M3) and one silent-shrinkage instance (commit 1, M4). Both drafts were mine, both caught by mutation, none escaped. | conventions added; the idiom joins the inventory |
| 2026-08-20 | **Three findings from the premise checks, none fixed inside the closeout.** The theorem test's tolerances are seed-calibrated (fully green at 2 of 8 seeds; its structural assertion 8/8) — the same defect as commit 1's, in the test beside it. **LITERATURE's evidence requirement has ZERO observers** (deleting it fails 0 of 471, against 1 each for SYNTHETIC-generator, SYNTHETIC-seed and DERIVED) — and LITERATURE is the class Track G's contributions arrive as, so Isaac's first cited value lands against an unexercised check. Cleared, not a defect: kriging's recurring 22.07 km range is the DECLARED `range_at_candidate_ceiling`. | two BACKLOGged with live triggers; the clearing recorded so it is not re-investigated |
| 2026-08-21 | **E3.1+2 APPROVED by Karl.** Four commits (`abd7516`, `e814b43`, `04de239`, `8d81986`); suite **471 → 502 passed / 2 skipped**; mutations **6/6, 7/7, 7/7**, each caught BY NAME. Karl's approval wrote "2026-08-2X — take the date from git log": the commits are **2026-08-21**, and every E3.0-approval and E3.1+2 label in the repo said 2026-08-20 (5 in the Phase-3 spec, 8 in the BACKLOG). Corrected here. **THE THIRD OCCURRENCE of the stated-vs-landed gap, and the second of mine** — C8.1's labels were the first, corrected at the P2.CLOSE approval. The cause is mechanical, not careless: the date is written while authoring and the commit lands after the session's date advances. | approved; dates corrected |
| 2026-08-21 | **What makes the result trustworthy rather than merely green — three facts.** (i) **PREDICTION (b) CONFIRMED PRECISELY**: 99.62% of predictable cells within 0.5 kg/m² of the training mean, 97.57% at the far-field variance ceiling, and **exactly 34** cells within one fitted range — the same 34 E3.0 derived from geometry BEFORE any surface existed. With the refinement that makes it honest: **0 cells within 0.001 of the mean**, so the surface is WITHIN HALF A KG/M² of the mean, never EQUAL to it; the looser phrasing is corrected at its one site in the Phase-3 spec. (ii) **PREDICTION (a) REFUTED** — the required STOP fired, the session investigated, and it was NOT a builder defect (next row). (iii) **THE UNIFORM-SAMPLE CAUTION WAS HALF WRONG AND WAS MEASURED RATHER THAN ACCEPTED**: a mean-everywhere mutation IS caught by a uniform check (0.3183 real vs 0.0 mutated); the blindness is real ONLY on the sd half, where clamping sd to its ceiling is invisible because 97.57% of cells already sit there and a 200-cell uniform sample holds ONE of the 34 informative cells. The false half came from Karl's prompt and is recorded as such. | recorded |
| 2026-08-21 | **THE REFUTED PREDICTION, with its mechanism — E3.0 §4(a).** Predicted: "order 10⁰–10¹" distinct values in [15.143, 21.657]. Measured: **~1,842 distinct values in [15.091, 21.681]**, outside the bound at BOTH ends. Two reasoning errors, both Track E's, both verified against the code before being written down: (1) the leaves were assumed to hold CELL MEANS — they hold quantile-forest's **weighted mean of the POOLED LEAF SAMPLES**, individual training y values (`quantiles="mean", aggregate_leaves_first=True, weighted_leaves=True`); (2) **bootstrap resampling was ignored** — confirmed empirically, `forest.bootstrap is True` (sklearn's default, not overridden), so a leaf holds a RESAMPLED SUBSET of a cell's stations whose mean is not that cell's mean. **What E3.0 got RIGHT and is worth separating: the tree STRUCTURE.** ≤ 4 leaves per tree on 4 distinct X — measured, 4. The error was leaf CONTENT, not leaf count. The entailed bound is [min(y), max(y)] = **[11.6, 26.8]**, which the surface respects; the tighter bound was presented as entailment and was not. | refuted; both bounds pinned as tests |
| 2026-08-21 | **WHAT SURVIVES THE REFUTATION — the saturation finding is not discarded with the wrong prediction.** The claim was always about INFORMATION CONTENT, not value count: 1,842 distinct values over a ~6.6 kg/m² spread, every one a convex combination of 35 y-values selected by **FOUR distinct X rows**, is still four effective examples wearing a smoother coat. **The blockiness prediction was wrong; the 0.348 ceiling it was meant to illustrate is untouched** — that ceiling is computed from within-cell variance (333.7/512.2) and does not depend on how many distinct values a surface happens to take. `E2.3.md`'s saturation table is a frozen record and keeps its three channels; a dated forward-pointer there sends a reader to the surface's actual behaviour. | framing corrected, ceiling intact |
| 2026-08-21 | **KARL'S DECISION: WIDEN SYNTHETIC'S EVIDENCE RULE** to "a generator import path, AND a seed OR a recorded determinism basis". **This looks like a loosening and is not**: it replaces an UNSATISFIABLE requirement with a satisfiable one carrying the same information — what produced this artifact, and what makes it reproducible. The current state is the proof: a SYNTHETIC-by-inheritance surface has no seed when its estimator has none (ordinary kriging is deterministic), the artifact exists, and it does not satisfy the rule. **A rule that cannot be honestly satisfied is not strict, it is unenforceable.** Returning None rather than fabricating a 0 was correct — a fabricated seed satisfies the check by lying to it, the exact failure the taxonomy exists to prevent. **ITS OWN COMMIT, not the approval's** (BACKLOG §3), and the loophole to close there is a bare `"deterministic"` string: the basis must name WHAT makes it deterministic. | decided; scheduled before E3.3 |
| 2026-08-21 | **Coverage-that-isn't ×4 → ×6, and the two additions share ONE cause.** The UNREACHABLE CHECK BELOW A STRUCTURAL GUARANTEE: C8.1's AUTHORED branch in the claim guard (dead once the loader refused AUTHORED outright) and E3.1+2's paired-uncertainty re-checks in the surface builder (dead because `Estimator.predict()` is a FINAL Template Method that already refuses them). **Defense-in-depth added BELOW a structural guarantee is not redundancy — it is unreachable code that LOOKS like a guard**, and a reader counts it as protection that is not there. Both were caught by trying to TEST the branch and finding the guarantee fired first. Count DERIVED from citable instances, not taken from the prompt: C8.1's branch was not among the four already listed, so both are new. | ×6 recorded |
| 2026-08-21 | **TAX.1 — SYNTHETIC's evidence rule WIDENED, and the written rasters CLASSIFIED.** Two commits (`5d97735` the rule, `c6938a0` the rasters), split because a taxonomy rule reviewed alongside its first consumer is not reviewable — and commit 1 left the suite at **502 → 502**, which is the evidence that no existing declaration changed status. New rule: *a generator import path AND (a seed OR a determinism basis)*. **Not a loosening**: the old rule was UNSATISFIABLE for a synthetic-by-inheritance artifact whose generator is seedless — all three premises verified before editing (the sidecar's state, kriging's absent seed, the audit's refusal by name) — so only a FABRICATED seed could pass it. The loophole is closed at the point the rule is written, `MIN_DETERMINISM_BASIS_CHARS = 40`, with the trade-off AND the blind spot stated at the constant: it verifies EFFORT, not MEANING. **Probed end to end both directions** against a staged output directory: zero unclassified / zero invalid with declarations, both rasters refused BY NAME with a bare basis. Mutations 4/4 (each by exactly ONE test, re-confirming the sole observer) and 5/5. Two further gaps the probe found and fixed: the JSON sidecar carried no evidence for its own SYNTHETIC claim, and sidecar exclusion was a per-path list that every new output directory would have had to extend — made generic. LITERATURE deliberately untouched. Suite 502 → **505**. | approved-pending-review; E3.3 next, after TS-6's origin class |
| 2026-08-22 | **TAX.1 APPROVED by Karl.** Two commits plus closeout (`5d97735`, `c6938a0`, `3cd2b32`); suite **502 → 505**; mutations **4/4** and **5/5**. Three facts make it more than a rule change. **(i) THE SPLIT PRODUCED EVIDENCE.** Commit 1 left the suite at exactly 502 — proof that every existing SYNTHETIC declaration still passes and none that previously failed now passes, i.e. the widening loosened nothing in practice. **That fact is INVISIBLE in a combined diff**, because commit 2's +3 would have absorbed it. It is the strongest argument yet for the reviewability fence, and it came out of FOLLOWING the rule rather than from the reasoning that motivated it. **(ii) THE UNREACHABLE-BRANCH DECLINE.** A bare-token reject list beside a 40-character threshold would have been unreachable — coverage-that-isn't applied to a NEW decision within one commit of that class being recorded at ×6. The fastest convention transfer in the project's record. **(iii) THE END-TO-END PROBE FOUND WHAT UNIT TESTS COULD NOT**: a JSON sidecar declaring itself SYNTHETIC without evidence, and `data_origin.yaml` returning unclassified because sidecar exclusion was a PER-PATH list needing a hand-added entry per output directory. Making it generic was a scope judgement beyond the literal ask, disclosed in the commit, the walkthrough and the report rather than made quietly. | approved |
| 2026-08-22 | **The loophole's honest limit, recorded as stated.** `MIN_DETERMINISM_BASIS_CHARS = 40` verifies **EFFORT, NOT MEANING**: *"this artifact is deterministic and always reproducible"* is 54 characters and says nothing. A length check is mechanically observable; a semantic one is not. **The trade was made knowingly and written at the constant**, not discovered later — and review, not the resolver, is the observer for vacuity. The basis at its first real use is 302 characters naming the solve, the fitting method and the specific absences, each falsifiable against `kriging.py`. | recorded |
| 2026-08-22 | **Karl's TAX.1 §2 paraphrase was WRONG, and it is a convention-5 instance at the LAUNDERING SHAPE, one scale down.** The prompt said the outputs directory "may sit outside" the audit's walk. It does not — the walk is `git ls-files -- data tests/fixtures`, so outputs under `data/` are INSIDE it, **and E3.1+2 had already MEASURED that**. The cause is specific and worth more than the instance: **a qualifier TRUE IN ONE CONTEXT (the coverage-boundary BACKLOG entry, where the boundary genuinely was uncertain) was reused in another WITHOUT RE-CHECKING**, and the measurement that refuted it already existed in the repo. Same shape as the laundering finding — a recorded claim read back as corroboration — at smaller scale, and with the same remedy: the primary source, not the entry that quotes it. It made the task look harder than it was. | ×5, count DERIVED |
| 2026-08-22 | **E3.3 APPROVED by Karl.** Three commits (`1c159f3`, `1febedc`, `cdb68b0`); suite **505 → 525 passed / 2 skipped**; mutations **4/4** and **8/8**, every refusal caught by exactly its own test. Two findings from running the REAL comparison are worth more than the task's nominal output. **(i) THE ULP-NOISE CORRELATION**: the baseline's "constant" surface has sd = 3.55e-15 — mean subtraction leaves floating-point residue — so an exact-zero degeneracy check passed it to np.corrcoef, which returned an r computed ENTIRELY from that noise and labelled it "ok": a number with no information content wearing a status flag saying it had some. Fixed by REUSING E2.4's ZERO_TOL_REL rather than inventing a second numerical-zero policy, and the degenerate-case test uses the baseline's REAL surface so the noise stays inside the test's path — an idealized constant could not have reproduced the bug. **(ii) n_eff ≈ N IS THE FORMULA BEING RIGHT, NOT BROKEN**: the synthetic TS-6 is white noise, and Clifford–Richardson honestly reports the naive df as nearly correct when one field is white; E3.0's "N_eff ≈ 2" assumed BOTH surfaces smooth — the real-TS-6 case, not today's. Recorded in the test's docstring IN ADVANCE, so neither the collapse nor its absence today can be reported as a finding later — **the geometry-theorem discipline applied to a statistic**. And the honest output survived the surprise: kriging vs fixture r = +0.026, inside the noise scale, emitted interpretation "NOT distinguishable from zero" — the machinery reached the right verdict by a different route than predicted. Divergence recorded: the paste dated the correlation decision 2026-08-20; the tracked spec says 2026-08-21 and the repo wins. | approved |
| 2026-08-22 | **THE FOURTH STATED-VS-LANDED OCCURRENCE, found by following "take the date from git log".** TAX.1 itself landed 2026-08-21 and was labelled correctly; the TAX.1 APPROVAL (`d8a3422`, `9a7ecac`) and ALL of E3.3 landed **2026-08-22** and were labelled 08-21 at ~15 sites (BACKLOG, ledger, TAX.1.md §5, Contract 6's v3 notes, contracts README). All corrected — the Contract 6 labels in the v4 commit, the rest here. Same mechanical cause as the third: the session began before midnight and committed after it, and the author carried the session-start date. | corrected across both commits |
| 2026-08-22 | **Karl's E3.3 premise failed the SAME WAY as the E2.5 one, and the recurrence now has two instances of its own: A DEFERRAL DESIGNED IN ONE TASK, REMEMBERED AS BUILT (or existing) IN A LATER ONE BY THE SAME AUTHOR.** Contract 8's `acceptance_thresholds` — designed deferred at P2.A, asserted as an existing refusal by the E2.5 prompt; Contract 6's `digitization_uncertainty` — required by the TAX.1-approval decision, asserted as "null today" by the E3.3 prompt when NO SUCH FIELD EXISTS. Absent vs null is this project's own distinction, with different remedies (a structural change is Karl's; a value is Track G's). **The remedy that keeps working: the session verified the field's state before writing against it** — which is why a THREE-STATE ACCESSOR exists instead of a null check. That accessor design is C8.1's posture reused correctly across contracts, not reinvented: absent refuses naming the STRUCTURAL gap, null refuses naming the UNFILLED value, present carries the value. | correction drift ×6, count derived |
| 2026-08-22 | **KARL'S DECISION: `RunManifest.ts6_agreement` BECOMES A MAPPING** — recorded for E3.4, not built here (a Phase-0 shape change belongs in that task, under the 2B revision protocol: before/after shapes and the stale-reference sweep, as CVScore and CrossValidator got). The reasoning, against the tempting alternative: collapsing to a single agreement would force a "which estimator IS the comparison" answer nobody has argued for, and the three genuinely differ — kriging's surface is near-constant, RF's is blocky and ceiling-bound, the baseline's is flat by construction. One number would silently privilege one of them. `compare_all_to_ts6` already returns one self-identifying agreement per estimator, so the SHAPE is the only thing missing; `reference.py`'s stub correctly names the arity as E3.4's and is left as written. | decided; E3.4 builds it |

| 2026-08-22 | **E3.4 commit 1 (`31dc10b`) — the manifest extension.** `ts6_agreement` BECOMES THE MAPPING (Karl's arity decision, built under 2B: before/after in `results.py`; `None` keeps meaning "the comparison did not run"); `provenance/emitter.py: extend_run_manifest` RECOMPUTES every chain link (corpus and stack substance, matrix arrays, benchmark bytes, every written raster with its VALUES re-read) and refuses by name when two records of one fact disagree; the limit is IN THE OUTPUT (`provenance_chain`: only the corpus verifiable off-machine) and the blast radius is measured to reach every output file; E2.5's verdict is data for EVERY design. **Two findings worth more than the assembly:** (i) THE HASH COVERS THE SHAPE — four new fields re-hashed the committed E2.4 artifact; re-stamped mechanically (four `null` lines + `content_hash`, every other byte identical), policy for Karl (BACKLOG §2); (ii) the separating-fixture rule applied to the emitter's own tests: a forged hash quoted CONSISTENTLY by every record is what distinguishes "recomputed" from "copied", and the first draft had no such fixture. Mutations 12/12. Suite 525 → 538. | landed |
| 2026-08-22 | **E3.4 commit 2 — the engine wiring (2B).** The feature seam returns a `FeatureBundle` (one stack, read twice — matrix and prediction grid — so a grid from a different stack is unrepresentable, not merely refused); `run()` emits E2.4's record BEFORE any surface, evaluates E2.5's guard for EVERY design, hands the writer the DECLARED `claim_design`'s verdict (a required argument — the engine never picks), builds and writes paired surfaces for every estimator, compares all to TS-6, extends the manifest. The Phase-0 `compare_to_ts6` stub RETIRED; the economics ABC's annotation now says what arrives. **The whole-run reproducibility test hit the path-hash limit at run scale** and was split: same tree → identical hash; different tree → the differing set is EXACTLY the stack-hash carriers (an equality, so a new field that picked up the hash widens it and fails). The 2B sweep caught a stale engine call in `test_cv_runner.py`. Mutations 8/8. Suite 538 → 550. | landed |
| 2026-08-22 | **E3.4's premise check: three contradictions, recorded where each lives.** (1) Repo-vs-repo: Contract 6's `digitization_uncertainty` slot landed in `4834e31` but its BACKLOG §2 entry was never checked off — the deferral rule's "closing = checking it off in the commit that closed it", missed by one commit; closed at E3.4. (2) The spec's closing list says "the AOI's trigger moved to Checkpoint 1" — it was ALREADY moved at the E3.0 approval (BACKLOG §1 verified); nothing to do. (3) "the LITERATURE observer gap now has two Phase-3 arrivals riding on it" is STALE since TAX.1 settled TS-6's raster as DERIVED; one Phase-3 arrival (the AOI) can still land there — the entry is re-qualified, the count corrected rather than carried. Also found while reading before wiring: `claim.py`'s runner-vs-record branch raises `NameError` (BACKLOG §3, deferred — E2.5's module); and a latent clock-in-hash once a pre-registration gate PASSES (BACKLOG §3). | recorded |

| 2026-08-22 | **E3.4 commit 3 — two BACKLOG triggers that had FIRED, found by reading §3 after commit 2 rather than before.** "Dependency versions into the provenance manifest — Trigger: Phase-3 manifest emitter" and the corpus-CSV-bytes pin ("natural fit: the Phase-3 manifest work"). Both small, both the emitter's: `provenance/environment.py` records the lockfile's sha256, the interpreter, the approved stack's INSTALLED versions and GDAL's under `inputs.environment` in every run manifest (inside the hash — they are inputs; stated consequence: CI and this laptop will not share run hashes); `provenance_chain.links.corpus.csv_sha256` pins the corpus BYTES at run level, saying it agrees with nothing upstream yet (the CorpusManifest-level pin stays the own task). The "2/2" label on the previous commit predates this discovery. Walkthrough `E3.4.md` with the Phase-3 closeout. Mutations 2/2. Suite 551 → 553. | landed; STOP for review before Checkpoint 3 |

| 2026-08-22 | **E3.4 commit 4 — the E3.4 PROMPT arrived after the work; verified section by section.** Seven divergences recorded in `E3.4.md`'s divergence report (the re-stamp reported AFTER acting, not before — reversible, six lines; the `reference.py` stub RETIRED rather than updated; the prompt's "shape fixed when the emitter is built" line in PROVENANCE.md no longer exists — a stale premise, rewritten at E2.4's own sweep; grid identity recorded once; a null `digitization_uncertainty` on the real path RAISES in E3.3's comparison, so no "refusal state" reaches a manifest). **Prompt §4's "count the limit's scope" corrected this task's own claim:** measuring the distinct hash values that move between two trees gave **11**, not the 12 that "every output file" implies — `data_origin.yaml` quotes no hash. The emitter now TESTS each file's bytes for the stack hash and records `path_dependent_hashes` (count / values / independent); two tests pin the count against the measured diff. Plus the prompt's three named tests the first pass lacked (each entry's five facts and benchmark state; `null` vs `{}` distinguishable and `{}` refused by name; keys == `registry.names()`), and §6's BOTH costs in the BACKLOG entry. Mutations 2/2. Suite 553 → 556. | landed; STOP for review |

| 2026-08-22 | **E3.4 APPROVED by Karl.** Four commits (`31dc10b`, `34f94de`, `4a214e1`, `e257542`); suite **525 → 556 passed, 2 skipped**; mutations **12/12, 8/8, 2/2, 2/2**. **Phase 3 Track E complete.** Three things the task produced beyond its deliverable: **(i) THE PROMPT ARRIVED AFTER THE WORK, and that made it a stronger test than a specification** — the implementation had to satisfy §0–§7 of a spec it never saw, and the conformance outcome is recorded as an AUDIT RESULT in `E3.4.md` ("§A — the post-hoc conformance audit"): 7 divergences, 3 of them stale premises in the prompt, 1 a sequencing miss, 1 a deliberate retirement, 2 benign; 3 named tests missing and added; 1 claim of the task's own found wrong. **The pattern is now ×2:** E2.4's audit and this pass both found more than the forward build did, which is an argument for making the post-hoc conformance check a standing step rather than an accident of timing. **(ii) §4's COUNT CORRECTED THE TASK'S OWN CLAIM** — 11 path-dependent hash values, not the 12 "every output file" implied; `provenance_chain.path_dependent_hashes` now carries count + values + JSON paths, and two tests go RED when BACKLOG §3's path-hash fix lands: **the defect carries its own measurement, so the fix cannot pass silently, and a number ages better than a sentence.** **(iii) TWO MORE STALE PREMISES, recorded under convention 5 with the count DERIVED (×6 → ×11, see CLAUDE.md's row):** PROVENANCE.md's "shape fixed when the emitter is built" line — a sweep target E2.4's own sweep had already cleaned, reused verbatim from E2.4's sequencing prompt; and a "refusal state" for a null `digitization_uncertainty`, which cannot exist downstream of E3.3's refusal — a state requested that the primary source forbids. | approved |
| 2026-08-22 | **§6's SEQUENCING WAS MISSED — recorded as a miss, not absorbed.** The prompt said report BEFORE acting on the re-stamp of committed provenance; it was reported after (the prompt arrived after the work, and the handoff carried no such stop). Nothing was lost — six diff lines, reversible, both costs recorded, trigger set before the path-hash fix — and the handling afterward was correct. **Why the instruction existed, which is the point:** a re-stamp of committed provenance is easy to justify after the fact and hard to unwind, which is precisely the class of action that wants a STOP BEFORE rather than a report after. Same shape as "a correction pass is scoped to the claim, not the entry": the ORDERING is the safeguard, and performing the action correctly does not substitute for it. No new convention — convention 5's family covers the principle. | recorded |
| 2026-08-22 | **THREE DECISIONS (Karl, E3.4 approval), BACKLOGged, not built.** **(1) THE HASH SCHEME BECOMES SHAPE-TOLERANT:** hash over PRESENT fields, record the schema version alongside, leave historical manifests with their original hashes; the E3.4 re-stamp of `data/runs/e2.4/run_manifest.json` STAYS (un-stamping would be the after-the-fact edit the decision exists to stop). The bounded historical set, VERIFIED by `git ls-files`, not restated from the report: **2 committed artifacts** lack a schema version — `data/runs/e2.4/run_manifest.json` and `data/corpus/manifest.json` (the base class's `substance()` is shared by all four artifact types, so the decision reaches the corpus manifest too). Trigger: before the path-hash fix. **(2) NARROW THE WRITER'S SIGNATURE, NOT THE GUARD:** eligibility is a property of the evidence behind a RUN; none of the six preconditions differs per estimator; what differs is the WATERMARK, already per surface. The engine's required `claim_design` — the engine never picks — must survive the narrowing. **(3) THE RETIRED `compare_to_ts6` STUB STAYS RETIRED:** no implementation, no caller, a signature that cannot express the mapping; a shim would restore a seam nothing uses (PATTERNS.md §3's ceremony). Recorded in `E3.4.md`'s divergence report as a choice. | decided |
| 2026-08-22 | **WHAT TRACK E CAN STILL DO WITHOUT TRACK G — Karl's reading ("nothing substantial") VERIFIED AND FOUND WRONG, as the approval asked.** The alpha proposal's Phase 4 Track-E lane (§"Phase 4", E4.1–E4.3) is DEFINED as placeholder-based: "EconomicModel reading Contract 4; two scenarios with PLACEHOLDER values + 'illustrative only' marker", and Contract 4 already carries those placeholders (`illustrative_only: true`, AUTHORED). So a whole phase of Track-E work needs no Track-G input, watermarked ILLUSTRATIVE ONLY by design — and E5.3/E5.4 (run harness; CI end-to-end on fixtures) are G-free too. Plus six §3 engineering items owned by E alone: the path-hash fix, the shape-tolerant hash, the writer narrowing, the LITERATURE observer fixture, `claim.py`'s `NameError` branch, the corpus-bytes own task. **What genuinely needs G:** Checkpoint 3 (G3.1's raster), Checkpoint 1 (real GEBCO), the AOI (G + Karl), Contract 8's threshold. Recorded in `E3.4.md` §5 and CLAUDE.md's status line; the phase-boundary rule still governs — E4.1 is Karl's to start. | verified; changes what happens next |
| 2026-08-22 | **E3.4 APPROVED by Karl.** Four commits (`31dc10b`, `34f94de`, `4a214e1`, `e257542`); suite **525 → 556 passed, 2 skipped**; mutations **12/12, 8/8, 2/2, 2/2**. **Phase 3 Track E complete.** Three things the task produced beyond its deliverable: **(i) THE PROMPT ARRIVED AFTER THE WORK, and that made it a stronger test than a specification** — the implementation had to satisfy §0–§7 of a spec it never saw, and the conformance outcome is recorded as an AUDIT RESULT in `E3.4.md` ("§A — the post-hoc conformance audit"): 7 divergences, 3 of them stale premises in the prompt, 1 a sequencing miss, 1 a deliberate retirement, 2 benign; 3 named tests missing and added; 1 claim of the task's own found wrong. **The pattern is now ×2:** E2.4's audit and this pass both found more than the forward build did, which is an argument for making the post-hoc conformance check a standing step rather than an accident of timing. **(ii) §4's COUNT CORRECTED THE TASK'S OWN CLAIM** — 11 path-dependent hash values, not the 12 "every output file" implied; `provenance_chain.path_dependent_hashes` now carries count + values + JSON paths, and two tests go RED when BACKLOG §3's path-hash fix lands: **the defect carries its own measurement, so the fix cannot pass silently, and a number ages better than a sentence.** **(iii) TWO MORE STALE PREMISES, recorded under convention 5 with the count DERIVED (×6 → ×11, see CLAUDE.md's row):** PROVENANCE.md's "shape fixed when the emitter is built" line — a sweep target E2.4's own sweep had already cleaned, reused verbatim from E2.4's sequencing prompt; and a "refusal state" for a null `digitization_uncertainty`, which cannot exist downstream of E3.3's refusal — a state requested that the primary source forbids. | approved |
| 2026-08-22 | **§6's SEQUENCING WAS MISSED — recorded as a miss, not absorbed.** The prompt said report BEFORE acting on the re-stamp of committed provenance; it was reported after (the prompt arrived after the work, and the handoff carried no such stop). Nothing was lost — six diff lines, reversible, both costs recorded, trigger set before the path-hash fix — and the handling afterward was correct. **Why the instruction existed, which is the point:** a re-stamp of committed provenance is easy to justify after the fact and hard to unwind, which is precisely the class of action that wants a STOP BEFORE rather than a report after. Same shape as "a correction pass is scoped to the claim, not the entry": the ORDERING is the safeguard, and performing the action correctly does not substitute for it. No new convention — convention 5's family covers the principle. | recorded |
| 2026-08-22 | **THREE DECISIONS (Karl, E3.4 approval), BACKLOGged, not built.** **(1) THE HASH SCHEME BECOMES SHAPE-TOLERANT:** hash over PRESENT fields, record the schema version alongside, leave historical manifests with their original hashes; the E3.4 re-stamp of `data/runs/e2.4/run_manifest.json` STAYS (un-stamping would be the after-the-fact edit the decision exists to stop). The bounded historical set, VERIFIED by `git ls-files`, not restated from the report: **2 committed artifacts** lack a schema version — `data/runs/e2.4/run_manifest.json` and `data/corpus/manifest.json` (the base class's `substance()` is shared by all four artifact types, so the decision reaches the corpus manifest too). Trigger: before the path-hash fix. **(2) NARROW THE WRITER'S SIGNATURE, NOT THE GUARD:** eligibility is a property of the evidence behind a RUN; none of the six preconditions differs per estimator; what differs is the WATERMARK, already per surface. The engine's required `claim_design` — the engine never picks — must survive the narrowing. **(3) THE RETIRED `compare_to_ts6` STUB STAYS RETIRED:** no implementation, no caller, a signature that cannot express the mapping; a shim would restore a seam nothing uses (PATTERNS.md §3's ceremony). Recorded in `E3.4.md`'s divergence report as a choice. | decided |
| 2026-08-22 | **WHAT TRACK E CAN STILL DO WITHOUT TRACK G — Karl's reading ("nothing substantial") VERIFIED AND FOUND WRONG, as the approval asked.** The alpha proposal's Phase 4 Track-E lane (§"Phase 4", E4.1–E4.3) is DEFINED as placeholder-based: "EconomicModel reading Contract 4; two scenarios with PLACEHOLDER values + 'illustrative only' marker", and Contract 4 already carries those placeholders (`illustrative_only: true`, AUTHORED). So a whole phase of Track-E work needs no Track-G input, watermarked ILLUSTRATIVE ONLY by design — and E5.3/E5.4 (run harness; CI end-to-end on fixtures) are G-free too. Plus six §3 engineering items owned by E alone: the path-hash fix, the shape-tolerant hash, the writer narrowing, the LITERATURE observer fixture, `claim.py`'s `NameError` branch, the corpus-bytes own task. **What genuinely needs G:** Checkpoint 3 (G3.1's raster), Checkpoint 1 (real GEBCO), the AOI (G + Karl), Contract 8's threshold. Recorded in `E3.4.md` §5 and CLAUDE.md's status line; the phase-boundary rule still governs — E4.1 is Karl's to start. | verified; changes what happens next |

| 2026-08-22 | **HASH.1 APPROVED by Karl.** Two commits (`d74b18a`, `132d382`); suite **556 → 563 → 566 passed, 2 skipped**; mutations **4/4 (one re-run in realistic form) and 4/4**. **The outcome as a MEASUREMENT:** eleven directory-dependent hash values → **ZERO**, verified at whole-run scale from a second tree — same stack hash, same raster bytes, an EMPTY set of differing run-manifest fields, equal `content_hash`. **The three REMAINING limits, separated as the approval requires:** two are ACCIDENTAL — `matrix_sha256` and the coordinate fingerprints hash native byte order (same-endianness hosts only), and raster bytes across GDAL versions are unmeasured — and one is DELIBERATE — `inputs.environment` sits inside the run's `content_hash` by design (E3.4 commit 3), so two machines with different installed versions hash differently on purpose. A statement that did not separate them would not be checkable: the accidental two can be removed by a fix, the deliberate one only by a decision. | approved |
| 2026-08-22 | **THE LEGACY-MODE FINDING — the prompt's STOP condition was reachable, and it decided the design.** HASH.1 made "the committed hashes must not move" a STOP condition on the assumption that a plain present-fields rule would satisfy it. MEASURED before building: it does not, for a reason neither BACKLOG entry could have predicted — the E3.4 re-stamp hashed five nulls INTO the E2.4 manifest's substance, so `exclude_none` moves it (`e3ac1561…` → `b649fd96…`); and `exclude_defaults` moves the corpus manifest via `upstream_hashes: {}`. A naive shape-tolerant scheme fails the one property it exists to have. **THE DISCRIMINATOR, the reusable part:** only `finalize()` ever sets `content_hash`, so an artifact arriving WITH a `content_hash` and WITHOUT a `schema_version` is unambiguously reloaded history, and is hashed under the old rule over a per-class FROZEN field list. **Backward compatibility earned from a FACT ABOUT THE CODE, not a convention** — a date cutoff, a filename convention or a manual list would have been a convention wearing a mechanism's name. The rule that keeps the legacy path from widening silently: a field a subclass introduces must default to `None`, refused at class definition. Convention-5 instance (l), count derived in CLAUDE.md's row: the prompt said two premises failed inside HASH.1 — the record substantiates ONE (this); the other failed premise of this batch is in the approval itself (next row). | recorded |
| 2026-08-22 | **THE FIX REINTRODUCED ITS OWN DEFECT ONE LAYER UP.** The first draft of commit 2 echoed the stack's `dem_path` inside the RUN's chain block — inside the run substance — and the two-tree measurement showed `provenance_chain` as the single differing field. Caught by measuring, not by reading. **The generalization:** a property must be asserted END TO END, not at the site where its cause was removed; removing a cause and verifying there cannot see the same defect reintroduced downstream by the removal itself. **Its instance count, DERIVED rather than taken:** the approval named E3.1+2's mask-union check as the first instance — **that precedent does not hold**: `test_the_mask_is_exactly_the_stacks_nan_union…` recomputes the union FROM THE RASTERS ON DISK (end to end), and its note records a fixture degeneracy (the union equals BPI's set), not a cause-site assertion. The nearest prior is E3.4's "every output file" — a claim made at the emitter and refuted by the downstream count (11, not 12). So: ONE exact instance (this) and one adjacent; convention-5 instance (m) for the misnamed precedent. **Two instrument findings, and the harness is now the project's most frequent source of near-misses:** (i) mutation H-M2 "survived" as an IMPORT error the harness's line filter miscounted; re-run in realistic form and caught by name — the THIRD harness finding on record (E2.2 §2's reviewer restore; E2.4 §2's session harness with no backups; this), and every one has been the harness rather than the code (the wider "instrument" class — the K3-A probe, the E1.4 determinism test — is larger, and is not the harness); (ii) the emitter's path-free assertion was first placed AFTER the stack-hash recomputation, so a planted path tripped the hash check first and the refusal was unobservable — E3.4's missing-CSV lesson recurring: an assertion downstream of a stricter check can never observe its own condition. | recorded |
| 2026-08-22 | **HASH.1 closing checks, verified against the files rather than the report.** Both BACKLOG entries closed (`[x]` at the shape-tolerant and path-hash entries). The three OTHER "before Checkpoint 1" triggers are reported in one §3 entry and each is still live in its own: the bathymetry `data_origin: null` (Karl + G), the GEBCO TID classification (Karl + G), `DemGrid.load`'s south-up assertion (E-only; its trigger reads "before Checkpoint 1 (the next new DEM entering the system)" — survived). PROVENANCE.md's content-hash section states directory-independence in full and names the three remaining limits (lines 183–188), so it does not overclaim. CLAUDE.md's status line carries the qualifier **"on Track E's side"** — the GEBCO download is an action nobody has taken, and without the qualifier the line would read as "done". Suite unchanged at **566 passed, 2 skipped**; this commit touches nothing under `engine/` or `tests/`. | verified |

| 2026-08-22 | **E4.1 landed — two commits (`cd0c032`, commit 2 this row ships in).** Karl's two decisions built as recommended: the per-reason WATERMARK VERDICT (terrain ↔ Checkpoint 1, economic parameters ↔ Checkpoint 4; derived from declared facts; DISCRIMINATES — tested in both directions where the lattice says AUTHORED for both) and CONFIDENCE-LEVEL FOOTPRINTS (mu − z·sd; each estimator's semantics travels). The 2B revision FIRST: `apply(inputs, scenario) -> ScenarioFootprints`, `difference` concrete on the ABC, the copied `illustrative_only` gone from the result, `economic_differences` on the manifest (schema 2, HASH.1's rule), `FeatureBundle.cell_area_m2` from DemGrid as the one deliberate seam addition. The flag resolved: the declared fact one reason derives from; the flag and the origin answer different questions and contradict in ONE direction, refused by name. **Measured, as stated in advance:** both scenarios are the WHOLE predictable domain (2,880/2,880; 347,707 km²) at z = 0 and z = 1 for all three estimators; the difference is EMPTY; the 6° slope filter removes nothing against a 0.87° maximum. `dollar_value` refuses by name (zero GRADE rows, zero prices); a non-empty exclusion set refuses by name. Four deferrals entered at the moment of deferral: the slope filter (Checkpoint 1), the WET/DRY BASIS (a Contract 1 + Contract 4 gap with a live consumer — empty on all 108 rows, "confirm wet/dry basis" at the source), the scenarios-bracket-nothing finding and the five missing slots as G4.1's sharpened ask. Tests +14 / +11; mutations 6/6 and 8/8. Suite 566 → 580 → 591. | landed; STOP for review |
| 2026-08-22 | **E4.1 landed — two commits (`cd0c032`, commit 2 this row ships in).** Karl's two decisions built as recommended: the per-reason WATERMARK VERDICT (terrain ↔ Checkpoint 1, economic parameters ↔ Checkpoint 4; derived from declared facts; DISCRIMINATES — tested in both directions where the lattice says AUTHORED for both) and CONFIDENCE-LEVEL FOOTPRINTS (mu − z·sd; each estimator's semantics travels). The 2B revision FIRST: `apply(inputs, scenario) -> ScenarioFootprints`, `difference` concrete on the ABC, the copied `illustrative_only` gone from the result, `economic_differences` on the manifest (schema 2, HASH.1's rule), `FeatureBundle.cell_area_m2` from DemGrid as the one deliberate seam addition. The flag resolved: the declared fact one reason derives from; the flag and the origin answer different questions and contradict in ONE direction, refused by name. **Measured, as stated in advance:** both scenarios are the WHOLE predictable domain (2,880/2,880; 347,707 km²) at z = 0 and z = 1 for all three estimators; the difference is EMPTY; the 6° slope filter removes nothing against a 0.87° maximum. `dollar_value` refuses by name (zero GRADE rows, zero prices); a non-empty exclusion set refuses by name. Four deferrals entered at the moment of deferral: the slope filter (Checkpoint 1), the WET/DRY BASIS (a Contract 1 + Contract 4 gap with a live consumer — empty on all 108 rows, "confirm wet/dry basis" at the source), the scenarios-bracket-nothing finding and the five missing slots as G4.1's sharpened ask. Tests +14 / +11; mutations 6/6 and 8/8. Suite 566 → 580 → 591. | landed; STOP for review |

| 2026-08-22 | **E4.1 correction, same day: the Phase-4 prompts file is UNTRACKED.** `docs/prompts/PHASE4-track-E-prompts.md` exists on disk and is absent from the repo (`git status` `??`, `git ls-files` empty) — the E3.0 situation one phase later. E4.1's walkthrough §0 and commit `cd0c032`'s message said "tracked, committed with E4.0's report" because the file's existence was checked with `ls` and its tracking was not. Corrected in the walkthrough; convention-5 instance (n), count derived ×14; the commit message stands as history. Committing the file is Karl's (the INSTRUCTIONS channel is the repo; the E3.0 approval committed its own). | corrected |

**Why this table exists here:** the ledger's stated role is that disposition
rows append AT REVIEW TIME rather than being reconstructed at a phase boundary
(CLAUDE.md workflow conventions; the E2.X audit's own framing). These rows were
written as each event closed.

---

## Suite

**446 passed, 2 skipped** — unchanged by this audit, which adds no code and no
test. `git diff --stat` for the audit commit: `docs/audits/` only.
