<!-- Tracked UNEDITED at E2.4 §1 (2026-08-18) per the P2.PRE instruction-provenance rule
     (the instruction record lives in the repo, not in a chat). This is the E2.4 prompt Karl
     pasted after the handoff (docs/handoffs/HANDOFF_claude_code_E2.4.md). Its AUTHORITY NOTE
     makes docs/prompts/phase2_prompts.md §E2.4 (revised 2026-08-14) win on divergence; the
     §1 first-actions check found the embedded obligations VERBATIM (650 words, ratio 1.0)
     and the paste ADDS sequencing (1A–1F), the Phase-0 revision protocol (2B), the routing
     declaration (2C), the chain assertion (2D), the review probes, and Section 3's framing —
     additions, not divergences. Recorded in docs/walkthroughs/E2.4.md §0. -->

E2.4 — SPATIAL CROSS-VALIDATION AND THE COMPARISON REPORT (final, self-contained: the tracked §E2.4 spec as revised 2026-08-14, merged with the sequencing prompt and the first-actions findings)
AUTHORITY NOTE: this document EMBEDS docs/prompts/phase2_prompts.md §E2.4 as revised 2026-08-14. If this paste and the tracked file diverge, THE REPO WINS — read the tracked section first and report any divergence rather than resolving it silently.
Task E2.4 only. This is the project's most defensible methodological claim — build it carefully. Three commits: Section 1 (folds, metric policy, doc fixes, two proposals — ends at ONE STOP), then the runner, then the comparison report. Adversarial review on the SECOND commit — the runner is where the eight obligations either hold or silently don't.
ALSO READ FIRST: the BACKLOG runner-obligations entry, and docs/walkthroughs/E2.2.md §2 and E2.3.md §2 for what each estimator's report() exposes.
SCOPE FENCE: no acceptance gate, no threshold logic, no refuse-to-validate — that is E2.5. This task produces measurements and their provenance, framed per the theorem. The word "passes" appears nowhere in its output.
════════════════════════════════════════════════════════════════════ THE DESIGN — three CV schemes behind one splitter Strategy ════════════════════════════════════════════════════════════════════
Two CV designs, because they answer two different questions — plus a third that exists to be wrong. Implement all three as Strategy implementations behind one splitter interface:

1. LEAVE-ONE-CLUSTER-OUT — train on one cluster, predict the other, ~991 km away. Two folds. This measures extrapolation to unsampled regions, which is what the project's actual use case requires — AND, on this data, it is bounded by the theorem below: across the clusters it cannot rank the estimators.
2. WITHIN-CLUSTER SPATIAL BLOCKING — spatially blocked folds inside each cluster. This measures local interpolation at 1–13 km, a genuinely different and much easier question — and (per the theorem) the ONLY gate at which kriging can beat the baseline on this data. Report both. Collapsing them into one number would hide that the model may interpolate well locally and be useless at range — which given the geometry is the likely truth.
3. RANDOM K-FOLD — implement it too, labelled as the demonstrably-wrong comparison. Its purpose is to be reported alongside the spatial results to demonstrate the inflation that random splitting produces on autocorrelated data. GUARD it so it cannot be selected as the validation method for a published claim. Its known-answer test consumes tests/fixtures/known_answer.py's gaussian_process_field with a KNOWN range: random k-fold vs leave-one-cluster-out on the same field, the leakage measured as a NUMBER in our own suite.

════════════════════════════════════════════════════════════════════ THE TWO-FOLD GEOMETRY THEOREM — the frame for Sections 1 and 3 ════════════════════════════════════════════════════════════════════
On record before this runner exists, so the report cannot mistake geometry for a finding. Leave-one-cluster-out with exactly two clusters is two folds:
fold A: train E-cluster (21) → predict W-cluster (14), ~991 km away fold B: train W-cluster (14) → predict E-cluster (21)
With the fitted variogram range ≤ 13 km of support (E2.2: 21.6 km AT the candidate ceiling, unconstrained from above — nowhere near 991), kriging at ~991 km reverts to the training cluster's local mean with variance ≈ sill + Lagrange term — MEASURED in E2.2 on the real data: mid-gap prediction 19.29 vs training mean 19.53, variance 25.3 > sill 21.4. Therefore ACROSS clusters, kriging ≈ baseline BY CONSTRUCTION — the across-cluster comparison measures exactly one thing, cluster A's mean against cluster B's values, and structurally CANNOT rank the estimators. Three consequences:
(a) "kriging ≈ baseline across clusters" is GEOMETRY, not a model finding — the report must frame it as such; a walkthrough sentence reading "kriging failed to beat the baseline at range" would be reporting a theorem as an outcome. (b) the WITHIN-cluster gate is the only place kriging can beat the baseline on this data, so the two-gate separation is LOAD-BEARING, not decorative — the two designs above are not two views of one comparison but the only comparison and a measurement that is not one. (c) the across-cluster fold STILL RUNS AND REPORTS, because "the two clusters differ by X" is itself a measurement — it is just not a model comparison. (RF is bound additionally by the E2.0-3 ceiling — 4 distinct X rows — and its across-cluster predictions are the training cluster's cell means; the E2.3 closeout saturation finding applies. Kriging is exempt from that ceiling: coordinates.)
(The theorem appears AGAIN inside obligation 8 below, verbatim. The repetition is deliberate: the obligations are copied verbatim from BACKLOG per the verbatim rule, and deduplication does not outrank it.)
════════════════════════════════════════════════════════════════════ SECTION 1 — FOLDS, METRIC POLICY, DOC FIXES, TWO PROPOSALS (first commit; ends at one STOP carrying BOTH proposals) ════════════════════════════════════════════════════════════════════
1A. FOLD ASSIGNMENT (leave-one-cluster-out). Deterministic, from coordinates, reproducible — state the rule (the ~991 km gap makes any sane clustering identical; say which you used and why it cannot be sensitive to tie-breaking at these geometries). Two folds, per the theorem. Assignment recorded in the run manifest.
1B. WITHIN-CLUSTER BLOCKING — PROPOSAL ONE OF TWO. The within-cluster gate is the only live empirical question, so the runner needs a within-cluster scheme. Propose: with 21 and 14 stations in ~12 km clusters and 4 distinct covariate cells, the options are leave-one-station-out within cluster (n-fold, maximal data, but adjacent stations ~2 km apart leak spatial structure at the fitted 13 km range — QUANTIFY: per-fold nearest-neighbor distances, min and median, per cluster), or block-by-cell (honest for RF whose X is cell-constant, but 3–4 blocks is few), or both reported side by side. Show the pair-distance consequences of each on the real clusters. (Random k-fold needs no proposal — it is fully specified above and implemented in Section 2.)
1C. THE PHASE-0 SEAM — PROPOSAL TWO OF TWO (first-actions finding E). CrossValidator = Callable[[X, y, Estimator], list[CVScore]] takes ONE estimator and NO coords; the runner iterates a registry and routes per-estimator design matrices. Propose the seam resolution alongside the blocking proposal. Options at minimum: revise the Callable to take the registry and splitter; or keep it per-estimator, called once per name, with the shared deterministic splitter injected. State trade-offs against obligations 1 and 2 specifically — under each option, WHERE does fold-refit atomicity live, and what enforces never-cherry-pick?
1D. METRICS, decided at design time per obligation 3 (LIVE): name the metric set (RMSE, MAE at minimum; state anything else and why), and for EACH metric that divides by uncertainty or by variance, state the sd=0 policy NOW — the count is 0 today and pinned, so this is the insurance clause written while it is cheap. A metric that would silently drop sd=0 points excludes exactly the points where a model is most wrong about itself; if any metric in the set has that shape, it either gets an explicit named policy or it is not in the set.
1E. DOC FIXES RIDING ALONG (same commit):
(a) BACKLOG §4 — check the ORIGINAL fold-structure entry's box, delete the duplicate [x] twin the audit added, correct the header count. (Obligation 8 is what closes the item; this fixes how it was closed.)
(b) CLAUDE.md "Current status" — MINIMAL update only: complete through E2.3, training target settled (P2.B → Contract 8), next task E2.4 — and REMOVE "Do not jump ahead to Phase 2." That line is an ACTIVE instruction contradicting the current work in the highest-authority file, not stale decoration; every E2 session has survived it only because a handoff overrode it. The full status refresh remains P2.C.
(c) Track the E2.4 handoff in docs/handoffs/, unedited, per the P2.PRE instruction-provenance rule.
1F. NEW BACKLOG §2 ENTRY — P2.C doc-consistency fixes, landing the deferral per the deferral rule (its prompt text currently exists only in a planning transcript). Items, one line each: BACKLOG AOI denominator (108/114 stale); covariates.yaml header-vs-version (check current drift post-P2.0c bump); root README status (verify main is pushed FIRST); handoff Task A–D closeout paragraph; "blocked on" framing → contract-slot framing; docs/ prose contradicting the origin taxonomy; CI comment describing fixture-only runs; SESSION_STATE.md fate [KARL — DECIDE: update / supersede with pointer / delete]. Owner Karl + E. Trigger: before Phase-2 closeout or before the repo is shown to anyone, whichever comes first. Note that CLAUDE.md's countermanding line was pulled forward into this task (1E-b).
THEN STOP — one STOP, both answers: Karl picks the blocking scheme (1B) and the seam resolution (1C). Do not proceed to Section 2 until both are answered.
════════════════════════════════════════════════════════════════════ SECTION 2 — THE RUNNER (second commit; adversarial review) ════════════════════════════════════════════════════════════════════
2A. Implement against THE EIGHT RUNNER OBLIGATIONS, VERBATIM from BACKLOG §3 "E2.4 runner obligations" (a structural guarantee that depends on the runner honoring an unread backlog entry is a convention wearing a guarantee's name). Do not summarize them in code comments — cite the section and implement:
(1) assert_complete() makes the baseline REGISTERED, not run: the runner must iterate EstimatorRegistry.names() — never cherry-pick via get() — so a complete registry implies a run baseline; the registry header states exactly this division. (2) The registry hands out ONE shared stateful instance per name (unlike the stateless normalizer/covariate registries it mirrors): the runner must refit per fold or build fresh instances per fold (build_default_registry() per run is the cheap discipline) — an E2.2 kriging fit that caches partial state would otherwise leak across folds silently. (3) LIVE, not hypothetical (E2.3 closeout, 2026-08-14). Written when sd=0 meant "the baseline on constant y" — a constructed edge. QRF's zero-width predictions make it a real MECHANISM on real data: any training row whose pooled leaf population is single-valued has q16 == q84 and sd == 0, and the pairing template passes it (non-negative). Evidence: the real-matrix count is 0 of 35 today (every cell has ≥ 7 distinct y), pinned in test_random_forest.py, so a corpus change that produces a single-valued cell is a visible finding, not a silent 0 — but the mechanism exists now, and any E2.4 metric that divides by uncertainty WILL hit sd=0 the moment it does. Therefore the first dividing metric's sd=0 handling must be decided AT DESIGN TIME in E2.4, not discovered when the division throws — or worse, does not throw: a metric that silently drops sd=0 points is excluding exactly the points where the model is most wrong about itself. Constant-y baseline (uniform barren) remains the second, legitimate source. (4) Added at E2.2: kriging's reportable state (OrdinaryKrigingEstimator.report() — fitted + alternative models, the bin table the fit SAW, every excluded bin with its reason, the unsupported 13–986 km lag range) must reach the RUN PROVENANCE — E2.2 only exposes it; a prediction whose consumer cannot see the model was extrapolating between the clusters is exactly the unlabeled-scientific-looking-output defect the watermark family exists to prevent. (5) range_at_candidate_ceiling MUST be carried NEXT TO the fitted range_km in the run manifest (Karl, E2.2 §2 pre-commit): the real-data fit put the range at the candidate ceiling — the 10–13 km bin (γ 23.4, above the total variance 15.1) is still rising at the edge of support — so the honest answer is not "the range is R" but "the range exceeds what 13 km of support can resolve"; a manifest reader must see "unconstrained from above" beside the number, not in a walkthrough they may never open. Same for its floor twin range_below_first_supported_lag and residual_dof. (6) Added at E2.3: RF's report().validation_facing_fields() — seed, n_estimators and every sd-defining hyperparameter READ BACK from the fitted forest (max_samples_leaf, aggregate_leaves_first, weighted_leaves, the quantile grid, sd_ddof), the uncertainty method + its stated semantics, distinct_x_rows, and importance PER SEED — joins kriging's reportable state; the runner consumes report() and NEVER reaches around it to _forest (which can still be asked for OOB). The honest-named oob_diagnostic_not_validation may be carried ONLY under that name and never in a validation-facing field — E2.5's guard re-asserts this at claim time. (7) THE UNCERTAINTY-SEMANTICS COLUMN (Karl, E2.3 decision 5): the comparison table MUST carry an uncertainty-semantics column, because the three estimators now report three different KINDS of number — a sample moment (baseline SD, ddof=1), a model moment (√kriging variance, exceeding the sill far-field by the Lagrange term), and a quantile half-width (QRF (q84−q16)/2, uncertainty_method = "qrf_half_width_q16_q84"). A table that prints three "sd" columns without saying so invites a reader to compare them as one quantity. Each estimator's report() names its semantics; the runner prints them beside the numbers. Also carry RF's zero_width_training_predictions (0 of 35 today) — a zero paired uncertainty on real data is a red flag to show, never to floor. (8) THE TWO-FOLD GEOMETRY THEOREM — E2.4 design input, on record BEFORE the runner is written so the report cannot mistake geometry for a finding (E2.3 closeout, 2026-08-14). Leave-one-cluster-out with exactly two clusters is TWO folds:
fold A: train E-cluster (21) → predict W-cluster (14), ~991 km away fold B: train W-cluster (14) → predict E-cluster (21)
With the fitted range ≤ 13 km (E2.2: 21.6 km AT the candidate ceiling, unconstrained from above — but nowhere near 991), kriging at 991 km reverts to the training cluster's local mean with variance ≈ sill + Lagrange term (E2.2 measured this on real data: mid-gap prediction 19.29 vs mean 19.53, variance 25.3 > sill 21.4). Therefore ACROSS clusters, kriging ≈ baseline BY CONSTRUCTION — the across-cluster comparison measures exactly one thing, cluster A's mean versus cluster B's values, and CANNOT distinguish the estimators. Consequences the E2.4 prompt must carry: (a) "kriging ≈ baseline across clusters" is a geometry theorem, not a model finding, and the report must frame it as such; (b) the WITHIN-cluster gate — spatial blocking inside a cluster — is the only place kriging can beat the baseline on this data, which makes the E2.1 registry docstring's registered-vs-executed separation and any two-gate design load-bearing rather than decorative; (c) the across-cluster fold is still worth running and reporting, because "the two clusters differ by X" is itself a measurement — it is just not a model comparison. Cross-reference: §4 "Spatial CV fold structure" (the n=2-folds limitation, recorded 2026-07-28) — this item is WHY the n=2 across-cluster fold cannot rank estimators, not only that it is small.
2B. THE PHASE-0 REVISION PROTOCOL APPLIES (the E2.1 precedent). CVScore (single metric_value + baseline_metric_value, no fold id, no uncertainty semantics), RunManifest (no scores_first_visible, no estimator reports, no fold assignment), and the CrossValidator seam are being extended: restate before/after shapes, run the stale-reference sweep (Phase-0 sketches, engine.py, PROVENANCE.md's "shape fixed when the emitter is built" line now comes due), and record all of it in the walkthrough.
2C. INPUT ROUTING IS A DECLARATION, NOT AN INFERENCE. Which design matrix an estimator consumes (coords vs covariates) is declared ON the estimator or its registry entry — never matched by name in the runner. Name-sniffing is the same defect class as the path-inference the origin taxonomy replaced (P2.0d-2) and the title-sniffing the watermark refused (P2.0d-3): an inference where a declaration belongs. Add the declaration to the ABC or registry with a completeness check, and a test that a registered estimator lacking the declaration fails BY NAME. Flag to the reviewers: the temptation is `if name == "ordinary_kriging": use coords` — it works, and it is exactly the inference-over-declaration shape this project has removed three times.
2D. THE RUN MANIFEST AND THE PRE-REGISTRATION CLOCK (BACKLOG §2). The manifest extends the provenance chain (upstream_hashes to the TrainingMatrix manifest) and carries: fold assignment, per-fold per-estimator scores, both estimators' reportable state, the uncertainty-semantics column, the computed origin (SYNTHETIC via the matrix; watermark automatic), and scores_first_visible OUTSIDE the substance hash. Assert the chain end to end: matrix hash in the run manifest matches the actual matrix artifact, not a literal.
The clock, honestly sequenced: E2.4 produces the comparison scores; the moment they exist in a walkthrough, every acceptance threshold set afterward is POST-HOC FOR THIS DATASET, PERMANENTLY — and Contract 8's acceptance_thresholds slot does not exist yet ("arrives with E2.5"), so Track E cannot pre-register one. That design is correct; therefore:
· E2.4 RUNS ANYWAY. Its scores are measurements under the SYNTHETIC watermark, and E2.5's refuse-to-validate verdict — "no pre-registered gate existed when these scores were computed" — is the honest RECORDED OUTPUT, not a gap. · scores_first_visible sits outside the substance hash (the generated_at parallel: identical inputs must hash identically across days). State the consequence plainly IN THE FIELD'S OWN DESCRIPTION: the timestamp is MUTABLE METADATA — honest by convention, not by mechanism; nothing detects a hand-edited timestamp. The authoritative date is the COMMIT that introduced the scores, and the description must say so, so a reader knows where the real witness lives: the git history, not the JSON. · The kriging nuance, stated so it survives: RF's synthetic-era scores are NOISE-scores (X is synthetic noise on 4 distinct rows), and a later threshold is only weakly contaminated by them. But KRIGING fits real coordinates against real y — its scores are REAL MEASUREMENTS TODAY — so a threshold set after seeing kriging's scores is post-hoc in the FULL sense. The manifest dating (and the commit behind it) is what makes that checkable rather than arguable.
2E. THE ORIGINAL FOUR REQUIREMENTS — still binding; the eight obligations extend, not replace:
· The mean baseline runs in every fold, always, for every design. Report uplift over baseline per fold, not only averaged. · Report per-fold results, not just means. With two folds, a mean hides everything. · Fold assignment must be deterministic and recorded in provenance. · A spatial-leakage assertion: for every fold, assert train and test sets are disjoint and separated by a stated minimum distance. This is the test that makes the methodological claim real rather than asserted.
REVIEW PROBES (adversarial pass, aimed at the obligations' failure modes): cherry-picking (register a fourth dummy estimator; confirm the runner picks it up with zero code change), stale refit (the E2.2 atomicity finding's shape — force a mid-sequence refit refusal and confirm no estimator predicts with a stale system matrix), provenance completeness (remove one of kriging's reportable-state fields; confirm a test fails BY NAME, not a KeyError downstream), and the routing declaration (2C).
════════════════════════════════════════════════════════════════════ SECTION 3 — THE COMPARISON REPORT (third commit) ════════════════════════════════════════════════════════════════════
The report is a walkthrough section plus the manifest — no new claims surface. Frame per the theorem's three consequences: across-cluster results under a heading that names them as the geometry measurement ("how much the clusters differ"), within-cluster results as the empirical comparison, RF everywhere accompanied by the ceiling. The uncertainty-semantics column appears in every table that prints an sd-shaped number.
State the expected result honestly, IN ADVANCE, so deviation is the news: across clusters, kriging ≈ baseline is the theorem, not an empirical outcome to be surprised by — all three estimators ≈ cluster-mean differences. Within clusters, kriging may or may not beat the baseline at 1–13 km — THAT is the empirical question, and if it does not, that is a finding about the data (E2.2 measured ~68% of variance unstructured below station spacing), not a failure of the code. RF cannot beat the 0.348 ceiling anywhere on today's fixture. Do not tune toward beating the baseline. If the numbers match this, the finding is that the framework measures what it should. If they deviate, STOP and investigate before writing the report — deviation from a theorem is a bug until proven otherwise.
TESTS (across Sections 2–3, one consolidated list): · spatial disjointness per fold with a stated buffer, mutation-verified; · determinism of fold assignment; · baseline present in every fold's results (a completeness-style test — obligation 1 made structural); · random k-fold CANNOT be selected for a published claim (the guard, mutation-verified); · the known-answer leakage number on gaussian_process_field (random k-fold vs leave-one-cluster-out on the same known-range field — the inflation as a NUMBER in our own suite); · the sd=0 policy's named observer (obligation 3); · scores_first_visible outside the substance hash, and its description naming the commit as authoritative; · a registered estimator lacking the routing declaration fails BY NAME (2C).
MUTATION-VERIFY per the obligations' own list, one table in the walkthrough. Suite trajectory per commit. PATTERNS.md: the splitter Strategy lands in §4.2 — update its "no pattern fixes n=2; that's BACKLOG §4" line to point at obligation 8 (the theorem), per first-actions finding D. Stop for review before E2.5.
