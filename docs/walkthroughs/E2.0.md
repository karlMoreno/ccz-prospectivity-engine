# E2.0 — The training matrix (tasks 1–3)

**Audience:** the engineer of record. This walkthrough grows one section per
task, the P2.0 convention: §E2.0-1 is written; §E2.0-2 (covariate extraction +
the single-DEM guard) and §E2.0-3 (`TrainingMatrix` + its manifest) land with
their commits.

Reading order for E2.0-1:
[samples/source.py](../../engine/prospectivity/samples/source.py) (the ABC —
unchanged except one diagram label) →
[samples/corpus_csv.py](../../engine/prospectivity/samples/corpus_csv.py) →
[test_corpus_csv_sample_source.py](../../tests/test_corpus_csv_sample_source.py)
→ this file.

---

## E2.0-1 — `CorpusCsvSampleSource`

**What this closes.** `SampleSource` was a Phase-0 Strategy with zero
production implementations (PATTERNS.md §3.2; BACKLOG §3 since E1.3). The
MASS-only rule reached production only through
`Observation.is_training_eligible()` called ad hoc — the corpus manifest and
`plot_stack` each filter the corpus list directly — and every `SampleSource`
caller was a test double. E2.0-2/-3 need a production "give me the training
rows" seam; this is it.

```
  data/corpus/master_observations.csv          (108 rows, MASS/COUNT/COVER)
        │  pd.read_csv → NaN→None → Observation(**row)
        ▼                             └─ Contract-1 validation per row: a
  CorpusCsvSampleSource (STRATEGY impl)          forbidden row raises at load
        .load_observations()  ← the abstract hook, and ALL this task adds
        │
        ▼
  SampleSource.get_training_samples()  ← inherited; the gate lives on the
        │                                 ABC + Observation, NOT here
        ▼
  35 training-eligible MASS rows       (== manifest.training_eligible_count)
```

**The design decision, and its refusal:** the eligibility gate is deliberately
NOT reimplemented, not even as a private helper. `get_training_samples()` is
inherited unchanged, and eligibility is decided by
`Observation.is_training_eligible()` alone — two implementations of one rule
is how they drift (the prompt's words, and E1.2's normalizer-vs-schema
history's lesson). The one thing the class does is the hook: read, NaN→None,
validate each row through `Observation`. That last step means the production
read path applies the same evidence-class discipline as ingestion — a
hand-edited CSV row violating Contract 1 fails loudly at load, before any
consumer sees it.

**Per-file:**

| File | What it is |
|---|---|
| `engine/prospectivity/samples/corpus_csv.py` | The implementation: ~20 lines. `DEFAULT_CORPUS_CSV` resolves via `find_repo_root` (the `plot_stack` precedent); `csv_path` is constructor-injected (the same seam every adapter has via `dataset_loader`), so tests point it at tmp CSVs. A missing file raises `FileNotFoundError` — an absent corpus must never look like a corpus with nothing eligible. |
| `tests/test_corpus_csv_sample_source.py` | 10 tests in two deliberate groups: real-corpus (counts checked against `manifest.json` AND pinned literals) and constructed-row (rows built directly, serialized with the production `write_corpus_csv`, read back through the class). |
| `engine/prospectivity/samples/source.py` | One diagram label: "(Phase 1, real)" → "(E2.0-1, production)". No code change. |

**Why the constructed-row tests construct directly** (CLAUDE.md testing
convention 2): `Observation` does not enforce the eligibility gate at
construction — an ineligible row is representable — so the gate's negation is
observable. And the real corpus cannot distinguish some claims from their
negations: every real row is `is_open=true`, so only a constructed closed row
can catch the `is_open` condition being dropped. The mutation table below
shows that is not hypothetical.

**Test inventory (10 new; suite 261 → 271 at the committed state):**

| Test | Asserts, in plain English | Rule enforced |
|---|---|---|
| `test_default_path_is_the_committed_corpus_csv` | The zero-argument construction reads the real corpus file | production wiring, not a fixture |
| `test_load_observations_returns_the_full_pool_not_the_training_set` | 108 rows, {MASS:36, COUNT:36, COVER:36}, matching the manifest | the hook returns the pool; a pre-filtering loader would make the ABC's gate decorative |
| `test_training_samples_are_35_of_108_matching_the_manifest_count` | 35 == `training_eligible_count` == the literal | AR-P02 end-to-end; manifest equality checks CSV round-trip fidelity, the literal pins the number |
| `test_every_excluded_row_fails_a_named_gate_and_every_eligible_row_fails_none` | Per-row: each of the 73 exclusions fails a named condition (72 non-MASS, 1 qa-gated, 0 missing-abundance, 0 closed); each of the 35 fails none | per-reason accounting — an aggregate 35-of-108 does not prove per-item claims |
| `test_the_failed_box_core_is_loaded_but_excluded_from_training_by_name` | SO268/1_12-2 is IN the pool, flagged, with a real abundance — and NOT in training | flag-never-drop, and excluded for the RIGHT reason |
| `test_a_barren_zero_abundance_station_survives_the_round_trip_as_eligible` | abundance 0.0 comes back as exactly 0.0 and trains | barren is valid; the E1.2 falsy-zero bug class |
| `test_a_closed_mass_row_is_excluded_by_the_is_open_gate` | `is_open=false` excluded, control included | the license gate — this test is its sole observer (see mutations) |
| `test_flagged_and_fail_mass_rows_are_excluded_through_the_csv_path` | flagged and fail rows excluded via the class's own path | qa_status gates training; fail is terminal |
| `test_count_and_cover_rows_never_train_even_when_count_carries_abundance` | a COUNT row WITH legal abundance+mean_nodule_mass_g never trains; COVER never trains; MASS control does | MASS-only keys on evidence_class, not "has an abundance" |
| `test_a_missing_corpus_csv_raises_rather_than_returning_an_empty_pool` | absent file → `FileNotFoundError` | no silent empty pool |

**Mutations run (all in `Observation.is_training_eligible`, all reverted —
`git diff` empty — suite green after: 272 passed, 2 skips locally, which is
271 + the pre-existing uncommitted `test_regenerate_default_stack_plot` in
the working tree):**

| Mutation | Result |
|---|---|
| qa_status condition dropped | **16 tests FAILED** across 5 files, incl. 4 of the new ones (the per-reason accounting, the by-name exclusion, the CSV-path test, the 35-count) ✓ |
| `is_open` condition dropped | **exactly 1 test FAILED**: `test_a_closed_mass_row_is_excluded_by_the_is_open_gate` — 271 others green. The constructed row is the SOLE observer of this gate in the entire suite, because every real corpus row is `is_open=true`. This is the mutation the prompt required separately, and it is why ✓ |
| `abundance_kg_m2 is not None` → truthiness (`bool(...)`) | **2 tests FAILED**: the new barren-station test and `test_ingestion_pipeline`'s synthetic-fixture eligibility test (the synthetic corpus carries a 0.0 MASS row) ✓ |

The qa_status and is_open gates were mutated **separately**, per the prompt: a
single mutation tripping both would only prove that *a* gate runs.

**Hand-check:**

```bash
.venv/bin/python -c "
from engine.prospectivity.samples.corpus_csv import CorpusCsvSampleSource
src = CorpusCsvSampleSource()
pool = src.load_observations()
train = src.get_training_samples()
print(len(pool), len(train))                      # 108 35
print(sorted({o.evidence_class.value for o in train}))   # ['MASS']
print(any(o.event_id == 'SO268/1_12-2' for o in train))  # False
"
```

**Deliberately missing (scope fence):** covariate extraction, the single-DEM
guard, `TrainingMatrix`, and any provenance artifact — E2.0-2 and E2.0-3. Also
no caching of the parsed CSV (three ~40 KB parses per run is nothing; the
d-2 `source_queue_entries` precedent — correctness over caching) and no
origin declaration on the class itself (it carries no values; the corpus's
origin is declared where the corpus is).

**BACKLOG delta:** closed §3 `CorpusCsvSampleSource` (the last of the three
E1.5 reverse-audit findings); added §3 "Known-answer fixtures for the
estimators" (owner E, trigger E2.1) — recorded, not built, per the prompt.

**PATTERNS.md:** no new pattern — an existing seam got its first production
implementation. Updated for accuracy only: §1 gains the SampleSource Strategy
row, §3.2's zero-impl claim is resolved with the mutation evidence, §4.4's
suggestion is marked done.

**What the next task adds:** E2.0-2 samples the 8 covariate rasters at the 35
station coordinates (nearest-cell, recorded), enforces the Contract 3 v3
single-DEM rule at extraction, and reports the shared-cell count on the
synthetic DEM.

### E2.0-1b — review carry-overs (2026-08-13)

**The sole-observer warning moved into the test itself.**
`test_a_closed_mass_row_is_excluded_by_the_is_open_gate` now carries, in its
own docstring: that it is the SOLE observer of the is_open gate in the entire
suite; the mutation number (dropping the condition failed this test and only
this test — 271 of 272 green); and what the gate protects — is_open is the
LICENSE condition on a CC BY-NC 4.0 corpus, so a silently-absent gate is a
licensing problem in a published run, not a bad prediction. Same treatment as
P2.0d-1's unclassified-file negation fixture (the audit's own sole observer):
the person tempted to delete a test is reading the test, not a module header.
Docstring only — logic, name, and assertions untouched; suite unchanged.

**Other known single-observer cases** (from prior mutation tables only; no
new mutations run): the d-1 negation fixture is the one other case already
carrying an in-file warning ([test_data_origin_audit.py:515](../../tests/test_data_origin_audit.py#L515)).
Documented as single-observer in P2.0's tables but WITHOUT an in-file
warning: the SYNTHETIC and DERIVED evidence-check negation fixtures (d-2 §0.1,
each "FAILED alone"), and d-3's render-level bytes-differ test ("the ONLY
test that catches" a plot bypassing the watermark helper). The P2.0c review
mutations (#9 layer-origin copy, #10 hardcoded composition) each name a
single catching test but the record does not state whether others also
failed. Reported for a future hygiene pass, not fixed here.

**The prompts file revised, not preserved-stale.** The uncommitted rewrite of
`docs/prompts/phase2_prompts.md` lands in this same commit with the four
superseded E2.0 points corrected (target settled by P2.B/Contract 8;
watermark automatic since P2.0d-3; fourth-FORMAT vs fourth-ARTIFACT reworded;
STOP questions decided) and a dated revision note at the top of the E2.0
section. E2.1–E2.5 are retained byte-identical from the prior commit; the
Track G brief likewise except one deliberate deviation — its carry-forward
paragraph repeated the superseded "(blocks E2.0)" claim and was rewritten to
point at Contract 8 and the six-event contradiction (BACKLOG §1). The
pre-revision text remains in git history.
