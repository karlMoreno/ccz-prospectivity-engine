# E2.2 — Ordinary kriging (TS-6 parity)

**Audience:** the engineer of record. Grows one section per commit: §0 and
§1 are written; §2 (the estimator + fitter) lands after Karl answers the
three §1 decisions — the STOP is part of the task, not a delay.

---

## §0 — The fixture-degeneracy rule (committed alone, 954e4e0)

CLAUDE.md's testing conventions gained rule 4, in the section's
cite-the-defect style: a hand-computed fixture must separate the claimed
statistic from its neighbors — on `[2,4,6]`, SD, MAD, and half-range are
all 2.0 and a spread-statistic swap survived every hand-computed test
(E2.1 MB9); E2.0-2's `shared_cell_count == len(stations)` was the same
blindness in different clothes. The rule binds every fixture in E2.2, and
each fixture below states in its docstring which neighboring claims it
separates (see `test_variogram.py`'s module docstring for the first).

## §1 — The support report (REPORT, NOT FIT; ends in a STOP)

**What was built:**
[estimators/variogram.py](../../engine/prospectivity/estimators/variogram.py)
— `empirical_variogram(coords, values, bin_edges_km)` returning per-bin
(range, pair count, semivariance), with empty bins REPORTED (count 0,
semivariance None — absence, never zero) and `unsupported()` naming them.
Distances are great-circle km via the corpus manifest's own
`haversine_km`. `known_answer.empirical_semivariance` was NOT reused,
with the reason stated in the module docstring: it computes planar
Euclidean distances — right for the fixture's arbitrary planar units,
wrong for geographic coordinates, where a degree is not a kilometre and
an E-W degree at 14°N is ~3% shorter than a N-S one (both facts pinned in
`test_distances_are_great_circle_km_not_euclidean_degrees`).

**Bin edges, and why** (stated per the prompt): fine 2–3 km bins over
0–13 km — the within-cluster structure is the only place fine bins have
support; ONE wide bin over 13–986 km so the void is a named row rather
than an absence; one bin at 986–997 km isolating the between-cluster
contrast. `DEFAULT_BIN_EDGES_KM = (0, 2, 4, 6, 8, 10, 13, 986, 997)`.

**THE REPORT, on the real 35 training stations** (y: mean 19.53 kg/m²,
sample variance 15.06, SD 3.88):

| lag bin (km) | pairs | semivariance |
|---|---|---|
| 0–2 | 102 | 10.69 |
| 2–4 | 4 | 15.88 |
| 4–6 | 48 | 9.96 |
| 6–8 | 50 | 10.28 |
| 8–10 | 19 | 12.57 |
| 10–13 | 78 | 23.40 |
| **13–986** | **0** | **EMPTY — the unsupported range** |
| 986–997 | 294 | 16.17 |

Total pairs 595 (matches the manifest's training-eligible pair count).
Zero-pair bins, named explicitly: **13–986 km** — every curve any fitter
draws across it is an assumption, not an estimate. A finer pass also
found a secondary zero-pair gap at **3–4 km** inside the clusters (the
default 2–4 km bin's 4 pairs all sit at 2–3 km).

**What the data says before any fit** (the finding, with caveats):
semivariance at 0–1 km is already 10.2 — **~68% of the total variance is
unstructured below the station spacing** (a large nugget, whether
measurement-scale variability or sub-km geology). Weak structure at
0–8 km (~10–12), then 23.4 at 10–13 km — ABOVE the total variance,
suggesting a within-cluster trend (cluster-edge contrast) rather than
stationary structure. The far bin (16.17) sits at about the total
variance: the two clusters are, empirically, uncorrelated — consistent
with reversion-to-mean between them.

**Tests (6; suite 326 → 332):** hand-computed meridian fixture (unequal
spacings so each pair owns a bin; the ½ factor observable; km-vs-degrees
load-bearing — the degeneracy statement is in the module docstring);
great-circle-not-Euclidean pinned; empty-bin honesty; the real support
structure pinned as literals (595 / 301 within / 294 far / 13–986 empty —
the E2.0-2 pinning precedent, to be re-reported when between-cluster
sources land); full-state determinism; refusals.

**NO FITTING CODE lands in this commit**, per the prompt.

---

## §1-STOP — the three decisions (Karl's; proposals below, then WAIT)

**1. Minimum pairs per lag bin.** Proposal: **≥ 30** (the standard
geostatistical rule of thumb for a usable lag estimate). On the real
data with the default edges this EXCLUDES 2–4 km (4 pairs — its 15.88 is
noise from four pairs spanning a bin with an internal zero-pair gap) and
8–10 km (19 pairs), leaving 0–2 (102), 4–6 (48), 6–8 (50), 10–13 (78),
and 986–997 (294). The softer alternative ≥ 15 would keep 8–10 and
exclude only 2–4.

**2. Which lags the fit may see.** Proposal: **within-cluster only
(0–13 km)**, with the 986–997 bin REPORTED beside the fit as the
far-field contrast, never fed to it. Consequences of each choice on this
data, qualitatively: *within-only* — the fitted range means "local
structure scale inside a cluster" (bounded by ~13 km); the sill is only
weakly constrained (the 10–13 bin exceeds the total variance), and the
model honestly claims nothing beyond 13 km — far predictions revert to
the mean with variance ≈ sill + Lagrange term. *Including 986–997* — the
sill anchors near the total variance (~16), but the range is then chosen
by the curve family's shape across a void with ZERO pairs: any value
from ~13 to ~986 km fits the same data nearly equally, so the parameter
would be an artifact of the family, not an estimate — two orders of
magnitude of pure assumption wearing a number.

**3. Model family + nugget.** Proposal: fit **exponential** (REQUIRED in
the family list regardless — `known_answer.py` plants an exponential
model, so §2's recovery test has nothing to recover otherwise), report
**spherical** alongside as the alternative fit; decline Gaussian (its
parabolic origin is unstable under exactly our conditions — large nugget,
few short-lag bins); defer Matérn (a third shape parameter fitted from
4–5 usable bins is overfitting by construction). **Nugget: FITTED**,
bounded [0, sill] — the data's headline IS the large nugget (~68% of
variance below 1 km); fixing it at zero would misstate the main finding.
If TS-6 parity requires matching TS-6's exact family, that is a
`[GEOLOGY — ISAAC]` question worth one look before Checkpoint 3.

*§2 (implementation, known-answer recovery, adversarial review) proceeds
only after all three answers.*
