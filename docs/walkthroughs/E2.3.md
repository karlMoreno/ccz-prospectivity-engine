# E2.3 — Random forest + uncertainty

**Audience:** the engineer of record. One section per commit: §1 (the
uncertainty-method report — STOPS for Karl's decision), §2 (the estimator,
adversarially reviewed), §3 (the known-answer tests).

---

## §1 — The uncertainty method (REPORT; ends in a STOP)

Every estimator returns prediction AND uncertainty, paired — the E2.1
Template Method enforces it structurally. The question is HOW, and the two
candidates are not equivalent. Both were RUN, not argued.

### A fact that reframes the "zero new dependencies" option

**scikit-learn is not installed in this project.** It is on CLAUDE.md's
approved stack, and `pyproject.toml` says the approved ML libraries "land
with the phases that first use them" — E2.3 is that phase. So BOTH options
below add a dependency today; the difference is one package versus two.
The measurements were run in a throwaway venv, isolated from the project's
lockfile, so nothing was added before the decision.

### Option A — Quantile regression forest (`quantile-forest`, Zillow)

| Fact | Finding |
|---|---|
| Live package | `quantile-forest` 1.4.2 (PyPI; 25 releases from 1.1.0 to 1.4.2 — an active cadence). `scikit-garden` is the stale predecessor and is not considered. |
| License | **Apache-2.0** — compatible with an open, reproducible project. |
| Requires-Python | ≥ 3.9 (ours is pinned 3.11 — fine). |
| Declared deps | `numpy>=1.23`, `scipy>=1.4`, `scikit-learn>=1.5` — no upper pins, so no conflict with a current sklearn (1.9.0 resolved cleanly alongside it in the probe venv). |
| Transitive additions | NONE beyond scikit-learn's own (`scipy`, `joblib`, `threadpoolctl`). |
| Wheel | cp311 macOS-arm64 wheel available (12 MB) — no source build, unlike the netcdf4 case that pushed pangaeapy to an extra. |
| `requirements.lock` gains | `quantile-forest==1.4.2` on top of `scikit-learn`, `scipy`, `joblib`, `threadpoolctl` (which Option B ALSO gains). Net delta A over B: ONE line. |
| Under the origin taxonomy | a TOOL, not data — the taxonomy classifies values, not code dependencies (recorded so it is not re-asked). |

**What it measures:** the conditional distribution of y given X, from the
per-leaf y values across all trees — per-point predictive intervals, not a
spread of tree means.

### Option B — Ensemble spread (SD of per-tree predictions), sklearn only

**What it measures:** disagreement among trees trained on bootstrap
resamples. What a consumer will read it as: predictive uncertainty. Those
differ, and on our data they differ badly (measured below).

### The measurement — known-answer covariate field, planted y = 2·X₃ + N(0, 1)

Holdout of 2,000 points of the SAME generated field; 500 trees; nominal 90%
intervals (spread as ±1.645·sd; QRF as its 5–95% quantiles).

| n | gen seed | true holdout RMSE | spread: mean sd → coverage | QRF: mean width (ideal 3.29) → coverage | spread + residual-SD floor → coverage |
|---|---|---|---|---|---|
| 500 | 42 | 1.049 | 0.959 → **0.85** | 3.11 → **0.84** | 1.038 → **0.89** |
| 500 | 7 | 1.078 | 0.970 → 0.84 | 3.11 → 0.84 | 1.047 → 0.88 |
| 35 | 42 | 1.280 | 1.153 → 0.86 | 3.54 → 0.81 | 1.247 → 0.89 |
| 35 | 7 | 1.272 | 0.841 → **0.73** | 2.56 → **0.69** | 0.917 → 0.77 |
| 35 | 11 | 1.222 | 1.256 → 0.90 | 3.89 → 0.85 | 1.334 → 0.92 |

Read: on WELL-SEPARATED X, spread and QRF are both mildly under-covered
and roughly comparable; at n=35 both swing seed to seed (0.73–0.90 and
0.69–0.85). Neither wins here. The floor option is the most calibrated
row — but see its caveat.

### THE MEASUREMENT THAT MATTERS — rank-4 X, mimicking today's real matrix

35 rows, only FOUR distinct X rows (14/7/7/7, the E2.0-2 occupancy), y
varying WITHIN each group with the real cell-group means and sd ≈ 3.2:

| Quantity, at the 4 distinct rows | Values |
|---|---|
| True within-group sd (irreducible to ANY X-model) | 2.61 · 3.68 · 2.81 · 3.52 |
| **Ensemble spread** (SD of tree means) | **0.68 · 1.46 · 1.03 · 1.37** — understates by 2–4× |
| QRF 5–95% half-width, as an sd-equivalent | 2.70 · 3.08 · 1.93 · 2.83 — tracks the truth |
| Residual sd (in-bag training fit) | 2.93 — near the pooled within-group sd 3.06 |

**This is the prompt's warning made numeric.** With four distinct rows,
trees agree for bad reasons — they are all fitting the same 4 examples —
so the spread of tree MEANS collapses to a fraction of the real predictive
spread. A consumer reading it as predictive uncertainty is misled by a
factor of 2–4. QRF, which returns the conditional distribution of the y
VALUES in the leaves rather than the tree means, recovers the within-group
spread by construction.

### Option C — spread + a stated floor: honest, and here is where it fails

Combining spread with an in-bag residual SD floor was the best-calibrated
row on separated X. But its floor is the TRAINING residual — 0.39 at n=500
against a true noise sd of 1.0 (a forest overfits in-bag by design), so on
separated X the floor is 40% of the truth and the calibration was partly
luck. The honest floor is the OOB residual (1.06 — accurate), and OOB is
RANDOM resampling: on autocorrelated data it is spatial leakage, the
failure mode this project's methodology exists to refuse. Using OOB inside
the uncertainty would smuggle a leaked quantity into a validation-facing
number by another door — exactly what §2's structural OOB rule exists to
prevent. On rank-4 X the in-bag residual (2.93) happens to be near the
truth, but that is a property of this degenerate fixture, not of the
method. **Not defensible as a general rule; recorded so it is not offered
decoratively.**

### Recommendation

**Option A — quantile regression forest.** Its uncertainty is the
conditional distribution, which is what E2.4's comparison table means by
"uncertainty" for the other two estimators (the baseline's SD is a
predictive spread; kriging's variance is a predictive variance) — the
three columns would then answer the same question. On today's rank-4 data
the ensemble spread is wrong by 2–4× in the flattering direction, and no
in-house floor fixes that without either overfitting (in-bag) or leaking
(OOB). The dependency cost is one Apache-2.0 line on top of the
scikit-learn line E2.3 adds regardless, with no transitive additions, no
version conflict, and a binary wheel.

If Option B is chosen anyway, the estimator MUST report the spread under a
name that states what it is (e.g. `tree_disagreement_sd_not_predictive`)
and the walkthrough must carry the 2–4× understatement as a standing caveat
on every RF uncertainty column E2.4 prints.

**STOP.** The choice — dependency policy and uncertainty semantics at once
— is Karl's. §2 does not begin until answered.

---

## §1 — ANSWERED (Karl, 2026-08-14): QRF via `quantile-forest`

Correction to the §1 lockfile line, found on install: `scikit-learn` 1.9
also pulls `narwhals` (MIT), which the probe venv's name-filtered freeze
missed. It belongs to the sklearn line regardless of A vs B. Final
`requirements.lock` delta: `scikit-learn`, `scipy`, `joblib`,
`threadpoolctl`, `narwhals` (the sklearn set) + `quantile-forest` (the
decision) — six lines. `pyproject.toml` records the reason beside the
pins. Under the origin taxonomy these are TOOLS, not data.

## §2 — Implementation (second commit; adversarially reviewed)

### The two facts, reported before the code

**Fact 1 — the ceiling binds.** n = 35, k = 8, and on today's fixture X
has **FOUR distinct rows** (centered matrix rank **3** — four points span
a 3-D affine subspace). The E2.0-3 ceiling R² = 0.348 binds this
estimator: co-celled stations have identical X and different y,
irreducible to any covariate model. Importance is arithmetic over 4
examples. Kriging is exempt (coordinates); the asymmetry is on record in
the E2.0 closeout and will matter when E2.4 tables both.

**Fact 2 — the covariates are strongly collinear by construction.** All 8
derive from one DEM. The correlation matrix on the real TrainingMatrix:

```
                   depth slope aspect rough  prof   plan   tpi   bpi
  depth             1.00  0.04 -0.76  0.17  0.94  0.97  0.96  0.98
  slope             0.04  1.00 -0.02 -0.73  0.23  0.27 -0.00 -0.03
  aspect           -0.76 -0.02  1.00 -0.57 -0.90 -0.80 -0.90 -0.86
  roughness         0.17 -0.73 -0.57  1.00  0.22  0.07  0.37  0.34
  profile_curvature 0.94  0.23 -0.90  0.22  1.00  0.98  0.97  0.96
  plan_curvature    0.97  0.27 -0.80  0.07  0.98  1.00  0.95  0.95
  tpi               0.96 -0.00 -0.90  0.37  0.97  0.95  1.00  1.00
  bpi               0.98 -0.03 -0.86  0.34  0.96  0.95  1.00  1.00
```

**12 of 28 pairs have |r| > 0.9**; tpi/bpi correlate at 1.00 (on 4
distinct rows they are the same column up to affine map). Consequence:
collinear features split credit unstably, so a single-seed importance
ranking is noise wearing a bar chart — `report()` carries importance PER
SEED, and the churn is measured below.

### What was built

```
  X (n,k), y ──► RandomForestEstimator.fit  (ATOMIC, the E2.2 lesson)
                   ├─ RandomForestQuantileRegressor(seed, n_estimators=500,
                   │     max_samples_leaf=None ← the ONE non-default)
                   ├─ importance under `importance_seeds` (extra forests)
                   └─ OOB only if compute_oob_diagnostic=True
                 ._predict → (mean, sd) of the CONDITIONAL DISTRIBUTION
                   (199-point quantile grid, aggregate_leaves_first=False)
                 .predict_quantiles → interval face of the same distribution
                 .report → RandomForestReport
                   .validation_facing_fields()  ← explicit ALLOW-LIST
                   .oob_diagnostic_not_validation  ← OUTSIDE it, by name
```

Registered as `random_forest`; the baseline remains the only REQUIRED
entry.

**Uncertainty semantics delivered (the §1 claim, checked on the rank-4
fixture — numbers RE-DERIVED after the review's must-fix, see below):**
QRF sd at the four distinct rows **2.52 / 3.38 / 2.57 / 3.25** against
true within-group sd0 2.51 / 3.41 / 2.60 / 3.26 — ratios 1.00 / 0.99 /
0.99 / 1.00 — where the ensemble spread of per-tree means is 0.69 / 1.50 /
1.01 / 1.38 (sd/spread 2.3–3.7×, the §1 finding now measured on the test
fixture itself). Means match the group means to < 0.1. On DISTINCT X
(the §1 known-answer field, n=35, planted noise sd 1.0): mean sd 1.11,
90% coverage 0.84. Deterministic under a seed.

**The pre-review code was wrong, and the rank-4 fixture could not see
it (the review's must-fix — CLAUDE.md rule 4 exactly).** The WIP commit
passed quantile-forest `aggregate_leaves_first=False`, which computes
each TREE's quantile function and AVERAGES them — not the pooled
Meinshausen distribution the docstring described. On any training set
with DISTINCT X rows every leaf holds one y, every per-tree quantile
function is flat, and **the sd is identically 0** (reviewer probe on the
§1 field: sd max 0.0000, 90% coverage 0.000; the same forest under
`True`: sd 1.12, coverage 0.85 — which is how §1's own table was
produced, since that probe used the library default). On rank-4 X it
understated the pooled sd by 17–31% (2.08 / 2.39 / 1.80 / 2.24), which
the pre-review docstring misattributed to "bootstrap shrinkage". Today's
corpus is co-celled, so the sd was nonzero by accident of the fixture;
the day a corpus with distinct covariate rows arrives, RF uncertainty
would have been 0 and the pairing template (finite, non-negative) would
have blessed it. Fixed: `aggregate_leaves_first=True, weighted_leaves=
True` (Meinshausen eq. 5–7), the mean from quantile-forest's exact
`quantiles="mean"`, and a DISTINCT-X test that pins sd > 0 near the
planted noise — the observer the rank-4 fixture structurally lacked.

**The RF2 record, corrected twice.** Mutation RF2 (`max_samples_leaf` →
1) left the pre-review suite green; the author's first explanation ("leaf-
1 is a random-subsample approximation, less bootstrap-shrunk") was
half-right: quantile-forest's leaf-1 IS a random one-per-leaf-per-tree
subsample, but its predict path POOLS across trees regardless of the
aggregation flag — so the comparison was pooled-leaf-1 vs quantile-
averaged-retain-all, apples to oranges. Under the correct pooled setting
retain-all is 0.99–1.00× the truth and leaf-1 0.94–1.02×: retain-all is
both the definition AND empirically closer. And RF2 was invisible for a
second reason the review found: `report()` ECHOED the constructor literal
`max_samples_leaf: None` instead of reading it back from the fitted
forest — a mutated forest carried a FALSE provenance record with no test
failing. `report()` now reads `get_params()` and `len(estimators_)`; RF2
and RF5 (n_estimators forced) both fail by name through the report test.

**The OOB hard rule, structural.** OOB is computed only on explicit
request, carried ONLY under `oob_diagnostic_not_validation` (docstring:
random resampling = spatial leakage on autocorrelated data), and
`validation_facing_fields()` is an explicit allow-list — a future field is
not validation-facing until added on purpose, so OOB cannot arrive by
omission. The test (strengthened by the review) asserts with OOB COMPUTED
(a real float exists to leak): no OOB-shaped name anywhere; a RECURSIVE
leaf walk finds no value equal to the OOB float or its cheap derivations
(1−oob, 100·oob, rounded, negated); and the STRUCTURAL check — an OOB-on
and an OOB-off estimator under the same seed produce IDENTICAL validation-
facing structures, so nothing in them can have been computed from OOB.
The production registry instance is pinned OOB-off. Honest limit, stated
(review): the rule holds for `report()`/`predict()`/`predict_quantiles()`;
the underscore-private `_forest` can still be asked for OOB by a caller
who reaches around the estimator — E2.5's refuse-to-validate guard is the
claim-time backstop, and E2.4's runner must consume `report()`, never
`_forest`.

### Importance instability — measured on the real matrix, 10 seeds, 500 trees

```
  seed 0: aspect .331  slope .320  profile_curvature .242
  seed 1: aspect .361  slope .290  profile_curvature .247
  ...
  seed 7: slope  .315  aspect .313  profile_curvature .252
  seed 9: profile_curvature .304  aspect .304  slope .281
  top-1 across seeds: aspect 8, slope 1, profile_curvature 1
  rank ranges: aspect 1–2 · slope 1–3 · profile_curvature 1–3 ·
               roughness 4–4 · depth/plan/tpi/bpi 5–8
```

**The ranking is rock-stable in its top-3 SET, and that is itself the
suspicious result the prompt anticipated — with the MECHANISM corrected by
the review.** The author's first reading ("choosing among ~3 effective
directions, so importance cannot churn") was audited and refuted: random
rank-4 fixtures with a KNOWN generating column churn UNIFORMLY across
seeds (all 8 features 0.15–0.22, top-1 changing seed to seed) and never
reliably place the generating column — or its exact affine twin — first.
So 4-row degeneracy per se produces INSTABILITY, and the stability on the
real matrix has a different, more mundane cause: with four X rows, split
candidates are the ORDERING ENDPOINTS of each column, and sklearn's
absolute 1e-7 split threshold on the ~1e-6-magnitude curvature values
freezes which columns can even be split — a unit change on
plan_curvature moves it into the top group with IDENTICAL predictions.
Either way the conclusion holds: the top-3 stability is an artifact of
the 4-row support, not evidence of a robust signal, and importance claims
on the real 35-station matrix are unsupported. Within the top 3 the order
does churn (three different top-1 features across ten seeds).

**Two numbers that close the loop:** RF in-sample R² on the real matrix =
**0.348 — the E2.0-3 ceiling exactly** (a full-depth forest on 4 distinct
rows memorizes the four group means; independent confirmation the ceiling
arithmetic is right). The OOB diagnostic, requested for the record only:
R² = 0.161 — a RANDOM-resample number, NOT validation, printed here once
so its magnitude is on the page and never in a claim.


### Adversarial review — run against a COMMITTED state (efc683a), tree verified clean after

The E2.2 incident's procedural fix, applied: the work under review was
snapshotted as a WIP commit BEFORE the review launched; reviewers were
instructed to restore mutations with `git checkout` (safe against a
committed baseline) and verify a clean tree — all three did, and each
noted the concurrent walkthrough draft as not theirs and left it alone.

Three lenses (uncertainty semantics / OOB leakage paths / testing
conventions incl. rule 4): 21 raw → 13 distinct findings, each first
REPRODUCED on the committed code before any fix.

| Finding | Verdict | Taken as |
|---|---|---|
| `aggregate_leaves_first=False` averages per-tree quantile functions — sd identically 0 on distinct X, 17–31% low on rank-4 (found independently by two lenses) | **must-fix, reproduced** | pooled aggregation + exact mean; distinct-X observer test; RF4 |
| Rank-4 fixture blind to the collapse; 0.6–1.4× band could not tell pooled from per-tree-averaged | should-fix (rule 4) | band tightened to ±5%; distinct-X fixture added |
| RF2 explanation wrong: leaf-1 predict path pools regardless of the flag | should-fix | docstring + walkthrough corrected (above) |
| `report()` echoes constructor literals; a mutated forest carries a false provenance record | should-fix | read back from `get_params()` / `len(estimators_)`; RF2/RF5 now caught |
| "exactly ONE non-default" false (n_estimators=500; aggregation flags absent from the report) | should-fix | every sd-defining setting recorded in `hyperparameters`; claim reworded |
| `_predict` accepts NaN X (sklearn routes it silently to a finite number) | should-fix | refused by name; RF8 |
| Per-seed importance never checked to be per-seed | should-fix | tuples-differ + fresh-forest equality; RF6 |
| OOB test blind to nested/derived values | should-fix | recursive leaf walk + derivations + on/off structural diff; RF7 |
| Production registry OOB-off unpinned | should-fix | pinned |
| ddof convention unstated; E2.4 comparability | should-fix | stated in docstring + `UNCERTAINTY_SEMANTICS` (√((n−1)/n) caveat) |
| Importance-stability mechanism misread | should-fix | corrected (above) |
| Real-matrix test pins nothing about predictions | nit | R² ≤ ceiling (0.348) + cell-mean tracking pinned |
| `predict_quantiles` squeeze/float32; grid-mean overclaim; module-wide warnings filter | nits | reshape + float coercion; exact `quantiles="mean"`; filter removed |
| Verified CLEAN by the review | — | OOB on/off leaves every allow-listed value bit-identical; leakage claim correct; default False; leaf-1 IS a random subsample (that half was true) |

**Every §2 mutation, one table (RF1–RF3 pre-review; RF4–RF8 closing
review-demonstrated gaps; all `cp`-restored, suite green after):**

| # | Mutation | Result |
|---|---|---|
| RF1 | `random_state=None` (seed ignored) | determinism test FAILED ✓ |
| RF2 | `max_samples_leaf` → 1 | pre-review: 11/11 GREEN (the gap); post-fix: report test FAILED via read-back ✓ |
| RF3 | OOB added to `validation_facing_fields` by name | OOB test FAILED by name ✓ |
| RF4 | `aggregate_leaves_first` → False (the must-fix reverted) | 2 FAILED: the ±5% semantics test AND the distinct-X observer ✓ |
| RF5 | `n_estimators` forced to 100 inside `_make_forest` | report test FAILED (200 claimed vs 100 read back) ✓ |
| RF6 | importance loop reuses the seed-0 forest | report test FAILED (tuples identical / fresh-forest mismatch) ✓ |
| RF7 | OOB nested under `hyperparameters` | OOB test FAILED (recursive walk) — plus 6 collateral, since the mutation site was the report constructor ✓ |
| RF8 | predict-time NaN refusal removed | refusals test FAILED ✓ |

---

## §3 — The known-answer tests (third commit)

Consumes what E2.1's fixture planted for this task: `covariate_driven_field`
— X noise, y = c·X₃ + N(0, σ²), one named driving column.

**The RF-specific degeneracy trap, checked and stated.** On a finite noise
draw the driving column can correlate with a noise column BY CHANCE, and
importance cannot separate "found the planted signal" from "found a
chance correlate of it". Measured under the fixture's seeds: at n=500 the
realized max |corr(driving, noise)| is 0.06–0.11; at n=35 it is 0.24–0.47
— **gen seed 1 realizes 0.47, a chance correlate at nearly half strength**.
The tests assert each used seed's realized correlation against a stated
threshold (0.15 at n=500; 0.32 at n=35, using seeds 0/3/5 at 0.29/0.25/
0.24) and keep gen seed 1 as the guard's NEGATION — the guard rejects it,
so the guard is not vacuous. Mutation KA2 (threshold weakened to 0.5, seed
1 admitted) fails that negation test.

**The two regimes — the contrast is a THRESHOLD, and that is the finding**
(6 generator seeds × 3 forest seeds each, 200 trees):

| planted coefficient (σ = 1) | n=500 top-1 hits | n=35 top-1 hits | median margin (driving / runner-up), 500 vs 35 |
|---|---|---|---|
| 2.00 | 18/18 | 18/18 | 32× vs 16× |
| 1.00 | 18/18 | 17/18 | 9.0× vs 4.1× |
| **0.50** | **18/18** | **9/18** | **3.0× vs 1.0×** |
| 0.35 | 18/18 | 3/18 | 2.0× vs 0.9× |
| 0.25 | 18/18 | 3/18 | 1.4× vs 0.8× |

Stated plainly: **a planted covariate signal is recoverable at n=35 only
when it is at least as strong as the noise (SNR ≥ 1); at SNR 0.5 — a
signal half the noise — n=500 still finds it every time with a 3× margin,
while n=35 finds it at chance (9/18) with the runner-up competitive
(median margin 1.0×).** Therefore importance claims on the real 35-station
matrix require an effect at least as large as the residual scatter to be
believable on sample-size grounds alone — and on today's fixture the
covariates are synthetic noise with four distinct rows, so no such claim
is available regardless. The tests pin the strong regime's recovery at
BOTH sizes (reported, not tuned away — the prompt expected degradation at
35 and the honest measurement is that SNR 2 recovers), and the weak
regime's asymmetry: recovery at 500, INSTABILITY at 35 asserted as such
(hit rate < 0.8, median margin < 2×).

**Uncertainty sanity on the known field**, generation-level holdout (a
second draw of the same field, not CV machinery): at n=500 predictions
track 2·X₃ (RMSE < 1.5σ), the ddof=0 sd sits near the planted σ, and the
QRF 5–95% interval covers held-out y at ≥ 0.75 (nominal 0.90; §1 measured
0.84 — mild under-coverage is the honest QRF behaviour, stated).

**Mutations (§3; `cp`-restored, suite green after):**

| # | Mutation | Result |
|---|---|---|
| KA1 | the driving column's values shuffled post-generation (its marginal kept, its use broken) | 3 FAILED: both strong-regime recovery tests + the contrast test ✓ |
| KA2 | degeneracy threshold weakened to 0.5 and gen seed 1 admitted at n=35 | 1 FAILED: the guard's negation test — the guard would have accepted a chance correlate ✓ |

---

# E2.3 CLOSEOUT

**Three commits:** `88bbc03` (§1 report → STOP → Karl: QRF) · `6fb8d7c`
(§2 estimator, reviewed against a COMMITTED state, WIP amended) · E2.3-3
(§3 known-answer tests, this commit).

**Suite trajectory:** 352 → **352** (§1: docs only) → **364** (§2: +12) →
**371** (§3: +7). 2 expected skips throughout.

**Every mutation, one table** (consolidated at the E2.X disposition audit,
ledger row 20 — the original closeout line predated E2.3-4's RF9–RF12 and
pointed at three separate tables; this is the one place):

| # | Task | Mutation | Result |
|---|---|---|---|
| RF1 | §2 | `random_state=None` (seed ignored) | determinism test FAILED ✓ |
| RF2 | §2 | `max_samples_leaf` → 1 | pre-review: 11/11 GREEN (the gap); post-fix: report test FAILED via read-back ✓ |
| RF3 | §2 | OOB added to `validation_facing_fields` by name | OOB test FAILED by name ✓ |
| RF4 | §2 review | `aggregate_leaves_first` → False (the must-fix reverted) | 2 FAILED: the ±5% semantics test AND the distinct-X observer ✓ |
| RF5 | §2 review | `n_estimators` forced to 100 inside `_make_forest` | report test FAILED (200 claimed vs 100 read back) ✓ |
| RF6 | §2 review | importance loop reuses the seed-0 forest | report test FAILED ✓ |
| RF7 | §2 review | OOB nested under `hyperparameters` | OOB test FAILED (recursive walk) + 6 collateral ✓ |
| RF8 | §2 review | predict-time NaN refusal removed | refusals test FAILED ✓ |
| KA1 | §3 | driving column shuffled post-generation | 3 FAILED: both strong-regime recovery tests + the contrast test ✓ |
| KA2 | §3 | degeneracy threshold weakened, gen seed 1 admitted | 1 FAILED: the guard's negation test ✓ |
| RF9 | §2-DECISION | q16/q84 swapped: sd = (q16 − q84)/2 | 9 FAILED — negative width refused by the crossing check ✓ |
| RF9b | §2-DECISION | swapped AND abs()'d | 14/14 GREEN — an equivalent computation; recorded, not a gap |
| RF10 | §2-DECISION | crossing refusal removed | pre-observer: 14/14 GREEN (monotone by construction); post-observer (injected crossing): 1 FAILED by name ✓ |
| RF11 | §2-DECISION | zero-width count hardcoded 0 | 1 FAILED: the constructed constant-cell observer ✓ |
| RF12 | §2-DECISION | half-width replaced by the moment sd | 3 FAILED incl. the seed test (the moment sd is not seed-invariant) ✓ |

Fifteen, of which two are honest survivals (RF9b: equivalent output;
RF10 pre-observer: no natural observer, since fixed) and one taught
something twice (RF2: a docstring overclaim, then a read-back defect).

**Dependencies (deliberate, one at a time):** `scikit-learn` — approved
stack, first-use phase; `quantile-forest` — Karl's decision, Apache-2.0,
one line, no transitive additions beyond sklearn's own set (which includes
`narwhals`, corrected from §1). Under the origin taxonomy these are TOOLS
— the taxonomy classifies values, not code; the audit's coverage-boundary
item (BACKLOG §3) is about value-bearing FILES, so nothing there changes
this. Rationale beside the pins in `pyproject.toml`; `requirements.lock`
regenerated (+6).

**PATTERNS.md: untouched, deliberately.** `RandomForestEstimator` is a
third Strategy under an existing seam; `RandomForestReport` is a value
object; the allow-list is a validation function. Recorded here so the
omission is visible.

**BACKLOG:** the E2.4 runner-obligations entry now numbers SIX (RF's
`report()` fields join kriging's reportable state; the runner consumes
`report()`, never `_forest`).

**What E2.4 continues with — and the six obligations belong in its prompt
VERBATIM, not as a pointer:** iterate `names()` and never cherry-pick;
refit the shared stateful instance per fold; name the sd=0 case in the
first dividing metric; carry kriging's reportable state (bin table,
exclusions, unsupported range, `range_at_candidate_ceiling` beside the
range) into run provenance; carry RF's `validation_facing_fields()`
likewise, never touching `_forest`; and — from this task's finding —
E2.4's known-answer CV test on `gaussian_process_field` measures the
random-vs-spatial leakage as a NUMBER, with the E2.0 ceiling (binding RF,
exempting kriging) in view when reading any fixture-era comparison.

---

## §2-DECISION — Karl's Section-1 resolution, applied (E2.3-4)

**Sequencing, stated honestly:** the six-point decision arrived after §2
(`6fb8d7c`) and §3 (`007a83e`) had been committed under a MOMENT mapping
(sd = the ddof=0 sd of the pooled conditional distribution). The decision
changes the paired-uncertainty MAPPING, not the aggregation (the review's
must-fix — pooled Meinshausen — stands untouched), so it lands as an
amending commit rather than a rewrite. Conditional escape checked first
and NOT triggered: `pip check` clean; sklearn 1.9.0 satisfies quantile-
forest's `>=1.5` (no upper pin); the Apache-2.0 LICENSE ships in the
installed dist; 1.4.2 is the current release of an actively-versioned
package.

**1. The quantile→sd mapping.** The paired sd is now **(q84 − q16) / 2** —
the ±1σ half-width, equal to the SD under normality and a distribution-
free analogue otherwise; `uncertainty_method = "qrf_half_width_q16_q84"`.
WHY a half-width and not a moment (in the module docstring): QRF's leaves
hold EMPIRICAL conditional samples — a handful at n=35 — and order
statistics are what such samples support honestly, where a second moment
over a few retained values is dominated by whichever extreme landed in
the leaf; and it is the quantity most comparable to what the other two
estimators call "one sigma". The FULL quantile set (q05, q16, q50, q84,
q95) per training row is carried in `report()` and its validation-facing
structure, so the asymmetry survives in provenance even though the pair
carries a symmetric width.

Numbers under the new mapping (re-derived): rank-4 fixture half-widths
2.66 / 2.89 / 2.95 / 3.33 against each group's OWN q16/q84 half-width
2.56 / 2.98 / 2.95 / 3.38 — ratios 0.96–1.04 (the moment version was
2.52 / 3.38 / 2.57 / 3.25; the two differ because the groups' draws are
skewed, which is exactly what makes a half-width and a moment separable —
a symmetric fixture could not tell the mapping from a moment, rule 4).
Distinct-X n=35: mean half-width 1.11 vs planted σ 1.0, ±1sd coverage
0.61 (nominal 0.68 — the honest mild under-coverage).

**2. Quantile sanity, tested.** q05 ≤ q16 ≤ q50 ≤ q84 ≤ q95 asserted on
every training row; the paired sd asserted equal to (q84−q16)/2 of the
SAME reported quantiles to float precision; a crossing (q84 < q16) is
REFUSED by name, never abs()'d into a plausible sd. Pooled quantile-forest
quantiles are monotone by construction (np.quantile over one sorted
array), so the refusal has no natural observer — mutation RF10 (check
removed) left every test green — and it is now observed by INJECTING a
crossing through the pooled-quantile seam (monkeypatch), so the belt is
tested rather than decorative.

**3. The zero-width diagnostic on the REAL matrix: 0 of 35.** Every cell
has ≥ 7 distinct y values, so no leaf population is single-valued today.
Recorded in `report().zero_width_training_predictions` and pinned in the
real-matrix test, so a corpus change producing a single-valued cell is a
visible finding, not a silent 0 sd. Not floored, not perturbed. The
constructed observer (a 14-station cell given constant y) yields exactly
14 zero-width predictions with sd exactly 0.0 there. The full quantile
set also shows the asymmetry the pair cannot: on the real matrix cell 1
(n=7) is left-skewed by 1.6 kg/m² — (q84−q50) − (q50−q16) = −1.6 — while
cell 0 is right-skewed by +0.4.

**A finding made while writing the seed test:** on rank-4 X the half-width
is **seed-invariant** — every tree's leaf for a cell holds a bootstrap
resample of the same ≤ 14 values, so the pooled q16/q84 is the group's own
quantiles whatever the draws; the MEANS differ across seeds, the widths do
not. A property of the mapping on degenerate X, pinned in the test as such.

**4. The hybrid, dismissed on the record:** ensemble spread plus a
residual-SD floor repairs AVERAGE coverage while carrying no per-point
information — the floor is a global constant, so it cannot express spatial
or feature-driven variation in confidence, which is what paired
uncertainty exists to express; that is why it lost, over and above the
in-bag-overfits / OOB-leaks problem §1 measured.

**5. E2.4 obligation 7 — the uncertainty-SEMANTICS column** — appended to
the BACKLOG runner entry: three estimators, three KINDS of number (sample
moment / model moment exceeding the sill by the Lagrange term / quantile
half-width); a table printing three "sd" columns without saying so invites
a reader to compare them as one quantity.

**6. Dependency hygiene:** `quantile-forest==1.4.2` and `scikit-learn==1.9.0`
pinned exact in `requirements.lock`; rationale beside the pins in
`pyproject.toml` and in §1 above; taxonomy note — a code dependency is a
TOOL, not data, unless the audit's coverage-boundary work later says
otherwise (it currently concerns value-bearing FILES).

**Mutations for the mapping (all `cp`-restored, byte-identical, suite green
after):**

| # | Mutation | Result |
|---|---|---|
| RF9 | q16/q84 swapped: sd = (q16 − q84)/2 | 9 FAILED — the negative width is refused by the crossing check, and every predict-path test sees it ✓ |
| RF9b | swapped AND abs()'d (magnitude identical) | 14/14 GREEN — an equivalent computation; the tests correctly cannot distinguish `abs(a−b)` from `b−a` when b ≥ a. Recorded, not a gap. |
| RF10 | crossing refusal removed | pre-observer: 14/14 GREEN (monotone by construction — no natural observer); post-observer (injected crossing): 1 FAILED by name ✓ |
| RF11 | zero-width count hardcoded 0 | 1 FAILED: the constructed constant-cell observer ✓ |
| RF12 | half-width replaced by the moment sd (the pre-decision mapping) | 3 FAILED: the half-width semantics test, the sd-equals-half-width test, and the seed test (moment sd is NOT seed-invariant) ✓ |

**Suite:** 371 → **374** (+3: monotonicity/mapping, zero-width, crossing
observer). E2.3 remains complete; E2.4's prompt now carries SEVEN runner
obligations verbatim (BACKLOG §3), the seventh being the semantics column.

---

# E2.3 CLOSEOUT — FINDINGS (approved 2026-08-14)

## The saturation finding — one fact, three channels, stated once

**THE COVARIATE CHANNEL IS SATURATED AT FOUR EFFECTIVE EXAMPLES, AND IT
SHOWS IN THE PREDICTIONS, THE IMPORTANCES, AND THE UNCERTAINTIES ALIKE.**

35 stations on 4 distinct covariate rows has now surfaced three times,
through three different instruments, and it is one fact:

| Channel | What surfaced | Where recorded |
|---|---|---|
| **Predictions** | the **0.348 R² ceiling** — identical X, different y: irreducible error to any covariate model; RF's in-sample R² sits at 0.348 exactly, memorizing the four cell means | [E2.0.md](E2.0.md) closeout ("The finding of the phase"); §2 above ("Fact 1"; "Two numbers that close the loop") |
| **Importances** | rank-4 importance — 8 covariates fighting over 4 examples; top-3 set frozen by ordering-endpoint splits, order churning across seeds; a planted signal recoverable at n=35 only at SNR ≥ 1 | §2 above ("Importance instability"); §3 above ("The two regimes") |
| **Uncertainties** | **sd == 0 predictions** — QRF leaves whose retained populations have only ever seen one answer, declaring certainty; **0 of 35 on the real matrix today** (every cell has ≥ 7 distinct y), pinned so the day it changes is a visible finding | §2-DECISION above (item 3); `report().zero_width_training_predictions` |

The forest is not confident; it is UNDER-INFORMED in a way that READS as
confidence — which is why the zero-widths were reported, not floored, and
why the E2.4 comparison table carries a semantics column rather than three
look-alike "sd" columns.

**Expiry condition, carried with the finding:** this dissolves at
**Checkpoint 1**, when real-GEBCO cells (~460 m at 13°N vs the fixture's
~11 km) re-separate X into 35 distinct rows. The ceiling rises toward 1,
importance gains 31 more examples to argue over, and single-valued leaf
populations become the exception rather than the structure. Kriging is
exempt throughout (it predicts from coordinates — E2.0 closeout), which is
the asymmetry E2.4's table must read with. BACKLOG §3's Checkpoint-1
re-report item is the trigger; the pinned tests (ceiling, occupancy,
zero-width) fail on the GEBCO stack BY DESIGN and are the re-report's
starting list.

## What this closeout put on record for E2.4 (BACKLOG, this commit)

- **Runner obligation 3 is LIVE** — sd=0 is a real mechanism on real data
  (count 0 today, pinned); the first dividing metric's sd=0 handling is a
  design-time decision, and a metric that silently drops sd=0 points
  excludes exactly the points where the model is most wrong about itself.
- **Runner obligation 8 — the two-fold geometry theorem** — across the two
  clusters kriging ≈ baseline BY CONSTRUCTION (fitted range ≪ 991 km →
  reversion to the training cluster's mean); the across-cluster fold
  measures cluster A's mean against cluster B's values and cannot rank
  estimators; only within-cluster blocking can. On record BEFORE the
  runner exists so the report cannot mistake geometry for a finding.
- **The pre-registration clock** (§2, Karl + E) — E2.4 runs anyway; its
  scores are measurements under the SYNTHETIC watermark and E2.5's
  refuse-to-validate is the recorded verdict; the run manifest must DATE
  score visibility (`scores_first_visible`, outside the substance hash)
  so a later threshold can carry `set_after_scores` truthfully; and for
  KRIGING specifically — real coordinates against real y — post-hoc
  contamination is real, not synthetic-era-only.
