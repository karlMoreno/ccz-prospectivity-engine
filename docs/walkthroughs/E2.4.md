# E2.4 — Spatially-blocked cross-validation and the comparison report

Specification: `docs/prompts/phase2_prompts.md` §E2.4 (revised 2026-08-14) — the
geometry theorem, the eight runner obligations verbatim, the score-dating design.
Sequencing: `docs/prompts/E2.4-sequencing-prompt.md` (tracked at §1). Handoff:
`docs/handoffs/HANDOFF_claude_code_E2.4.md` (tracked at §1). Three commits:
§1 folds + metric policy + doc fixes + two proposals (ends at ONE STOP), §2 the
runner (adversarial review), §3 the comparison report.

Reading order for §1: `engine/prospectivity/provenance/geometry.py`
(`single_linkage_labels`, `minimum_spanning_tree_edge_lengths_km`) →
`engine/prospectivity/validation/splitter.py` → `validation/metrics.py` →
`tests/test_cv_splitter.py`, `tests/test_cv_metrics.py` → this file.

---

## §0 — First actions (2026-08-18): the handoff cross-check, recorded

Suite at session start: **374 passed, 2 skipped** (matches the handoff). The
seven first actions surfaced five findings; each has a disposition in THIS
commit (the deferral rule: a landing spot at the moment of finding):

| # | Finding | Disposition |
|---|---|---|
| A | BACKLOG §4 carried the "Spatial CV fold structure" item TWICE — the audit closed it by ADDING a checked twin beside the open original; the header claimed "0 open" | **Fixed here (1E-a):** original box checked, twin deleted, closure text moved onto the original |
| B | `CLAUDE.md` "Current status" said Phase 1 / E1.4, "needs a decision on the training target first", and carried the ACTIVE instruction "Do not jump ahead to Phase 2" | **Fixed here (1E-b), minimally:** through E2.3, target settled as a Contract 8 slot, next task E2.4; the countermanding line removed with a note saying so. Full refresh stays P2.C |
| C | The handoff's "P2.C doc fixes" and "SESSION_STATE.md fate" existed only in a planning transcript — no BACKLOG landing spot (the exact shape the deferral rule forbids) | **Landed here (1F):** BACKLOG §2 entry with each item's CURRENT state verified against the repo (not transcript memory) |
| D | `PATTERNS.md` §4.2 said "no pattern fixes n=2. That's BACKLOG §4" — pointing at an item now closed by obligation 8 | **Fixed here:** §4.2 points at the theorem; "Realized in E2.4 §1" note added (the splitter Strategy shipped) |
| E | Phase-0 shapes E2.4 must EXTEND: `CVScore` (single value, no fold id, no semantics), `RunManifest` (no `scores_first_visible`, reports, or fold assignment), `engine.py`'s `CrossValidator = Callable[[X, y, Estimator], list[CVScore]]` (ONE estimator, NO coords) | **Design input → proposal 1C below;** the Phase-0 revision protocol (2B) applies in §2 |

**Paste vs tracked spec (the AUTHORITY NOTE):** the eight obligations in the
paste and in `phase2_prompts.md` §E2.4 are IDENTICAL — `difflib` ratio 1.0,
zero differing opcodes over the (1)–(8) text (the review re-derived it; the
first draft's "650 words" was one tokenizer's count of a shorter span and is
dropped — the identity is the claim). The paste ADDS (does not diverge): the
Section-1 sequence (1A–1F), the Phase-0 revision protocol (2B), the input-
routing declaration (2C), the end-to-end chain assertion (2D), the four
review probes, and Section 3's framing. Both instruction documents are now
tracked in the repo (P2.PRE rule): the handoff unedited under
`docs/handoffs/`, the prompt unedited under `docs/prompts/` — a header
comment on each says what it is and where its cross-check lives (here).

Handoff facts verified in the repo before honoring them: 21 E (lon −117.03)
/ 14 W (lon −125.90); centroid distance **991.1 km**; within-cluster extents
11.17 / 12.02 km; min pair 0.054 km; 595 training pairs, zero in 13–986 km;
zero-width count pinned at 0 (`test_random_forest.py`); the E2.2 atomic-fit
comment names E2.4's per-fold refit; obligation 3 marked LIVE; the tripwire
in the pre-registration entry; §E2.4's revision line dated 2026-08-14.

---

## §1 — Folds, metric policy, doc fixes, two proposals (first commit; ONE STOP)

### What was built

```
  TrainingMatrix.coords (n,2) lon/lat ──► FoldSplitter.split()  [STRATEGY]
                                                │
      SingleLinkageBlockSplitter(linkage_km, design_name)
        · geometry.single_linkage_labels  ← THE ONE union-find (the corpus
        ·   manifest's cluster summary calls the same function)
        · geometry.minimum_spanning_tree_edge_lengths_km → the STABILITY
        ·   INTERVAL [largest MST edge ≤ t, smallest MST edge > t)
                                                ▼
                                          FoldAssignment  (as data, for the
                                            labels · folds · rule ·   run manifest)
                                            parameters · stability_interval_km
                                            INVARIANTS at construction: every
                                            index in range; every row held out
                                            EXACTLY once; labels ↔ folds agree;
                                            coords_sha256 = the coordinates it
                                            was split from (row order included)
                                                │
          assert_spatially_separated(assignment, coords, min_separation_km)
            re-MEASURES every fold's min train–test distance from the
            coordinates (never trusts the fold's recorded number); refuses
            coordinates whose fingerprint is not the assignment's; raises
            by fold name
  ------------------------------------------------------------------------
  validation/metrics.py: rmse · mae · mean_error · coverage_1sigma ·
     standardized_rmse (sd=0 POLICY) · r2_pooled (never per fold) ·
     rmse_uplift (difference) · rmse_ratio (zero-baseline policy)
     every value is a MetricValue(value | None, status NAMING why)
```

`FoldSplitter` is the §4.2 Strategy (PATTERNS.md updated). Its ONE piece of
class-level state is a DECLARATION, `spatially_blocked: ClassVar[bool]`,
enforced by `__init_subclass__` exactly as E2.1 enforces predict-is-final:
a splitter that does not say whether its folds may back a claim cannot be
declared (looked up through the MRO, so a subclass of a declared splitter
inherits the declaration — inheritance is a declaration; the ABC carries no
value, so an undeclared hierarchy is still refused at its first concrete
class, abstract or not — an intermediate base must declare or inherit).
E2.5's guard will read the CLASS declaration and the recorded
`FoldAssignment.spatially_blocked` (validated as a bool, and written FROM
the class, never as a literal); nothing infers it from a name (the P2.0d-2 /
P2.0d-3 / 2C rule). `FoldAssignment` enforces the invariants every planned
design shares — every index is a valid row; every row is held out exactly
once; every fold's test set is exactly the rows of one label (labels and
folds are one record) — and carries `coords_sha256`, so the leakage
assertion refuses to measure an assignment against coordinates it was not
split from (same length, different row order — a rebuilt matrix). The
splitter also refuses (latitude, longitude)-ordered input by name: the
real corpus swapped is still (n, 2) and finite and would split into two
plausible folds with wrong distances.

### 1A — Fold assignment for leave-one-cluster-out: the rule, and why it cannot be tie-break-sensitive

**Rule.** Blocks are the connected components of the graph joining station
pairs within `linkage_km` (great-circle, `haversine_km`) — single linkage.
One fold per block: hold the block out, train on every other row. Default
linkage = `geometry.DEFAULT_LINKAGE_KM` = 100 km, the corpus manifest's own
clustering, so `spatial_summary_training_eligible.clusters == 2` and "LOCO
has two folds" are ONE computation (`single_linkage_labels`), not two that
happen to agree.

**Why no tie-break exists.** Connected components are a property of the
graph, not of the order edges are visited. The partition changes ONLY when
the threshold crosses a minimum-spanning-tree edge length; between two
consecutive MST edges it is identical. On the real 35 stations the MST's
last two edges are **10.018 km** and **986.036 km**, so any linkage in
**[10.018, 986.036)** — a 976 km plateau with 100 km near its logarithmic
middle — yields the SAME two folds. `FoldAssignment.stability_interval_km`
records that interval per split, so the claim is a manifest fact, not a
sentence here. Tested at both edges EXACTLY (bit-identical: the same
haversine computes the edge and the threshold): linkage == 10.018 km
reproduces the assignment; linkage == 986.036 km merges everything and the
splitter refuses by name ("1 block").

```
  fold holdout_block_1(n=14, W)   train E 21 ──► predict W 14   min sep 986.04 km
  fold holdout_block_0(n=21, E)   train W 14 ──► predict E 21   min sep 986.04 km
```

### THE MEASUREMENT THAT REFRAMES 1B — the corpus is five SITES, and kriging cannot refit on most within-cluster folds

Before proposing a within-cluster scheme I measured what each candidate
does to the REAL clusters. Two facts the handoff did not carry:

**Fact 1 — each cluster is itself a handful of tight sites.** The MST edge
lengths (km, sorted): thirty edges of **0.054–0.857**, then **4.614,
6.219, 10.018, 986.036**. So at ANY linkage in **[0.857, 4.614)** the 35
stations fall into **five sites** (single-linkage plateau, verified as
partitions): E = three sites of 6 / 8 / 7 (site-to-nearest-site 4.61 and
6.22 km; the far pair 9.13 km), W = two sites of 7 / 7 (10.02 km apart).
Nearest-neighbour distance per station: E min 0.054 / median 0.349 /
max 0.716 km; W min 0.156 / median 0.532 / max 0.857 km. Every station has
a site-mate within 0.86 km. The fixture DEM's four covariate cells
(7/7/7/14) ARE four of these sites — E's two nearer sites (the 6 and the 8)
share one ~11 km cell.

**Fact 2 — under Karl's E2.2 decisions (≥ 30 pairs per bin, fit ≤ 13 km,
≥ 3 supported bins) kriging cannot refit on most within-cluster folds.**
Measured with `OrdinaryKrigingEstimator().fit()` on each candidate
training set (script in the session scratchpad; the numbers are the point):

| Training set | n | Kriging refit under production settings |
|---|---|---|
| all 35 (E2.2 reference) | 35 | fits: range 21.6 AT ceiling, dof 1, nugget 7.9 / sill 21.4 |
| **LOCO** train E | 21 | fits, **PURE NUGGET** (partial sill 0, range 0; nugget = sill = 11.2) → kriging ≡ E mean everywhere, EXACTLY the baseline |
| **LOCO** train W | 14 | **REFUSED** — only 2 bins survive (0–2: 39 pairs; 10–13: 49; 2–4: 3 < 30) |
| within-cluster leave-one-SITE-out (train = cluster minus site), all 5 folds | 7–15 | **REFUSED, 5/5** — 0, 1, 2 or 2 surviving bins |
| within-cluster block-by-cell, all 4 folds | 7–14 | **REFUSED, 4/4** |
| within-cluster LOSO, E (train 20) | 20 | fits 21/21 — ALL pure nugget → kriging ≡ training mean |
| within-cluster LOSO, W (train 13) | 13 | **REFUSED 14/14** |
| leave-one-site-out over ALL 35 (train = 35 minus site) | 27–29 | hold E-6 or E-7: fits, range 21.9 at ceiling, dof 0 · hold W-a or W-b: pure nugget · hold E-8 (the middle site): **REFUSED** (no 4–8 km support once it is out) |
| block-by-cell over ALL 35 | 21–28 | hold W-a or W-b's cell: pure nugget · hold E-7's cell: structured (ceiling) · hold the E-6 + E-8 cell (n = 14): REFUSED |
| **LOSO over ALL 35 (train 34)** | 34 | **fits 35/35**, range 21.6 at ceiling, dof 1 — the full fit's shape every time |

**Is it decision 1 (min_pairs = 30) or the geometry?** Mostly geometry, and
where it is decision 1 the relaxation buys nothing: LOCO-train-W refuses at
min_pairs 20, 10, 5 and 4 and fits only at ≤ 3 (on a 3-pair 2–4 km bin,
range 9.4 km); the W site folds, the E-7 fold and both W cell folds refuse
at ANY min_pairs — 7 stations in a ~2 km site give 21 pairs in one 0–2 km
bin, and no pair threshold creates lags that do not exist; the two E folds
that keep 4–10 km lags (hold E-8: train 13; hold E-6: train 15) fit once
min_pairs drops to 10 / 5 — and then find NO STRUCTURE (pure nugget, range
0), i.e. kriging ≡ the training mean. So relaxing Karl's decision 1 would
convert two refusals into two baseline-equivalents, never into a
comparison; there is nothing to ask him to relax.

**What this puts on record BEFORE the runner (obligation 8's spirit).** The
theorem said kriging ≈ baseline across clusters because the fitted range
(≤ 13 km) is ≪ 991 km. Under per-fold REFIT the mechanism is more direct
still, and it decomposes E2.2's full fit: the full fit's structure
(partial sill 13.5 of sill 21.4; range at the ceiling) rests on the ONE
rising bin, 10–13 km (γ 23.4, 78 pairs) — and that bin is **29 E–E pairs at
γ 15.5 plus 49 W–W pairs at γ 28.1**, the W cluster's two sites having means
15.1 and 21.7 kg/m² (Δ 6.5). Train on E alone: its 10–13 km bin has 29
pairs — one short of decision 1's 30 — and its three surviving bins (γ 13.0
/ 9.96 / 10.28 at mean lags 0.58 / 5.27 / 7.25 km) are flat, so the fitter's
honest verdict is NO STRUCTURE and kriging IS the E mean. Train on W alone: two
bins survive (0–2 km, 39 pairs; 10–13 km, 49) and the fitter refuses. The
theorem's CONCLUSION (kriging = training-cluster mean; cannot rank) holds
by construction either way; what will differ from E2.2's full-fit
expectation is the across-cluster kriging **sd** (pure nugget: √(11.2·(1 +
1/21)) ≈ 3.4, not √25.3 ≈ 5.0) and the fact that fold B carries a
REFUSAL, not a number. Stated now so §3 cannot report either as a surprise.
And the E2.2 atomicity probe is not hypothetical: on LOCO, kriging FITS on
the fold that trains on E and REFUSES on the fold that trains on W, so the
shared registry instance holds the first fold's state when the second
refuses — exactly the stale-system-matrix shape; the runner must record the
refusal per (estimator, fold) and never predict that fold with it. (For
Track G, eventually: "the range exceeds 13 km of support" in E2.2 is, on
decomposition, mostly "W's two sites 10 km apart differ by 6.5 kg/m²" — a
between-site contrast, not a within-site gradient.)

Site names used below (geographic, because label numbers depend on
ordering): **E-6** (SW, n = 6, mean 20.1), **E-8** (middle, n = 8, mean
20.5; 4.61 km from E-6, 6.22 km from E-7), **E-7** (N, n = 7, mean 20.2;
9.13 km from E-6); **W-a** (n = 7, mean 15.1) and **W-b** (n = 7, mean
21.7), 10.02 km apart. The fixture's four covariate cells: E-6 + E-8 share
one cell; E-7, W-a, W-b are one cell each.

### 1B — WITHIN-CLUSTER BLOCKING: PROPOSAL 1 OF 2 (Karl picks)

The spec's options were LOSO-within-cluster, block-by-cell, or both. Fact 2
kills the literal within-cluster-training reading for kriging (option D
below) and Fact 1 adds a coordinate-defined option (B). Every option below
trains on ALL other stations (both clusters) unless stated; the "within-
cluster" question is asked by WHAT IS HELD OUT and at WHAT DISTANCE the
nearest training station sits, which the fold record carries.

| Option | Folds | Train n | Kriging refit (production settings) | What it measures — the nearest-training distance | Caveats |
|---|---|---|---|---|---|
| **A. LOSO over all 35** | 35 | 34 | **35/35 fit**, structured (range 21.6 at ceiling, dof 1) | **within-SITE interpolation** at 0.05–0.86 km (E median 0.35, W median 0.53) — the sampled scale, BELOW the spec's "1–13 km" | site-mates in training (5–7 within 2.31 km — site diameters 0.43 / 1.43 / 2.00 / 2.14 / 2.31 km): the question is "can it interpolate between box cores at the spacing they were taken", legitimate but easy. RF sees the held-out cell via co-celled mates → predicts the cell mean of the rest: its best case, ceiling-shaped. Baseline = 34-station POOLED mean (E 20.29 / W 18.40 / pooled 19.53), so part of any kriging edge is "knowing the cluster" — ~1 kg/m² against within-cluster SD 3.5–4.3, small but stated |
| **B. Leave-one-SITE-out over all 35** — sites = single linkage at 2 km (plateau [0.857, 4.614) km, recorded per run) | 5 | 27–29 | **3/5 fit**: hold E-6 or E-7 → structured (range 21.9 at ceiling, dof 0 — unfalsifiable near-interpolation); hold W-a or W-b → PURE NUGGET (≡ training mean); hold E-8 → REFUSED (no 4–8 km support) | **site-to-nearest-site interpolation at 4.6 / 4.6 / 6.2 / 10.0 / 10.0 km** — INSIDE the fitted lag range; the spec's "spatially blocked folds inside each cluster … 1–13 km" in the only refit-feasible form | kriging column: 2 structured / 2 ≡ baseline / 1 refused — thin, and that IS the finding at 5–10 km. RF: 3 folds hold out an UNSEEN cell (extrapolation to a new X row on 3-cell training); 2 folds (E-6 and E-8 share a cell) see the cell through the sister site. Baseline pooled (27–29). Stable across DEMs — sites are a property of the corpus coordinates, not the fixture |
| **C. Block-by-cell over all 35** (the spec's option) | 4 | 21–28 | 3/4 fit (W-a's and W-b's cells: pure nugget; E-7's: structured; the E-6 + E-8 cell, n = 14: REFUSED) | as B with E-6 + E-8 merged into one fold that holds out 40% of the data | a FIXTURE-GEOMETRY artifact: the cells are ~11 km because the fixture DEM is; at Checkpoint 1 (GEBCO ~460 m) every station is its own cell and C dissolves into A. Honest for RF today, gone tomorrow |
| **D. Strictly within-cluster training** (train ONLY the held-out station's cluster): LOSO / site / cell | 35 / 5 / 4 | 13–20 / 7–15 / 7–14 | LOSO: E 21/21 fit but ALL pure nugget (≡ baseline), W 0/14; site-out 0 of 5 folds and cell-out 0 of 4 at min_pairs 30 — the W and E-7 folds refuse at ANY min_pairs, the hold-E-8 / hold-E-6 folds fit at min_pairs ≤ 10 / ≤ 5 as PURE NUGGET (≡ baseline) | the literal reading of "inside each cluster" | kriging cannot participate → not a model comparison. Recorded so it is not re-proposed |

**Recommendation: A AND B, side by side** (the spec listed "both" as an
option; the runner cost is one more Strategy instance and one more table).
They answer different questions at different scales — A: can the models
interpolate at the sampled spacing (kriging fits everywhere and can show a
within-site edge; RF at its ceiling-shaped best); B: can they interpolate
site-to-site at 5–10 km, where the variogram is actually fitted, with the
refusals and pure-nugget degeneracies reported per fold as the finding they
are. If ONE only: **B** — it is the spec's gate at the spec's scale and is
DEM-stable; A flatters RF and sits below 1 km. Recommended parameters: sites
at `linkage_km = 2.0` (mid-plateau; the plateau is recorded), design names
`leave_one_station_out` and `leave_one_site_out`, both `spatially_blocked =
True`, minimum-separation assertions at 0.05 km (A: the real minimum pair,
0.054) and 4.0 km (B: below the 4.614 km smallest site gap) — the buffers
STATED per design, per the original requirement 4.

**Not proposed, and why:** a variogram FIXED on all 35 and reused per fold
(gstat's `krige.cv` default) would make kriging fit on every fold of every
option — and would put the held-out y into the three fitted parameters,
which is a leak this project's refit-per-fold reading of obligation 2 exists
to refuse. Not offered as an option; recorded so its absence is deliberate.
Buffered LOSO (Le Rest / Roberts) at buffers in (2.31, 4.61) km — above the
largest site diameter, below the smallest site gap — removes exactly the
held-out point's site-mates, so its training set is B's for that point;
measured under within-cluster training (n_train 13–15 E / 7 W at 3–4 km;
at exactly 2.0 km seven stations keep a 2.0–2.3 km site-mate). It collapses
into B on the plateau, so it is not a fourth option.

### 1C — THE PHASE-0 SEAM: PROPOSAL 2 OF 2 (Karl picks)

Today: `engine.py` `CrossValidator = Callable[[features, target, Estimator],
list[CVScore]]` — ONE estimator, NO coordinates, no fold record; and the
engine holds `estimator: Estimator`, singular. E2.4's runner iterates a
registry, routes coords vs covariates per estimator, refits per fold, joins
every estimator's per-fold scores to the baseline's, and records the fold
assignment.

| | **(i) Revise the seam: the runner takes the REGISTRY and the SPLITTER** | (ii) Keep it per-estimator; the ENGINE loops over `names()` and calls the CV once per estimator with the shared splitter injected |
|---|---|---|
| Shape | `CrossValidationRunner(splitters, registry).run(matrix) → CVReport`; `engine.py` takes `estimators: EstimatorRegistry` (plural) and calls the runner ONCE; `CrossValidator` type retired (Phase-0 revision protocol, 2B) | `CrossValidator = Callable[[matrix, Estimator, FoldAssignment], EstimatorFoldScores]`; engine iterates `names()`, calls per estimator, merges the per-fold tables, attaches the baseline |
| **Never-cherry-pick (obligation 1)** | STRUCTURAL in the runner: it iterates `registry.items()` and contains NO `get()` call; `assert_complete()` at run start; the dummy-fourth-estimator probe passes with zero runner change | lives in the CALLER: any second call site can call the CV for one estimator and get a "score" with no baseline attached — the convention-wearing-a-guarantee shape the obligation names |
| **Refit atomicity (obligation 2)** | estimator-level (E2.2/E2.3 atomic fits) PLUS runner-level: per (fold, estimator) a fit refusal is caught, RECORDED (`status = refused`, message) and `predict` is NOT called for that pair; a fresh `build_default_registry()` per run — the LIVE case (LOCO fold B kriging refuses after fold A fitted) is handled in ONE place | estimator-level only; the "no predict after a refusal in THIS fold" logic is per call — repeated in every caller or lost |
| Per-fold baseline join (original req. 1) | inside the runner, ONE place; every estimator's fold row is built next to the baseline's for the same fold | the caller merges tables — a second place to drop the baseline |
| Fold record in provenance (req. 3) | the runner emits ONE `FoldAssignment` per design; every score row carries its fold name | each per-estimator call must be handed the SAME assignment; nothing enforces it |
| Cost | `domain/results.py` `CVScore` extended, `engine.py` seam + constructor arg change, `test_engine_template_method.py` updated (2B sweep) | smaller diff to `engine.py`, larger surface for a wrong join |

**Recommendation: (i).** Both obligations become properties of ONE object
with tests aimed at them, and the runner is where the eight obligations
"either hold or silently don't". **Where the routing declaration (2C) lives
under (i):** on the `Estimator` ABC as `input_kind: ClassVar[str]` in
`{"covariates", "coordinates"}`, enforced by `__init_subclass__` (an
estimator that does not declare what it consumes cannot be declared — the
strongest form; kriging consumes coordinates by construction, a property of
its math, not of its registration), read by the runner as
`estimator.input_kind` to route `matrix.X` vs `matrix.coords`, and
re-checked by `EstimatorRegistry.assert_complete()` (belt to the brace).
Test: a subclass without the declaration fails BY NAME at class definition;
the registry refuses an unknown kind by name. Consequence: the test-only
Estimator subclasses in `tests/` gain a one-line declaration (2B sweep).

### 1D — Metrics, decided at design time (obligation 3, LIVE)

| Metric | Formula (e = predicted − observed) | Divides by | Policy, stated NOW |
|---|---|---|---|
| `rmse` | √mean e² | n | — |
| `mae` | mean \|e\| | n | — |
| `mean_error` | mean e (SIGNED bias; + = over-prediction) | n | — across clusters this IS the theorem's measurement, "cluster A's mean vs cluster B's values" (8c) |
| `coverage_1sigma` | mean(\|e\| ≤ max(sd, tol)); nominal 0.683 UNDER NORMALITY, labelled `nominal_if_gaussian` (QRF's mean ± half-width is [q16, q84] only for a symmetric leaf population — the note travels in `details`) | nothing | sd ≈ 0 & e ≈ 0 → covered (a plain comparison); sd ≈ 0 & e ≠ 0 → uncovered. `n_sd_zero` carried |
| `standardized_rmse` | √mean z², z = e/sd; nominal 1.0 (> 1 over-confident) | **sd** | **sd ≈ 0 & e ≈ 0: z = 0, counted. sd ≈ 0 & \|e\| > tol: CONFIDENTLY WRONG → `value = None`, `status = "undefined: k confidently-wrong sd=0 point(s)"`, `n_sd_zero` and `n_sd_zero_wrong` ALWAYS carried, the finite RMS over sd > 0 points carried under `value_sd_positive_only` (itself finiteness-guarded). Never dropped, never floored.** |
| `r2_pooled` | 1 − Σe²/Σ(y − ȳ)² over ALL held-out predictions of a design | **SS_tot** | NEVER per fold (a LOSO fold has one test point, SS_tot = 0). Pooled: `None` with a named status when n < 2 or y CONSTANT — detected on the VALUES (max = min), never as float SS_tot == 0.0 (seven copies of 19.6 have SS_tot ~1e-28, and 1 − SSE/1e-28 is an absurd finite number with status ok — §1 review). Read against the 0.348 in-sample ceiling for RF |
| `rmse_uplift` | rmse_baseline − rmse_estimator (kg/m²; > 0 beats the floor) | nothing | a difference — per fold, per the original requirement |
| `rmse_ratio` | rmse_estimator / rmse_baseline | **baseline rmse** | `None` + named status when the baseline rmse is exactly 0; both comparison metrics refuse rmse values scored on different n (a mis-paired fold cannot produce an "uplift") |

**"sd = 0" and "e = 0" are NUMERICAL, not bitwise (§1 review, should-fix).**
The two LIVE sources do not produce bitwise zeros: a constant-y baseline's
`std(ddof=1)` is ~1e-15 whenever the mean is not exactly representable
(seven copies of the real abundance 19.6 → 3.8e-15, not 0.0), and
quantile-forest's weighted mean of a single-valued leaf can sit 1 ulp off
its constant. A bitwise `== 0.0` would MISS the first source (a
confidently-wrong prediction under sd 1e-15 would come out as a finite,
astronomically large z with status ok) and flag the second as confidently
WRONG (a 1-ulp residual). Both are defined through `ZERO_TOL_REL = 1e-9`
relative to the point's scale max(\|y\|, \|p\|) — one part in 10⁹, twelve
orders of magnitude below any measured abundance difference: a floating-
point equality, NOT a statistical tolerance, stated in every result's
`details`, and pinned by a test on exactly those two sources.

Every value is a `MetricValue(value, status, n, details)`: `value is None`
⇔ `status != "ok"` (enforced), and a non-finite value is REFUSED at
construction — verified on pydantic 2.13.4: `model_dump_json()` renders
`inf` as `null` and the artifact path `json.dumps(model_dump(mode="json"))`
writes the non-standard `Infinity`, either way silently — so the honest form
is a null WITH a status string, which survives serialization and the
substance hash unchanged. Not
in the set, deliberately: CRPS / log score (assume a distributional form the
three sd semantics do not share — a quantile half-width is not a Gaussian
σ) and interval scores at other levels (need quantiles the baseline and
kriging do not report). Coverage at ±1σ is the one calibration number all
three semantics support; the uncertainty-semantics column (obligation 7)
sits beside every sd-shaped number in §3's tables.

### 1E / 1F — doc fixes and the P2.C landing (this commit)

1E-a BACKLOG §4 fixed (original checked, twin deleted, header count) · 1E-b
`CLAUDE.md` status minimally refreshed and the countermanding line removed ·
1E-c handoff tracked unedited (+ the sequencing prompt, same rule) · 1F the
P2.C doc-consistency entry in BACKLOG §2 with each item's CURRENT state
verified against the repo (details there) · PATTERNS.md §4.2.

### Test inventory (§1)

| Test | Asserts (plain English) | Rule it enforces |
|---|---|---|
| `test_single_linkage_labels_follow_each_threshold_crossing_on_the_line` | on 0/1/3/7 km points, labels change exactly at each gap | fold assignment is a threshold property (1A) |
| `test_single_linkage_partition_is_identical_under_input_permutation` | shuffled input → the same PARTITION (mapped back through the permutation) | order-independence = no tie-break |
| `test_mst_edge_lengths_on_the_line_are_its_three_consecutive_gaps` | MST edges 1.001/2.002/4.003 km; one point → no edges | the plateau computation |
| `test_splitter_without_the_spatially_blocked_declaration_cannot_be_declared` | a subclass without (or with a non-bool) `spatially_blocked` fails at class definition, naming the attribute | declaration, not inference |
| `test_a_subclass_of_a_declared_splitter_inherits_the_declaration` | a grandchild that does not restate the declaration is accepted and reads True | inheritance is a declaration (MRO, not `cls.__dict__`) |
| `test_fold_assignment_refuses_out_of_range_rows_a_row_tested_twice_and_a_row_never_tested` | index 4 in a 4-row assignment, a row in two test sets, a row in none, and a label-count mismatch each raise by name | every row tested exactly once |
| `test_fold_assignment_refuses_labels_that_disagree_with_the_folds_and_degenerate_shapes` | a fold spanning two labels, a fold whose test set ≠ its label's rows, a non-bool declaration, zero folds, a NaN / negative recorded distance — each by name; a consistent assignment accepted | labels and folds are one record |
| `test_splitter_refuses_coordinates_in_lat_lon_order_by_name` | the real corpus as (lat, lon) is refused naming "wrong column order" | input routing cannot be silently wrong |
| `test_recorded_spatially_blocked_comes_from_the_class_declaration_not_a_literal` | a subclass re-declaring False records False; LOCO records True | the manifest value is the enforced one |
| `test_fold_refuses_overlap_repeats_and_empty_sets_by_name` | overlap / repeat / empty train / empty test each raise by name | disjointness is a construction-time property |
| `test_line_split_at_2_5km_gives_two_folds_with_the_measured_4km_separation` | labels (0,0,0,1); min separation 4.003 km; interval [2.002, 4.003) | the fold record's numbers |
| `test_splitter_refuses_by_name_when_the_threshold_yields_one_block` | linkage above every gap → "1 block" refusal | nothing to hold out |
| `test_leave_one_cluster_out_on_the_real_corpus_is_two_folds_21_E_and_14_W_986_km_apart` | 2 folds, 21 E / 14 W, every fold ≥ 986 km, interval [10.018, 986.036), parameters echoed, separation assertion at 900 km holds | 1A on the real corpus |
| `test_fold_assignment_is_deterministic_and_identical_across_the_stability_interval` | FULL-STATE equality across two calls; at 11 / 900 km and exactly at lo, `dataclasses.replace(other, parameters=a.parameters) == a` (every field but the linkage); exactly at hi → refuses | determinism; the half-open plateau, edges exact |
| `test_leave_one_cluster_out_changes_when_the_linkage_leaves_the_plateau` | at 10.0 km (< 10.018) → 3 blocks, AND per cluster: E stays one block, W splits in two | the plateau's lower edge is real (per-item, not a count) |
| `test_assert_spatially_separated_remeasures_from_coordinates_and_fails_by_fold_name` | the ONLY sub-2 km fold RECORDS 999 km (really 1.001) — at a 2 km buffer the assertion names it with the measured distance while the two honest 2.002 km folds pass; row-count mismatch refused | the leakage assertion measures, never trusts |
| `test_fold_assignment_record_is_json_serializable_and_complete` | `to_record()` round-trips with EXACTLY the nine top-level keys and the six per-fold keys, each equal to the assignment's own state | the §2D manifest form cannot drop a field |
| `test_rmse_mae_and_mean_error_are_hand_computed_and_distinct` | √10 / 2.4 / −0.8 on the fixture — and median\|e\| = 2.0 ≠ MAE | 1D; fixture separates RMSE / MAE / ME AND MAE from its median twin |
| `test_coverage_counts_a_correct_certain_point_as_covered_and_divides_by_nothing` | 3/5 with a correct-certain point; a wrong point whose sd drops to 0 becomes uncovered (2/5); the nominal is labelled `nominal_if_gaussian` with the QRF symmetry note | sd = 0 policy for coverage; obligation 7 |
| `test_standardized_rmse_hand_computed_with_the_correct_certain_point_contributing_z0` | √(6.69/5); counts 1/0; `value_sd_positive_only` = √(6.69/4) | policy branch 1 |
| `test_standardized_rmse_is_undefined_by_name_when_an_sd_zero_point_is_wrong_and_is_never_dropped` | value None; status names "1 confidently-wrong sd=0"; counts 2/1; finite part √(2.69/3); n stays 5 | **obligation 3's named observer** |
| `test_the_numerical_zero_treats_ulp_noise_as_zero_for_both_live_sd_zero_sources` | seven copies of 19.6 have SD 3.8e-15 (not 0.0): a 1-ulp-off prediction is correct-certain (z-RMS 0, coverage 1, n_sd_zero 7); a 0.05 error under that sd is "7 confidently-wrong", coverage 0; a real 1e-4 sd is NOT zero | the two LIVE sd=0 sources reach the policy |
| `test_r2_pooled_hand_computed_and_undefined_by_name_for_constant_or_single_y` | 1 − 50/78.8; constant y (5.0 AND seven copies of 19.6, whose float SS_tot is ~1e-28) and n = 1 → named statuses | SS_tot policy on the values, not the float |
| `test_rmse_uplift_is_a_difference_and_rmse_ratio_names_the_zero_baseline_policy` | uplift = b − e > 0; ratio; perfect baseline → named status; wrong inputs and mismatched n refused | comparison metrics |
| `test_metric_value_refuses_a_bare_null_and_a_non_finite_value` | None+ok, value+non-ok, inf → refused; a None serializes with its status | JSON honesty |
| `test_score_predictions_returns_exactly_the_declared_metric_set_in_order` | keys == `METRIC_NAMES` | the set is pinned |
| `test_score_predictions_values_equal_the_standalone_metrics_and_keep_the_sign_convention` | full `MetricValue` equality against the five standalone calls; mean_error −0.8, and +0.8 with (observed, predicted) swapped | the runner's entry point cannot mis-route arguments (sign is 8c's whole content) |
| `test_scoring_refuses_shape_mismatch_non_finite_and_bad_sd_by_name` | five refusals by name (shape, non-finite, sd shape, negative sd, empty) | input discipline |

### Every mutation, one table (§1; all restored by `cp`, suite green after)

| # | Mutation | Failure (by name) |
|---|---|---|
| M1 | remove the `spatially_blocked` declaration check | `DID NOT RAISE TypeError` (…cannot_be_declared) |
| M2 | `assert_spatially_separated` trusts `fold.min_train_test_km` instead of re-measuring | `DID NOT RAISE ValueError` (…remeasures…fails_by_fold_name) — the liar is the ONLY sub-buffer fold, so trusting passes everything |
| M3 | Fold disjointness check removed | `DID NOT RAISE` (…refuses_overlap…) |
| M4 | fold separation recorded as the MAX train–test distance | line-fixture separation ≠ 4.003 |
| M5 | single linkage `<` instead of `<=` (plateau would be (lo, hi]) | labels at linkage == lo differ from the assignment |
| M6 | `standardized_rmse` lets a confidently-wrong sd=0 point FALL THROUGH as z = 0 (a floor) | `assert 0.7335 is None` (= √(2.69/5)) — the finite flattering number, caught |
| M6b | `standardized_rmse` DROPS the confidently-wrong point and reports the sd>0-only RMS as the headline | `assert 0.9469 is None` (= √(2.69/3)), caught |
| M7 | coverage uses strict `<` | 0.8 comparison failed (correct-certain point read as uncovered) |
| M8 | `rmse_ratio` divides through a zero baseline | `ZeroDivisionError` — no named status |
| M9 | `MetricValue` accepts None with status ok | `DID NOT RAISE` (…refuses_a_bare_null…) |
| M10 | `r2_pooled` divides through SS_tot = 0 | caught one layer down: `MetricValue 'r2_pooled' carries a non-finite value -inf` |
| M11 | declaration check reverted to `cls.__dict__` (grandchild falsely refused) | `TypeError` at the grandchild's class definition (…inherits_the_declaration) |
| M12 | every-row-once invariant removed | `DID NOT RAISE` (…a_row_tested_twice_and_a_row_never_tested) |
| M13 | out-of-range index check removed | caught by the every-row-once check with a DIFFERENT message ("rows [0] are never held out") — the match on "row 4, outside" fails, so the named refusal is still guarded |
| M14 | `mae` computes the MEDIAN absolute error | 2.0 ≠ 2.4 comparison failed (the old fixture was blind to this — §1 review) |
| M15 | `score_predictions` swaps (observed, predicted) for `mean_error` | full-equality assertion fails on the sign |
| M16 | `to_record()` drops `min_train_test_km` from each fold | per-fold key-set equality fails |
| M17 | numerical zero reverted to bitwise `sd == 0.0` / `e != 0.0` | the 19.6×7 test: sd 3.8e-15 no longer zero → assertion fails |
| M18 | `r2_pooled` detects constant y via float `SS_tot == 0.0` | 19.6×7 → finite absurd R² with status ok → assertion fails |
| M19 | `split()` records `spatially_blocked=True` as a literal | the re-declared-False subclass records True → fails |
| M20 | lat/lon range check removed | swapped real corpus: `DID NOT RAISE` |
| M21 | labels ↔ folds consistency check removed | `DID NOT RAISE` |
| M22 | coordinate fingerprint check removed | reversed coordinates measured anyway → a leakage refusal with a DIFFERENT message; the fingerprint regex fails |
| M23 | `rmse_uplift` n-equality check removed | `DID NOT RAISE` |

### §1 pre-commit review — read-only, against the working tree, findings taken

Not the adversarial review the prompt schedules for §2 (the runner) — a
lighter pass on the §1 diff before Karl reads the proposals, four lenses
(splitter/geometry correctness · metrics vs obligation 3 · every numeric
claim in this walkthrough re-derived · CLAUDE.md test conventions), each
finding then handed to two skeptics told to refute it. **Incident, stated:**
the first run's three non-test lenses died on a session rate limit
(HTTP 429, "session limit … resets 10:10pm") and returned NOTHING — a
workflow that reports "0 findings" from a dead agent reads exactly like a
clean pass, so this is recorded rather than smoothed over; the three lenses
were re-run after the reset (second table below). The test-conventions
lens returned **8 findings; all 8 survived both skeptics** (each
reproduced by a scratch-copy mutation), all taken:

| # | Finding (severity) | Taken as |
|---|---|---|
| R1 | `score_predictions` values unguarded — swapping (observed, predicted) passed both files (should-fix) | new test: full `MetricValue` equality against the standalone calls; mean_error −0.8 and +0.8 swapped; mutation M15 |
| R2 | `..._record_is_json_serializable_and_complete` did not check completeness — six dropped keys passed (should-fix) | test asserts the exact nine top-level and six per-fold keys, each equal to the assignment's state; mutation M16 |
| R3 | metrics fixture blind to `mean → median` in MAE (\|e\| = [1,2,0,3,4] has mean = median = 2.0) — rule 4 exactly (should-fix) | fixture changed to e = [1, −2, 0, 3, −6] (MAE 2.4, median 2.0); every hand number re-derived; mutation M14 |
| R4 | M2's row described a failure the 3-fold fixture no longer produced (an honest fold below the buffer raised instead) (nit) | fixture rebuilt so the liar is the ONLY sub-buffer fold: trusting → `DID NOT RAISE` |
| R5 | M6's number was the FLOOR variant (z = 0 fall-through), not a drop (nit) | row corrected; both variants now listed (M6 floor, M6b drop) with their numbers |
| R6 | inventory said "four refusals", the test has five (nit) | corrected |
| R7 | "identical across the interval" compared three fields, not full state (rule 3) (nit) | `dataclasses.replace(other, parameters=a.parameters) == a` |
| R8 | "changes when the linkage leaves the plateau" asserted a count, not the per-cluster claim in its docstring (corollary) (nit) | asserts E stays whole and W splits |

**The three re-run lenses returned 21 findings.** Second incident, same
shape: a second rate limit ("resets 3:10am") killed 14 of the 42 skeptic
calls, so the six walkthrough-claim findings carry NO refutation verdict —
each was re-derived by hand instead (scratch measurement, numbers below)
and every one was right. Of the rest, 2 survived both skeptics and 13 were
"refuted" — in most cases because the skeptics read the working tree AFTER
the fix had already landed (they say so: "describes code that is not in the
working tree"); the remainder were judged hardening-without-a-failure and
were taken anyway because each was a one-line guard on the §2D manifest
path. All 21 acted on:

| # | Finding (lens · severity) | Taken as |
|---|---|---|
| R9 | `r2_pooled` tested `SS_tot == 0.0` bitwise — seven copies of 19.6 have SS_tot ~1e-28 and would yield an absurd finite R² with status ok (metrics · **must-fix**) | constant y detected on the VALUES (max == min); test with 19.6×7; M18 |
| R10 | the sd=0 branch tested `e != 0.0` bitwise — QRF's weighted mean of a single-valued leaf can sit 1 ulp off, so a correct-certain point would read confidently-WRONG (metrics · should-fix) | the NUMERICAL zero: `ZERO_TOL_REL = 1e-9` × max(\|y\|, \|p\|); test; M17 |
| R11 | `sd == 0.0` bitwise misses the constant-y baseline, whose `std(ddof=1)` is ~1e-15 — a confidently-wrong prediction under it got a finite astronomical z with status ok (metrics · should-fix) | same numerical zero; the 19.6×7 test pins both live sources; M17 |
| R12 | coverage's 0.683 nominal claimed for QRF "by construction" — false (mean ± half-width ≠ [q16, q84] unless symmetric); a Gaussian nominal emitted for every estimator without semantics (metrics · should-fix) | docstring corrected; `details["nominal_if_gaussian"]` + the symmetry note travels with the number |
| R13 | `value_sd_positive_only` not finiteness-guarded — an inf in `details` would serialize silently (metrics · nit) | guarded (None + note); the headline too |
| R14 | serialization claim: inside dict/Any-typed fields pydantic renders inf as `null`, not `Infinity` (metrics · nit) | docstring says both shapes, silent either way |
| R15 | `rmse_uplift` / `rmse_ratio` accept rmse values scored on different n — a mis-paired fold could produce an "uplift" (metrics · nit) | n-equality refusal; test; M23 |
| R16 | `split()` recorded `spatially_blocked=True` as a LITERAL — the enforced declaration and the manifest value could diverge (splitter · should-fix, survived) | `type(self).spatially_blocked`; `FoldAssignment` validates a bool; test; M19 |
| R17 | swapped (lat, lon) input on the real corpus is (n, 2), finite, splits into two plausible folds with wrong distances (splitter · should-fix) | range check refusing BY NAME ("wrong column order"); test; M20 |
| R18 | labels and folds never cross-checked — a splitter could record labels that contradict its folds (splitter · should-fix) | every fold's test set must equal the rows of ONE label; test; M21 |
| R19 | `Fold.min_train_test_km` accepted NaN / negative → `NaN` token in JSON (splitter · nit) | finite, non-negative, by name; test |
| R20 | declaration check is class-creation-only; `FoldAssignment` accepted a non-bool (splitter · nit) | bool validated on the record; comment states what E2.5 reads (class attribute + validated record) |
| R21 | "first concrete class" wording — the refusal fires at the first SUBCLASS, abstract or not (splitter · nit, survived) | comment + walkthrough corrected |
| R22 | `n_rows == 0`, zero folds passed vacuously (splitter · nit) | refused (≥ 2 rows, ≥ 1 fold) |
| R23 | no coordinate fingerprint — a same-length reordered matrix would be measured against the wrong stations (splitter · nit) | `coords_sha256` recorded; the leakage assertion refuses a mismatch; test; M22 |
| R24 | "strictly-within-cluster site folds refuse at ANY min_pairs" false for the two E folds that keep 4–10 km lags (walkthrough · should-fix; no verdict — re-derived) | corrected: hold E-8 / hold E-6 fit at min_pairs ≤ 10 / ≤ 5 as PURE NUGGET; the conclusion (no comparison) unchanged |
| R25 | LOCO-train-W fits at min_pairs ≤ 3, not "only at 1" (walkthrough · nit; re-derived) | corrected |
| R26 | E-alone 0–2 km mean lag is 0.578, not 0.7 (walkthrough · nit; re-derived) | corrected (0.58 / 5.27 / 7.25) |
| R27 | "650 words" not reproducible; the identity claim holds (walkthrough · nit; re-derived) | the count dropped, the identity kept |
| R28 | site-mates are 5–7, not 6–7 (E-6 has five) (walkthrough · nit; re-derived) | corrected, with the five site diameters |
| R29 | buffered LOSO ≡ site-out only for buffers in (2.31, 4.61) km; the quoted n_train were within-cluster counts (walkthrough · nit; re-derived) | corrected |

Two more findings the author made while writing the §1 code, before any
review: the declaration check read `cls.__dict__` and would have falsely
refused a subclass of a declared splitter (fixed to MRO lookup + test +
M11); and `FoldAssignment` had no index-range / every-row-once invariants
(added + test + M12/M13). And one verified correction to a docstring
claim: pydantic 2.13.4's `model_dump(mode="json")` keeps a typed float
field's `inf` (so the artifact path writes the non-standard `Infinity`)
but renders one nested in a dict field as `null`; `model_dump_json()`
writes `null` — the metrics docstring now says exactly that.

**What the review did NOT cover, stated:** the §2 adversarial review the
prompt schedules is still ahead; this pass had no lens on the docs-only
edits (BACKLOG/CLAUDE.md/PATTERNS.md/handoffs) beyond the P2.C item
checks recorded in BACKLOG §2.

### Suite trajectory

374 → **402 passed, 2 skipped** (+28: 17 splitter, 11 metrics).

### Deliberately missing (§2 adds)

The runner (`CrossValidationRunner`, per proposal 1C), the within-cluster
splitter(s) per Karl's 1B pick, `RandomKFoldSplitter` (declared
`spatially_blocked = False`) and its known-answer leakage number, the
`Estimator.input_kind` routing declaration (2C), the `CVScore` /
`RunManifest` / `engine.py` extensions under the Phase-0 revision protocol
(2B), `scores_first_visible` and the end-to-end chain assertion (2D), the
per-fold baseline join and refusal records, and the adversarial review with
its four probes.

---

## §1-STOP — two decisions (Karl's; proposals above, then WAIT)

1. **Within-cluster blocking (1B):** A + B side by side (recommended); or B
   alone; or A alone; or C. Parameters as proposed unless changed.
2. **The Phase-0 seam (1C):** (i) runner takes registry + splitters, engine
   holds a registry, `CrossValidator` type retired, `input_kind` declared on
   the ABC (recommended); or (ii) per-estimator callable with the loop in
   the engine.

Not proceeding to §2 until both are answered.
