# E4.2 — minable-footprint rasters + the scenario difference map

**Landed 2026-08-22.** Specification: the consolidated E4.2 paste (the
operative text — `docs/prompts/PHASE4-track-E-prompts.md` exists on disk
and is UNTRACKED, so it is not in the INSTRUCTIONS channel; nothing in the
paste was found to diverge from it beyond what E4.1 had already settled).
Reading order: `engine/prospectivity/economics/writer.py` →
`surfaces/writer.py: write_cog` (the one writer, now public) → `engine.py:
_write_economics` → `tests/test_economic_writer.py`.

**STATED FIRST (E4.1's measurement, carried):** every footprint raster this
task writes on today's surfaces is UNIFORMLY TRUE — 2,880 ones, 520 NaN —
and every difference raster is uniformly "minable under both". That is a
property of the placeholder cutoffs' relation to the training mean, not of
the seafloor, and the constructed fixture is the only observer of the
encoding and the set difference; the real-data tests assert identity,
count, and the stated uniformity.

| commit | what | suite |
|---|---|---|
| 1 | the 12 footprint rasters: one writer reused, the mask kept apart from the footprint, the two-reason verdict per reason on every file, the machine-resolvable association | 591 → **598** |
| 2 | the 6 difference rasters: the three-state encoding, the meaning in the tags, the measured zero with its reason | see §2 |

```text
  ScenarioFootprints ──► write_footprints ──► <out>/economics/
       (E4.1)                 │                 footprint__<scenario>__<estimator>__z<z>.tif   × 12
                              │                 economics.footprints.json   ← THE ASSOCIATION (+ carrier 2)
                              │                 data_origin.yaml            ← the audit's marker, merged
                              └── write_cog (surfaces/writer.py — E3.1+2's, the only COG writer)
                                    tags: per-reason verdict · encoding · cutoff WITH origin · semantics ·
                                          claim_failing_preconditions · grid hashes      ← carrier 1
```

## §0 — Premises verified

| claim | verified |
|---|---|
| the verdict, the computation and the measurement exist | `economic_watermark_verdict`; `CutoffEconomicModel`; `test_economic_cutoff.py` pins 2,880/2,880 and the empty difference |
| 2 × 3 × 2 = 12 footprint files; 6 difference files | counted from `scenarios()`, the registry's three estimators and `DEFAULT_CONFIDENCE_LEVELS` — before writing, and after (the test counts both) |
| the prompts file is untracked | `git ls-files` empty; `??` in `git status` |
| the G4.1 entry carries the bracket-nothing ask | BACKLOG §1 "G4.1's ASK, SHARPENED" — not duplicated |
| one COG writer exists to reuse | `surfaces/writer.py: _write_raster`, private until now — exposed as `write_cog`, not copied |

## §1 — Commit 1: the footprint rasters

**The grid is the surface's, asserted against the recorded identity.**
`CutoffEconomicModel.apply` now records `grid_identity =
inputs.grid.identity()` at computation, and the writer refuses a grid whose
identity differs, naming the differing keys. The mask is asserted against
the SURFACE's mask exactly (`~isfinite(mu)`), and a footprint that marks a
masked cell minable is refused by name.

**The mask and the footprint are different things.** nodata (NaN) =
UNDEFINED (no covariates); 0.0 = NOT minable with covariates present; 1.0 =
minable. The constructed fixture is what separates them: its raster reads
`[[0, 1, 1, 1], [0, 1, 0, NaN]]` — one NaN, three zeros, four ones — where a
conflating writer would put a 0 at `[1, 3]`. The encoding sentence is in
every file's tags and in the association record.

**The two-reason verdict, per reason, on every file.** `watermark_reason_terrain`
and `watermark_reason_economic_parameters` are SEPARATE tags, each derived
from its own reason's state (never from the combined verdict), beside
`watermark_reasons_unlifted` and the combined `watermark` text. The collapse
observer has both halves: today both tags read UNLIFTED; a Checkpoint-1
fixture (MEASURED DEM) reads `lifted` / `UNLIFTED` — asserted per reason,
not by count — while `data_origin` stays AUTHORED (the lattice's lossy
answer, unchanged; the tags say what moved). Carrier 2, the association
record, carries the same verdict as data.

**The association is machine-resolvable.** The filename
`footprint__<scenario>__<estimator>__z<z>.tif` is readable, and nothing
resolves from it: `economics.footprints.json` maps every basename to its
(kind, scenario, estimator, z, sha256, counts, verdict), and the same facts
sit in each file's tags — the test resolves from the record and checks the
tags agree, parsing no name. `data_origin.yaml` classifies each raster with
its COMPUTED origin, its derivation, and the authorship it INHERITS from
Contract 4 (`author_inherited_from`) — never a new `author:`, which P2.0
does not make available to new work.

**The rasters land in `<out>/economics/`**, their own directory with their
own sidecars, so the surfaces' directory listing — which the manifest's
`output_hashes` is checked against in full — is untouched until E4.3 hashes
the economics block. The first draft wrote them beside the surfaces and the
end-to-end test caught 13 unhashed files: the right test doing its job.

**File inventory (measured):** 12 footprint rasters; `economics.footprints.json`;
`data_origin.yaml`. Each raster 2,880 ones and 520 NaN on today's surfaces.

### Tests (+7, `test_economic_writer.py`)

| test | asserts | separates |
|---|---|---|
| `…mask_and_the_footprint_are_different_things…` | the constructed raster `[[0,1,1,1],[0,1,0,NaN]]`; one NaN, three 0, four 1; NaN exactly the surface's mask | undefined from false |
| `…masked_cell_minable_or…another_grid_is_refused_by_name` | grid-identity refusal naming `stack_content_hash`; a masked cell marked minable refused; a surface set larger than the footprint set refused | — |
| `…both_reasons…per_reason_and_lifting_one_does_not_lift_the_other` | today UNLIFTED/UNLIFTED; after Checkpoint 1 lifted/UNLIFTED; `data_origin` still AUTHORED; carrier 2 agrees | a collapse that preserves the count |
| `…twelve_footprint_rasters…resolvable_from_the_record_not_the_name` | 12 = 2×3×2; every sidecar entry → an existing file whose sha256 recomputes and whose tags agree; the (scenario, estimator, z) set exactly; the origin sidecar cites the contract's authorship | name-parsing from record-resolution |
| `…every_footprint_shares_the_surfaces_grid_and_mask_exactly` | transform/CRS/size/driver/dtype equal to the SURFACE raster's; masks equal; 520 NaN; value set {1.0}; no "cog" in any tag | — |
| `…uniformly_true_as_stated_in_advance` | 2880/2880 in every file's tags; the slope note travels | — |
| `…deterministic_byte_for_byte` | a second write is byte-identical, sidecar included | — |

### Mutations 6/6

| # | mutation | caught by |
|---|---|---|
| W-M1 | per-reason tags derived from the COMBINED state (both read UNLIFTED after Checkpoint 1 — the count-preserving collapse) | the per-reason test: `lifted` expected for terrain |
| W-M2 | per-reason tags dropped (a single `watermark` flag) | the per-reason test: the tags are absent |
| W-M3 | masked cells written as 0.0 (mask conflated with the footprint) | the constructed encoding test AND the real-data mask test |
| W-M4 | grid-identity check removed | the refusal test: DID NOT RAISE |
| W-M5 | masked-cell-minable refusal removed | the refusal test: DID NOT RAISE |
| W-M6 | association sidecar not written | three tests (the resolution test, carrier 2 of the verdict test, determinism) |
