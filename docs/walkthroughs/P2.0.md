# P2.0 — Data-origin taxonomy (tasks a′–d)

**Audience:** the engineer of record. This walkthrough grows one section per
task; §a′ and §b are written, §c and §d land with their commits.

**Why P2.0 exists:** the 2026-08-08 origin-vocabulary audit
([docs/audits/2026-08-08-origin-vocabulary-audit.md](../audits/2026-08-08-origin-vocabulary-audit.md))
found ~24 mechanisms expressing "is this value real?" in unshared vocabularies,
two machine-readable origin fields (`backed_by`, `placeholder: true`) that
disagree in shape, and one latent guard hole (`data/fixtures/native/` books as
`real_data` under both path heuristics). `[06]`/`[18]` reached a production
build path as fabricated fixtures and were caught by a person, not a test.
P2.0 replaces that scatter with one declared taxonomy, one combine rule, and
(in P2.0d) an audit test + production-path guard.

Reading order: the audit → [origin.py](../../engine/prospectivity/provenance/origin.py)
→ [test_data_origin.py](../../tests/test_data_origin.py) → this file.

---

## a′ — The audit committed (99dd038)

The audit report was moved verbatim from the session transcript into
`docs/audits/2026-08-08-origin-vocabulary-audit.md`, with a header recording
the commit it was performed against (`34c86f4`), its point-in-time status
(code wins on disagreement), and its own classification under the scheme it
proposes: `data_origin: AUTHORED, author: model` — the taxonomy's first
classified artifact is the document proposing it. Two latent findings went to
BACKLOG (§3: corpus CSV bytes not hash-pinned by any manifest field, its own
task, deliberately not P2.0; §2: uncited literature-shaped numbers in the
contracts README). No code.

## b — The enum and the propagation rule

**Standing P2.0 decisions (Karl, 2026-08-08) — recorded here because
`origin.py`'s docstring cites this section:**

1. **LITERATURE is its own enum member** — five values. Its evidence bar is a
   citation specific enough to LOCATE the number (document +
   table/section/page); "TS-6" alone is insufficient. The `ts6_finding`
   strings in covariates.yaml already fail that bar; P2.0c reports them as
   evidence gaps rather than closing them by guessing.
2. **`author: unrecorded` is admissible only for values predating the rule**;
   P2.0d freezes the file list, so the set only shrinks. New work declares
   `model` or a named person.
3. **Origin is declared per entry**; file-level origin is computed by
   `combine_origins`, never hand-written. The corpus CSV gets no origin
   column.
4. **Nothing is renamed.** The `synthetic_*` / `src_synthetic_*` misnomer is
   recorded in docstrings, not corrected by renames.
5. **The declaration-based guard lands in P2.0d**; P2.0c widens the path
   predicates as interim cover only.

```
        engine/prospectivity/provenance/origin.py
   ┌───────────────────────────────────────────────────┐
   │  DataOrigin (str Enum)                            │
   │  MEASURED ─► DERIVED ─► LITERATURE ─► SYNTHETIC   │
   │                                      ─► AUTHORED  │
   │  most real ──────────────────────────► least real │
   │                                                   │
   │  ORIGIN_ORDER_MOST_REAL_FIRST  (the one ordering) │
   │  combine_origins([...]) ─► least-real input wins  │
   │  validate_author: "model" | named | "unrecorded"  │
   └───────────────────────────────────────────────────┘
        │ P2.0c: contracts declare data_origin per entry;
        │        file-level origin = combine over entries
        ▼ P2.0d: audit test walks the tree; production-path
                 guard + watermark derive from declarations
```

The ordering measures **provenance distance from a measurement this repo holds
and can hash — not trustworthiness**. DERIVED outranks LITERATURE because it
traces to a hashed input by a recorded formula; LITERATURE traces to a
publication with no file in hand. `combine_origins` returns the least-real
input (a real corpus does not launder a synthetic terrain) and serves two
scales with the same rule: artifacts → run, entries → file. Empty input
raises — a silent default is the failure mode the module exists to prevent.

**Deliberately not a pattern.** A value object with a monotone combine — no
Strategy, no Registry. One ordering exists and no second is coming, so a seam
would be indirection without a variation point (the PATTERNS.md §3 standard).
**PATTERNS.md is deliberately unchanged by this task** for that reason.

**Per-file:**

| File | What it is |
|---|---|
| `engine/prospectivity/provenance/origin.py` | The taxonomy's single code home: enum (values = names, so YAML `data_origin: MEASURED` round-trips loudly), ordering constant, `combine_origins`, author vocabulary (`AUTHOR_MODEL`, `AUTHOR_UNRECORDED`, `validate_author`). Docstring carries the evidence bar per label, the two-scales rule, the pattern note, and the "synthetic"-terminology collision. |
| `tests/test_data_origin.py` | 14 property tests (17 collected with the parametrized rejection cases), exhaustive over the finite domain (25 pairs, 125 triples) rather than sampled. The ordering is pinned as a literal first so property tests may derive expectations from the constant without tautology. |

**Test inventory (17 new collected; suite 202 → 219, 217 pass + 2 known
skips):**

| Test | Asserts, in plain English | Rule enforced |
|---|---|---|
| `test_the_ordering_is_the_contracted_five_member_sequence` | The constant is exactly (MEASURED, DERIVED, LITERATURE, SYNTHETIC, AUTHORED) | The P2.0b ordering contract; anchors all derived expectations |
| `test_every_enum_member_is_in_the_ordering_and_nothing_else_is` | Completeness both directions, no duplicates; failure names the member | Registry-style completeness (CovariateRegistry precedent) |
| `test_combine_returns_the_least_real_of_every_ordered_pair` | All 25 pairs: the later-in-ordering input wins | Least-real-wins |
| `test_a_measured_corpus_does_not_launder_a_synthetic_terrain` | MEASURED + SYNTHETIC = SYNTHETIC, hard-coded | The motivating example, independent of the constant |
| `test_authored_absorbs_every_origin_including_itself` | AUTHORED wins against every member (itself included), both argument orders, each failure naming the pair | Absorption |
| `test_combine_is_commutative_over_all_pairs` | 25 pairs, order-free | Monotone combine |
| `test_combine_is_associative_and_flat_call_matches_pairwise_over_all_triples` | 125 triples: both pairwise reductions and the flat call agree | Monotone combine |
| `test_combine_is_idempotent_for_singletons_and_repeats` | combine([x]) = x, combine([x,x,x]) = x | Monotone combine |
| `test_combine_of_empty_input_raises_instead_of_defaulting` | Empty input → ValueError with the refusal message | No silent origin |
| `test_combine_of_an_unknown_string_raises_naming_the_value` | "FABRICATED" → ValueError naming it | Loud on unknown labels |
| `test_combine_accepts_string_values_interchangeably_with_members` | Raw strings, and mixes of strings with members, coerce and combine | YAML round-trip |
| `test_author_vocabulary_constants_are_the_contracted_tokens` | `AUTHOR_MODEL == "model"`, `AUTHOR_UNRECORDED == "unrecorded"` | The author vocabulary spec |
| `test_validate_author_accepts_model_unrecorded_and_a_named_person` | All three accepted forms pass through | Author vocabulary |
| `test_validate_author_rejects_empty_whitespace_and_non_strings` | "", "   ", None, 7 all raise (parametrized, so a regression names its case) | No authorless AUTHORED |

**Mutations run (all reverted, suite green after):**

| Mutation | Result |
|---|---|
| `DataOrigin.LITERATURE` dropped from the ordering constant | Completeness test **FAILED** with `ordering constant is missing enum member(s): ['LITERATURE']` — names the member ✓ (literal-pin test also failed) |
| `combine_origins` flipped to most-real-wins (`max` → `min`) | 4 tests **FAILED**: cross-product pairs, the laundering example, AUTHORED absorption, string-coercion ✓ |
| Empty-input raise replaced with silent `return DataOrigin.AUTHORED` | `test_combine_of_empty_input_raises_instead_of_defaulting` **FAILED** (DID NOT RAISE) ✓ |

**Adversarial review before commit:** a three-agent verification pass (spec
conformance; CLAUDE.md testing-convention audit; internal consistency against
the audit and house style) returned 10 findings — 0 must-fix, 3 should-fix, 7
nits — all addressed: loop assertions now name the failing pair, the module's
P2.0c/d division of labour matches the prompt set, its decision citations
resolve to this section instead of to a list that existed only in chat,
`Iterable` imports from `collections.abc` per house style, and two test names
were tightened to match their bodies.

**Hand-check:**

```bash
.venv/bin/python -c "
from engine.prospectivity.provenance.origin import DataOrigin, combine_origins
print(combine_origins([DataOrigin.MEASURED, DataOrigin.SYNTHETIC]))   # SYNTHETIC
print(combine_origins(['MEASURED', 'DERIVED']))                        # DataOrigin.DERIVED
combine_origins([])                                                    # raises ValueError
"
```

**Deliberately missing from b (by instruction, arriving in c/d):** no marker
applied to any file, no manifest wired, no watermark boolean — the watermark
derives from origin in P2.0d, because two sources of truth for one fact is the
defect being designed out.

**What c adds next:** per-entry `data_origin:` markers across the mixed
contracts (file-level origin computed by `combine_origins`, never
hand-written), the DO-NOT-TOUCH hash-pinned list respected, the interim
widening of both path predicates to bare `fixtures`, metadata-only contract
version bumps with both manifests' hash changes reported, and origin counts in
the corpus and feature-stack manifests.

## c — Apply the origin markers (commits: section 0 alone, then the marker work)

### Section 0 carry-overs (committed alone, af0850d)

**0.1 — the tautology-pin mutation.** Swapped DERIVED and LITERATURE adjacent
in `ORIGIN_ORDER_MOST_REAL_FIRST`. Both halves demonstrated: the
pinned-literal test failed naming the literal expected sequence (`At index 1
diff: LITERATURE != DERIVED`, full literal in the assertion source), and **all
16 other tests stayed green under the swap** — the tautology shown, not
assumed. Reverted exactly; green. This is the mutation that matters because
the realistic threat is an argued reorder ("TS-6's published figures should
outrank our arithmetic"), not an accidental deletion.

**0.2 — author allow-list.** `validate_author` now checks the closed
`ALLOWED_AUTHORS = (model, karl, isaac, unrecorded)` instead of accepting any
string as a named person — free text let a typo'd `"moddel"` validate as a
person's name, the model-authored origin failing toward the safe-looking
answer. Unknown authors raise naming the value and the admissible set;
completeness is literal-pinned both directions. Mutation: silent acceptance
failed the rejection test on ids `[typo-moddel]` and `[case-Isaac]`; reverted.

### What was marked (the marker commit)

```
  YAML/JSON contracts            in-entry / file-level data_origin (+author/citation)
  ├─ source_queue.yaml   v3→v4   file AUTHORED·unrecorded; 9 entries MEASURED,
  │                              [18][19] LITERATURE+citation, 2 context entries
  │                              data_origin: null → [KARL] decision (BACKLOG §2)
  ├─ normalization.yaml  v1→v2   file AUTHORED·unrecorded; screening block
  │                              LITERATURE, citation "TS-6 (2010), Table 2" —
  │                              declared BESIDE `screening:` (apply_screening
  │                              iterates its items; marker keys stay out)
  ├─ covariates.yaml     v3→v4   file AUTHORED·unrecorded; 8 enabled entries
  │                              AUTHORED·unrecorded (windows = engineering
  │                              stand-ins); 4 candidates LITERATURE with the
  │                              citation gap RECORDED in the citation field
  ├─ scenarios.yaml      v2→v3   file AUTHORED·unrecorded; <<<PLACEHOLDER>>>
  │                              markers ABSORBED (info tails kept)
  ├─ ts6_reference.yaml  v1→v2   file AUTHORED·unrecorded (single-entry file)
  ├─ schema.json         v4→v5   AUTHORED·unrecorded, in-file
  ├─ exclusions.geojson  (n/a)   AUTHORED·unrecorded, in-file (not hash-pinned)
  └─ study_area.geojson  FROZEN  declared in the contracts README row #2 —
                                 hash-pinned, never edited for a marker
  Python fixture modules         DATA_ORIGIN (+DATA_AUTHOR) constants + docstring
  ├─ tests/fixtures/rasters.py   SYNTHETIC (the only qualifying module)
  └─ adapters/normalizers/sample_source.py  AUTHORED·unrecorded + misnomer notes
  Hand-typed CSVs                sidecar data_origin.yaml, never the binary
  ├─ data/fixtures/native/       3 × AUTHORED·unrecorded + misnomer note
  └─ tests/fixtures/samples/     3 × AUTHORED·unrecorded; so268_nodules_sample
                                 MEASURED (verbatim excerpt: DOI + parent hash)
```

### Absorbed vs left, with reasons

| Marker | Verdict | Why |
|---|---|---|
| `<<< PLACEHOLDER >>>` / `<<< ILLUSTRATIVE PLACEHOLDERS >>>` (scenarios.yaml) | **ABSORBED** | Pure origin claims, now stated once in the file-level `data_origin`; informational comment tails ("~upper-half of TS-6 range") kept |
| `illustrative_only` flags | **LEFT** | Watermark/publication policy consumed by `EconomicModel` and pinned by `test_contracts_parse`; the origin claim inside it now lives in `data_origin` |
| `[GEOLOGY — ISAAC]` tags (all 29) | **LEFT** | Task routing — who fills what, and when |
| `placeholder: true` (study_area.geojson) | **LEFT** | The file is hash-pinned; its origin declaration lives in the contracts README row |
| Header banner "ALL VALUES ARE ILLUSTRATIVE PLACEHOLDERS" (scenarios.yaml) | **LEFT** | Human-readable policy statement tied to the watermark rule, not a stacked machine field |
| null-until-download convention (source_queue) | **LEFT** | Track G's fill-on-download workflow; `data_origin: null` on the two context entries deliberately reuses it for the pending classification decision |
| `synthetic_*` / `src_synthetic_*` names | **LEFT** (decision: nothing renamed) | Misnomer recorded in adapters.py/normalizers.py docstrings and both sidecars |
| `backed_by` | **LEFT** this task | Predicate widened only (§4); P2.0d replaces path inference with the declaration |

### Evidence gaps carried (not closed, not downgraded)

- The four `ts6_finding` strings: labeled LITERATURE, `citation:` records
  "table/page NOT LOCATED". BACKLOG §1.
- depth's `geology_note` (TS-6 4,100–4,200 m) and scenarios' TS-6 anchors
  (grades, distribution): cited to TS-6 with no table/page. BACKLOG §1.
- `src_bathymetry_primary` / `src_deepdata_public_context`: classification
  genuinely undecided (MEASURED survey product vs LITERATURE compiled grid) —
  `data_origin: null` + `[KARL]` comment, BACKLOG §2. The bathymetry answer
  drives the Checkpoint-1 watermark, so it is Karl's call, not an improvised
  label.
- **Prospective MEASURED declarations** for the seven undownloaded sources
  ([02][03][04][06][11][12][14]): the class is known from each DOI, but
  MEASURED's evidence bar (input-file SHA-256) completes only on download —
  same fill-on-download status as their null license/hash fields. Hard
  consequence recorded for P2.0d (BACKLOG §1 addendum): the declaration
  guard must require the evidence COMPLETE (declared MEASURED **and** a real
  hashed file), never the label alone — a `data_origin: MEASURED` on `[06]`
  cannot distinguish real Dryad data from the fabricated fixture that
  motivated this guard family.
- `author: unrecorded` set for P2.0d to freeze: source_queue.yaml,
  normalization.yaml, covariates.yaml, scenarios.yaml, ts6_reference.yaml,
  exclusions.geojson, master_observations.schema.json, study_area.geojson
  (via README row), data/fixtures/native/data_origin.yaml,
  tests/fixtures/samples/data_origin.yaml (3 of 4 files),
  tests/fixtures/{adapters,normalizers,sample_source}.py.

### Hash accounting (Section 5) — two causes, separated

| Artifact | Old `content_hash` | New `content_hash` |
|---|---|---|
| `data/corpus/manifest.json` | `sha256:372be3bb381385fbbeacdcfb5bd407807e60ddedcd522737c7f9d350dbbbf32f` | `sha256:0227d6df608ee23476c7f5915bede82f1ffb360c542e33152386257a2fd07fd9` |
| `outputs/e1.4/stack/provenance.json` (untracked, regenerated locally) | `sha256:0b6ede5bcfae4da8b9046f20b35a2e6e60e77abdec3f876a34fd5932a00249fa` | `sha256:c0251d771d759d854a48c18bfeffc5404da1e507012e81bacb9260efac4fb2be` |

Cause 1 — contract version bumps flowing through `contract_versions`
(schema 5, covariates 4, normalization 2). Cause 2 — the new origin fields
(`admitted_rows_by_data_origin` + per-source `data_origin`;
`dem_data_origin` + `layers_by_data_origin`). The corpus CSV is
**byte-identical** (git diff empty) and every `backed_by` value is unchanged
(`real_data` ×2). Pins updated, named: `docs/diagrams/README.md` pinned the
old corpus hash as a staleness sentinel — updated LOUDLY with the
metadata-only rationale, old pin kept visible; the three version-pinning
tests (`test_contracts_parse`, `test_covariate_registry`,
`test_corpus_manifest`) and `test_covariate_stack`'s `registry_version`
assert were updated in the same commit. No test pins either content_hash.

**Deviation, reported:** Section 3's verify command
(`git diff data/sources/ data/aoi/ data/corpus/` empty) cannot pass as
written — Section 3 itself routes the `.tab` origins into
`source_queue.yaml`, which lives in `data/sources/`, and `exclusions.geojson`
(not hash-pinned) carries in-file markers per Section 2. Verified at file
level instead: both `.tab` files, `study_area.geojson`, and both
`data/corpus/` files untouched by hand; `manifest.json` changed only via the
deliberate rebuild above.

### Origin counts in the manifests (Section 6)

`CorpusManifest.admitted_rows_by_data_origin` — computed from each
contributing source's source_queue **declaration** (`_declared_data_origin`
raises on a missing or unknown declaration; never a silent unknown):
`{"MEASURED": 108}`. `FeatureStackManifest.dem_data_origin` (declared by the
caller, required keyword — no default, since SYNTHETIC-by-default would
mislabel real GEBCO at Checkpoint 1) + `layers_by_data_origin` computed as
`combine(DERIVED, dem_origin)` per layer: `{"SYNTHETIC": 8}` on the fixture
DEM — a synthetic DEM's features never launder into DERIVED.

### Mutations run in c (all reverted, suite green after)

| Mutation | Result |
|---|---|
| §0.1 adjacent swap DERIVED↔LITERATURE in the ordering | Pin **FAILED** naming the literal sequence; 16 property tests stayed GREEN (tautology demonstrated) ✓ |
| §0.2 `validate_author` accepts unknown strings silently | Rejection test **FAILED** on ids `[typo-moddel]`, `[case-Isaac]` ✓ |
| §4 `_require_production_path` reverted to two-part predicate | `..._raises_for_a_data_fixtures_native_path` **FAILED** ✓ |
| §4 `_backed_by` reverted to two-part predicate | `test_backed_by_books_any_fixtures_path_as_fixture...` **FAILED** ✓ |
| §6 `_declared_data_origin` raise replaced with silent `"unknown"` | `..._raises_for_missing_or_unknown_declarations` **FAILED** ✓ |
| §6 `layer_origin` copies the DEM origin instead of combining with DERIVED (found by the adversarial review — the SYNTHETIC-only fixtures could not distinguish copy from combine) | `test_layers_from_a_measured_dem_are_derived_not_measured` **FAILED** ✓ |
| §6 composition hardcoded to `{"MEASURED": total}` (found by the review — the homogeneous real corpus could not distinguish computed from hardcoded) | `test_composition_counts_are_computed_from_heterogeneous_declarations` **FAILED** ✓ |

### Marker forms, stated once for P2.0d's walker

1. YAML/JSON contracts: `data_origin:` (+ `author:`/`citation:`) per entry
   and/or at file top level.
2. The one exception: normalization.yaml's screening block uses sibling keys
   `screening_data_origin`/`screening_citation`, because `apply_screening`
   iterates the `screening:` mapping's items and marker keys must stay out
   of that shape.
3. Python fixture/generator modules: importable `DATA_ORIGIN` (+
   `DATA_AUTHOR`) constants plus a docstring line.
4. CSV directories: a `data_origin.yaml` sidecar with one `files:` mapping of
   per-file declarations (both sidecars share this schema).
5. Hash-pinned files: declared in the contracts README row, never in-file.

### Adversarial review before the marker commit

Three agents (metadata-only claim / consumer safety / spec + test quality)
returned 12 findings — 0 must-fix, 6 should-fix, 6 nits. The two that
mattered were both **mutation-verified test gaps**: the stack tests only ever
passed SYNTHETIC, where copy-the-DEM-origin and combine-with-DERIVED
coincide (a MEASURED-DEM test now separates them — the Checkpoint-1
laundering direction); and the homogeneous real corpus could not distinguish
computed composition counts from a hardcoded total (a heterogeneous-origin
unit test over the extracted `_admitted_rows_by_origin` helper now can).
Also taken: the seven prospective-MEASURED declarations recorded as an
evidence gap with the P2.0d guard consequence (above); the dangling
"P2.C item 7" reference replaced with a BACKLOG entry; both sidecars
normalized to one `files:` schema; `_declared_data_origin`'s unknown-label
error now names the source; E1.4.md's hand-check command updated (dated)
for the new required argument. Declined: editing
`docs/handoffs/HANDOFF_claude_code_phase2.md` (untracked, Karl's authored
paste-source; it pins the old corpus hash at line 36 — flagged for Karl,
whose doc's own disclaimer says the repo wins).

**PATTERNS.md unchanged, deliberately:** markers, a loader helper, and two
manifest fields — no pattern was added. The declaration-vs-inference shift is
recorded here and lands as enforcement in P2.0d.

Suite after c: **226 passed, 2 known skips** (was 219+2; +7: the native-path
guard test, three manifest-origin tests, the `_backed_by` branch test, and
two stack-origin tests).

## d-1 — The audit test (the test only; guards and CLAUDE.md untouched)

[`tests/test_data_origin_audit.py`](../../tests/test_data_origin_audit.py):
without this, the taxonomy is documentation, and documentation is what failed
for `[06]`.

```
  git ls-files data tests/fixtures     ← ENUMERATION: git, never the resolver
        │ (26 tracked subjects)
        ▼
  collect_declarations()               ← CLASSIFICATION: all three P2.0c
    1 in-file  (yaml/json top-level      marker locations, collected in ONE
       data_origin; importable           pass so a two-location disagreement
       DATA_ORIGIN for .py fixtures)     is a visible CONTRADICTION, not
    2 sidecar  (data_origin.yaml         first-match-wins
       files: entry)
    3 queue-via-manifest (input_path
       -> the source's data_origin)
        │
        ▼
  audit() → unclassified (fails BY NAME unless in EXCLUSIONS)
          → contradictions (two locations, different origins)
          → invalid (unknown label; AUTHORED without an allow-listed
            author; LITERATURE without a citation)
  unrecorded_subjects() → must equal FROZEN_UNRECORDED exactly, both
            directions (the DELIBERATELY_UNREACHABLE mechanism: the gap
            only shrinks, and shrinking is a deliberate list edit)
```

**The anti-tautology structure is the design.** Enumeration comes from git;
classification from the resolver. Enumerating tracked files also dissolves
`__pycache__`/`.DS_Store` with **no directory-level exclusion** — git does
not track them, so they are never subjects. The accounting: 9 in-file +
7 sidecar + 2 queue-via-manifest + 8 explicit exclusions = 26 tracked files.

**Exclusions** (each one file, one reason): `study_area.geojson` (hash-pinned;
declared in the contracts README row — the one classified file none of the
three machine locations can reach, excluded with exactly that reason rather
than silently special-cased), the two generated corpus artifacts, the two
sidecars themselves (mechanism, not subjects), two prose READMEs, and the
empty `__init__.py`. A stale entry or one that shadows a real classification
fails its own hygiene test.

**The frozen `unrecorded` set** — 16 files, enumerated from §c. The scan
covers the walk trees plus the two `docs/contracts/` files explicitly
(`UNRECORDED_SCAN_EXTRAS` — they sit outside the walk but inside the frozen
set). `so268_nodules_sample.csv` (MEASURED) is correctly not a member.
`study_area.geojson`'s README-row declaration says unrecorded in prose, not
machine-readably, so it is not a scanned subject — noted in the constant's
comment.

**Negation fixtures** (tmp trees run through the SAME audit functions): an
unclassified `orphan.csv` is reported by name; an in-file/sidecar origin
disagreement is reported as a contradiction; `FABRICATED` and an authorless
AUTHORED declaration are reported invalid; an unpermitted
`author: unrecorded` is reported by name against the frozen set.

**Mutations run (all reverted, 11/11 green after):**

| Mutation | Result |
|---|---|
| **MANDATORY (the P2.0b tautology shape):** `audit()` filters its subject list to files the resolver classified — the walk collecting from the resolver | Real-tree completeness stayed **GREEN** (structurally blind on a clean tree), and `test_audit_reports_an_unclassified_file_by_name` **FAILED**: `AssertionError: assert () == ('orphan.csv',)` — an observer exists, so the audit is not structurally blind; the negation fixture is the observer ✓ |
| `data/corpus/manifest.json` removed from EXCLUSIONS | Completeness **FAILED**: `files with no origin classification in any of the three marker locations and no exclusion entry: ['data/corpus/manifest.json'] — classify each in the same commit that adds it, or add an explicit exclusion with a reason.` ✓ |
| `data/config/normalization.yaml` dropped from FROZEN_UNRECORDED | Frozen-set test **FAILED**: `files newly carrying author: unrecorded: ['data/config/normalization.yaml'] — 'unrecorded' names a frozen pre-P2.0 gap and is NOT available to new work; declare 'model' or a named person.` ✓ |

**Adversarial review before commit** (two agents: spec conformance;
blind-spot hunt with live probes). 10 findings — 1 must-fix, 5 should-fix, 4
nits — all taken. The must-fix was real and probe-demonstrated: a new file
with a clean top-level `author: model` hiding a NESTED entry-level
`author: unrecorded` passed the whole suite, because the scan read only
top-level keys while per-entry authors are the blessed marker form
(covariates.yaml carries eight). The scan is now recursive
(`_contains_unrecorded_author`), with the probe kept as a permanent negation
fixture. Also from the review: marker form 2 (normalization's
`screening_data_origin`/`screening_citation`) was invisible — it now resolves
as its own `path#screening` subject so deleting the citation fails the
evidence test; the contradiction definition widened from origin-only to
origin/author/citation (model-vs-person is the load-bearing distinction);
sidecar `files:` entries get the same stale-entry hygiene as EXCLUSIONS;
the study_area README-row declaration is pinned as literal text (it was
guarded by nothing); the `.py` resolver no longer crashes on a bogus
constant (surfaces in `findings.invalid` by file name instead); the extras'
declarations are validated; two test names tightened; one decision-number
citation corrected. The mandatory tautology mutation was re-run against the
final structure: same observer, same failure.

**Deliberately untouched:** both interim path guards (P2.0c §4 stays until
P2.0d-2 replaces path inference with this declaration mechanism), CLAUDE.md
(the authoring rule lands in d-2), the watermark (d-2). PATTERNS.md unchanged
— no pattern added.

Suite after d-1: **243 passed, 2 known skips** (+17).
