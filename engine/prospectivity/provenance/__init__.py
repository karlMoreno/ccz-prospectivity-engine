"""Provenance manifest emitter (AR-D05, AR-P09). The RunManifest *type* is
defined now (domain/results.py); ProspectivityEngine._emit_manifest() already
assembles it mechanically. A richer corpus-manifest emitter (per-source
hashes, licenses) is Phase 3 (E3.4).
"""
